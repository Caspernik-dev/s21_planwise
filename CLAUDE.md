# CLAUDE.md

Behavioral guidelines to reduce common LLM coding mistakes. Merge with project-specific instructions as needed.

## Frontend style reference

`design_example/` — эталон стиля для всего будущего фронта Planwise (Next.js + Tailwind + shadcn/ui).
При создании новых страниц/компонентов сверяться с этой папкой: цветовая палитра (`brand`/`neutral`/`accent`/`warm` в `tailwind.config.ts`), типографика (Inter + Onest), радиусы, тени (`shadow-card`/`hover`/`brand`), паттерны карточек с `ring-1`, sticky navbar с backdrop-blur, бейджи аудитории. Папка — только референс, не для запуска.

**Tradeoff:** These guidelines bias toward caution over speed. For trivial tasks, use judgment.

## 1. Think Before Coding

**Don't assume. Don't hide confusion. Surface tradeoffs.**

Before implementing:
- State your assumptions explicitly. If uncertain, ask.
- If multiple interpretations exist, present them - don't pick silently.
- If a simpler approach exists, say so. Push back when warranted.
- If something is unclear, stop. Name what's confusing. Ask.

## 2. Simplicity First

**Minimum code that solves the problem. Nothing speculative.**

- No features beyond what was asked.
- No abstractions for single-use code.
- No "flexibility" or "configurability" that wasn't requested.
- No error handling for impossible scenarios.
- If you write 200 lines and it could be 50, rewrite it.

Ask yourself: "Would a senior engineer say this is overcomplicated?" If yes, simplify.

## 3. Surgical Changes

**Touch only what you must. Clean up only your own mess.**

When editing existing code:
- Don't "improve" adjacent code, comments, or formatting.
- Don't refactor things that aren't broken.
- Match existing style, even if you'd do it differently.
- If you notice unrelated dead code, mention it - don't delete it.

When your changes create orphans:
- Remove imports/variables/functions that YOUR changes made unused.
- Don't remove pre-existing dead code unless asked.

The test: Every changed line should trace directly to the user's request.

## 4. Goal-Driven Execution

**Define success criteria. Loop until verified.**

Transform tasks into verifiable goals:
- "Add validation" → "Write tests for invalid inputs, then make them pass"
- "Fix the bug" → "Write a test that reproduces it, then make it pass"
- "Refactor X" → "Ensure tests pass before and after"

For multi-step tasks, state a brief plan:
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

Strong success criteria let you loop independently. Weak criteria ("make it work") require constant clarification.

---

**These guidelines are working if:** fewer unnecessary changes in diffs, fewer rewrites due to overcomplication, and clarifying questions come before implementation rather than after mistakes.

---

## Проект «Классный час» — зафиксированные решения

ИИ-генератор сценариев внеурочных занятий (хакатонный кейс 5). Полная спека и планы — см. «Документы проекта» ниже. Здесь — короткая фиксация, что любой Claude-агент должен знать до того, как откроет код.

### Архитектура (не трогать без обсуждения)
- **Монолит Next.js 15 (App Router) + TypeScript.** Без отдельного бэка. Вся серверная логика — Route Handlers и Server Actions.
- **БД: PostgreSQL 16 + pgvector** в Docker (`pgvector/pgvector:pg16`). Внешний порт `5433`, БД/юзер `kc`.
- **ORM: Drizzle** (а не Prisma — экономия RAM).
- **Auth: Auth.js v5 (next-auth beta)**, credentials provider, JWT-сессии, bcrypt cost=10.
- **Стиль фронта строго из `design_example/`** — палитра brand/neutral/accent/warm, Inter+Onest, shadow-card/hover/brand. Папка `design_example/` — референс, не запускается.
- **UI-примитивы:** shadcn-style собственные (Button/Input/Label/Card в `components/ui/`).
- **Менеджер: pnpm 9.** Линтер/форматтер: Biome (не ESLint/Prettier).
- **Деплой:** Docker Compose (Next standalone + Postgres). Целевой прод — VPS 2 vCPU / 4 ГБ RAM.

### Жёсткие ограничения
- **Локальные LLM запрещены.** Всё связанное с LLM — внешний API: GigaChat для chat + embeddings. Ключи в `.env.local` через `GIGACHAT_AUTH_KEY`/`GIGACHAT_SCOPE`. Не предлагать sentence-transformers, spaCy-модели, ollama и т.п. **Размерность эмбеддингов на проде — 2560** (миграция `0010` подняла все `embedding`-колонки `1024→2560`; старые заметки про «1024 dims» в Plan 3/7 устарели).
- **RAM-бюджет 4 ГБ** на проде. Никаких Puppeteer/headless Chrome (для PDF используем `@react-pdf/renderer`), Prisma engine, локальных ML-моделей, jvm/python-воркеров.
- **UI только на русском.** Без многоязычности.
- **Auth — email/пароль.** Без OAuth/magic-link/2FA (это явно out-of-scope MVP).

### Пайплайн генерации (вариант B — двухэтапный с пред-рекомендацией)
Зафиксирован в спеке. Любая альтернатива требует обсуждения.
1. Форма → 2. PII-анонимизация → 3. Pre-match: поиск похожих готовых в `shared_scenarios` (порог `SIMILARITY_THRESHOLD` env, дефолт 0.78, **обязательно калибровать** до демо) → 4. RAG retrieval (3 методички + 2 shared + опц. 1 из плана) → 5. Stream skeleton (GigaChat call 1) → 6. Stream details (GigaChat call 2) → 7. Validate (zod) + автонормализация хронометража + save → 8. Edit (TipTap-блоки + ↑/↓, **не drag-handle**) → 9. Like + opt-in shared → 10. Export PDF/DOCX.

### Политики приватности (критичные)
- **PII-детект:** локально, regex + словари RU-имён + правила. **Без проверок контрольной суммы СНИЛС/ИНН** (в плане воспитательной работы их не бывает).
- **Авто-анонимизация по умолчанию.** Отмена замены пользователем требует подтверждения с явным текстом «эти данные будут отправлены во внешний сервис GigaChat. Продолжить?» + чекбокс «понимаю», решение логируется.
- **Шаринг лайков — opt-in** (галка при лайке). Перед записью в `shared_scenarios` — повторный PII-чек уже на content сценария; если что-то найдено — не пускаем.
- **«Использовать как есть» из библиотеки сообщества создаёт КОПИЮ** (новая запись `scenarios` с `source_shared_id`), не ссылку. Редактирование не затрагивает оригинал.

### Технические нюансы (от которых легко наступить на грабли)
- **`partial-json` для стрима** с fallback на skeleton-loader, если падает.
- **Гибридный поиск:** `0.7 * cosine + 0.3 * BM25` (PG `to_tsvector`). Словарь `russian` может быть недоступен — fallback `simple` через env `PG_TSV_LANG`.
- **Rate-limit:** lazy cleanup при каждом INSERT в `rate_buckets` (без cron). `MAX_GENERATIONS_PER_DAY=10` per user + whitelist `DEMO_USER_EMAILS` (без лимита) — для демо жюри.
- **Календарь поводов** — статический массив `lib/calendar-events.ts`, не LLM-вызов.
- **`scripts/ingest-razgovor.ts`** — критический путь до демо, не опциональный.
- **Playwright E2E — НЕ в CI**, только manual перед демо. CI = `vitest run` (unit + integration).
- **Никаких raw SQL без `WHERE user_id = ?`** для пользовательских таблиц. Изоляция данных — критерий жюри.

### Документы проекта
- **Brief кейса:** `klassniy-chas-brief.md`
- **Spec (утверждён 2026-05-20):** `docs/superpowers/specs/2026-05-20-klassniy-chas-design.md` — единственный источник истины по продукту.
- **Планы:** `docs/superpowers/plans/2026-05-20-plan-N-*.md` — 9 последовательных фаз (1 Foundation … 9 Demo-readiness). Все ГОТОВЫ.
- **Стиль-референс:** `design_example/` (Next+Tailwind+shadcn, токены в `design_example/tailwind.config.ts`).
- **Backlog доработок:** `docs/backlog.md` — живой трекер оставшихся задач (что сделано / критично / фичи / прод / артефакты кейса). Обновлять при закрытии пунктов.

### Статус реализации (обновлять при завершении фазы)
- **Plan 1 «Foundation» — ГОТОВ.** Ветка `feat/foundation`, тег `foundation-done` (19 коммитов). Реализовано через subagent-driven (имплементер + spec-review + code-review на каждую из 14 задач + финальный холистический ревью).
  - Что работает: Next.js 15 монолит, дизайн-токены из design_example, shadcn-примитивы (Button/Input/Label/Card), Postgres+pgvector в Docker (pgvector ставится миграцией `0001`), Drizzle + 4 auth-таблицы, bcryptjs (TDD), Auth.js v5 credentials+JWT, middleware-гейт `/app/*`, страницы `/login` `/register` (server actions с обработкой NEXT_REDIRECT + AuthError + защита от open-redirect), защищённый `/app` shell с Navbar+logout, Biome, Vitest (6/6).
  - Гейты зелёные: `pnpm test` (6/6), `pnpm lint`, `pnpm build`, fresh `db:up && db:migrate`.
  - **Технический долг → Plan 8:** CSRF на `/app/logout`; rate-limit на login/register; `AUTH_URL` derive из request; auth integration-тесты; таблицы `sessions`/`verificationTokens` пока пустые (оставлены под будущий OAuth). Edge-bundle тянет `postgres` через adapter — работает (JWT-стратегия), но при переходе на `strategy:'database'` сломается.
  - **GigaChat-ключи** уже в `.env.local` (`GIGACHAT_AUTH_KEY` = base64 client_id:client_secret, отдельный client_id не нужен; `GIGACHAT_SCOPE=GIGACHAT_API_PERS`).
- **Plan 2 «Generation v0 single-shot» — ГОТОВ.** Ветка `feat/generation` (от `foundation-done`), тег `generation-v0-done`. Реализовано через subagent-driven (имплементер + spec/quality-ревью).
  - Что работает: схема БД `scenarios`/`scenario_versions`/`generations` (миграция `0002`); zod `ScenarioContent` + `generationInputSchema` (`lib/scenario/schema.ts`); справочники `lib/scenario/options.ts`; нормализация хронометража `lib/scenario/normalize.ts` (TDD); промпт-builder `lib/scenario/prompt.ts`; GigaChat-клиент `lib/gigachat/*` (OAuth `ngw.devices.sberbank.ru:9443/api/v2/oauth` → access_token с in-memory кэшем, refresh за 60с; chat `/api/v1/chat/completions`, без стрима); оркестрация `lib/scenario/generate.ts` (parse+strip-fences → zod → 1 repair-pass → normalize); server action `app/app/new/actions.ts`; форма `/app/new`; read-only `/app/scenarios/[id]` с изоляцией по `user_id`; дашборд `/app` со списком и кнопкой «Создать».
  - Гейты зелёные: `pnpm test` (35/35, 8 файлов), `pnpm lint`, `pnpm build`. **Реальная генерация подтверждена** через прод-код (`generateScenario`) против живого GigaChat: валидный сценарий, хронометраж=30, repair-pass реально срабатывает.
  - **TLS-нюанс (важно):** GigaChat использует сертификаты «Минцифры РФ». TLS-обход реализован через `NODE_TLS_REJECT_UNAUTHORIZED=0` на уровне процесса (env `GIGACHAT_INSECURE_TLS=true`, dev-only) в `lib/gigachat/tls.ts` — НЕ через undici `dispatcher` (userland-undici Agent несовместим со встроенным fetch Node 24 → `UND_ERR_INVALID_ARG`). **Прод:** ставить корневой сертификат через `NODE_EXTRA_CA_CERTS` и держать флаг `false`.
  - **Технический долг → Plan 8:** rate-limit/whitelist на генерацию (`MAX_GENERATIONS_PER_DAY`); PII пока не подключён (Plan 5); `generation_meta`/`scenario_versions` пишутся, но не используются UI (версии — под будущий редактор Plan 4); embedding-колонка в `scenarios` НЕ добавлена (под RAG Plan 3); GigaChat нередко требует repair-pass — качество промпта калибровать позже.
  - **GigaChat-ключи** в `.env.local` (`GIGACHAT_AUTH_KEY` = base64 client_id:client_secret; `GIGACHAT_SCOPE=GIGACHAT_API_PERS`; + `GIGACHAT_OAUTH_URL`/`API_BASE`/`MODEL`/`INSECURE_TLS`). В worktree `.env.local` создаётся не из git — реальный ключ копировать из основного чекаута.
- **Plan 3 «RAG» — ГОТОВ.** Ветка `feat/rag` (от `generation-v0-done`). План: `docs/superpowers/plans/2026-05-20-plan-3-rag.md`. Реализовано через subagent-driven (имплементер + spec-review + code-review на каждую из 12 задач).
  - Что работает: GigaChat embeddings-клиент `lib/gigachat/embeddings.ts` (`EmbeddingsGigaR` 1024d, батчинг по `RAG_EMBED_BATCH`, mock-тесты); Drizzle `customType` vector/tsvector (`db/types.ts`); таблицы `rag_documents`/`rag_chunks` + `scenarios.embedding` (миграция `0003`) + HNSW/GIN/grade-индексы (кастомная миграция `0004_rag_indexes`); `lib/rag/hash.ts` (sha256-идемпотентность), `lib/rag/chunk.ts` (структурный чанкинг 300–800 ток., `section_kind`/`stage_idx`, TDD), `lib/rag/score.ts` (гибрид `0.7·cosine+0.3·BM25` + диверсификация ≤2/doc, TDD), `lib/rag/ingest.ts` (идемпотентное ядро, инъекция deps) + `lib/rag/ingest-db.ts` (Drizzle-адаптер, tsv через `to_tsvector(lang::regconfig)`), `lib/rag/retrieve.ts` (гибридный SQL `<=>`+`ts_rank`, фильтр grade/direction, fallback без направления при <topK, TDD); сериализатор `lib/scenario/to-markdown.ts` (TDD, frontmatter quoted — colon-safe round-trip); RAG в промпте `lib/scenario/prompt.ts` (`[RELEVANT_METHODOLOGY]`, `PROMPT_VERSION=v1-rag-2026-05-20`) + `generate.ts` (retrieval инъектируется, **сбой retrieval НЕ валит генерацию**, `usedChunkIds` в meta) + `actions.ts` (best-effort populate `scenarios.embedding`).
  - Скрипты: `scripts/gen-seed.ts` (12 эталонов через `generateScenario` с отключённым retrieval → markdown), `scripts/ingest-seed.ts`, `scripts/ingest-razgovor.ts` (по sitemap, только `1s`+`2m`, grade из URL, rate-limit, идемпотентно). Источник РоВ: см. memory `razgovory-source-structure`.
  - Гейты зелёные: `pnpm test` (69/72, 3 skip — guarded live smoke), `pnpm lint`, `pnpm build`, `tsc --noEmit`. Миграции применяются идемпотентно (схема-смоук зелёный).
  - **Решение пользователя:** seed-контент генерируется через GigaChat, не вручную; живые сетевые шаги (gen-seed, ingest-seed, ingest-razgovor) — **manual/opt-in вне CI** (флаг `RAG_LIVE=1` для guarded-смоук).
  - **Статус ingest на проде (проверено 2026-05-21):** ingest РоВ **ВЫПОЛНЕН** — корпус наполнен: `rag_documents=88` (77 `razgovor` + 11 `seed`), `rag_chunks=529` (510 razgovor), все диапазоны классов (1-2…10-11 + СПО), 0 чанков без `embedding`/`tsv`, оператор `<=>` живой (self-NN=0). «Опора на методички» реально работает. **Калибровка `SIMILARITY_THRESHOLD` ВЫПОЛНЕНА** — порог `0.78 → 0.72` (см. пост-milestone блок «Контекст генерации + библиотека»). Эмбеддинги — 2560-dim (миграция `0010`).
  - **Технический долг:** у чанков «Разговоров о важном» `direction=null` (в URL направление не размечено) → фильтр по направлению для РоВ-чанков не работает, опора на cosine + fallback; `scenarios.embedding` без HNSW-индекса (под будущий pre-match); pre-match по `shared_scenarios` и калибровка `SIMILARITY_THRESHOLD` — вне scope (план Likes/Shared); токены оцениваются эвристикой `chars/3` (локальный токенайзер запрещён).
- **Plan 4 «Редактор сценария» — ГОТОВ.** Ветка `feat/editor` (от `rag-done`), тег `editor-done`. План: `docs/superpowers/plans/2026-05-20-plan-4-editor.md`. Реализовано через subagent-driven (имплементер + spec-review + code-review на каждую из 6 задач + финальный холистический ревью). Согласованный scope: структурный блочный редактор (НЕ TipTap/rich-text — контент остаётся строго `ScenarioContent`, нет HTML/XSS), explicit save (НЕ авто-сейв), минимальная точечная регенерация активности.
  - Что работает: `lib/scenario/edit-ops.ts` (чистые иммутабельные `moveStage`/`moveActivity`, TDD); `lib/scenario/regenerate.ts` (регенерация ОДНОЙ активности через GigaChat + RAG-чанки, repair-pass, валидация `activitySchema`, DI `chat`, зеркалит `generate.ts`, TDD); примитив `components/ui/textarea.tsx`; server actions `app/app/scenarios/[id]/actions.ts` — `saveScenarioAction` (zod-валидация content, изоляция `user_id` на load И update, UPDATE+снапшот `scenario_versions` в одной транзакции, `revalidatePath`) и `regenerateActivityAction` (изоляция, RAG retrieve best-effort, лог в `generations`); клиентский редактор `editor.tsx` (правка title/goals/materials/stage.title/duration_min/activity.text/questions, ↑/↓ reorder этапов и активностей с disable на границах, 🎲 точечная регенерация, dirty-индикатор, explicit Save); страница `[id]/page.tsx` рендерит редактор вместо read-only.
  - Гейты зелёные: `pnpm test` (83 passed, 3 skip — guarded live), `pnpm lint`, `tsc --noEmit`, `pnpm build`.
  - **НЕ выполнено (ручной шаг перед демо):** живой браузерный UAT golden-path (правка → save → reload → reorder → 🎲 с живым GigaChat → проверка 404 на чужой сценарий). Проверено только статикой (тесты/типы/сборка). 🎲 требует живого ключа GigaChat.
  - **Технический долг → Plan 8:** rate-limit на `regenerateActivityAction` (LLM-вызов без лимита); `scenario_versions` пишутся без retention/pruning и без UI-истории версий; добавление/удаление этапов и активностей в редакторе НЕ реализовано (вне scope Plan 4); `latencyMs` для regen не замеряется (пишется `null`).
- **Plan 5 «Экспорт PDF/DOCX» — ГОТОВ.** Ветка `feat/export` (от `editor-done`), тег `export-done`. План: `docs/superpowers/plans/2026-05-20-plan-5-export.md`. Реализовано через subagent-driven (имплементер + spec-review + code-review на каждую задачу + финальный холистический ревью на Opus). Согласованный scope: server-side рендер в одном GET route, общая нейтральная модель блоков под оба формата, полный методический бланк, PT Sans вшит в PDF, изоляция по `user_id`.
  - Что работает: `lib/export/document-model.ts` (чистый маппер `ScenarioContent`+`ExportMeta` → `DocBlock[]`: heading/paragraph/bullets/metaTable; нумерация только нерефлексивных этапов; метки типов активностей; TDD 7 тестов); `lib/export/to-docx.ts` (билдер `docx` v9, шрифт Times New Roman, без embed); `lib/export/to-pdf.tsx` (`@react-pdf/renderer` v4, регистрация PT Sans regular+bold через `process.cwd()/assets/fonts`, `renderToBuffer`); `lib/export/index.ts` (диспетчер `isExportFormat` + `renderScenarioExport` → `{body,contentType,ext}`); route `app/api/scenarios/[id]/export/route.ts` (runtime `nodejs`, auth→401, валидация формата→400, изоляция `WHERE id AND userId`→404, Content-Disposition с RFC 5987 `filename*` UTF-8 + экранирование `'`/`*`); кнопки PDF/DOCX (plain `<a href>` download) + dirty-подсказка в тулбаре `editor.tsx`. Шрифты вшиты в репо `assets/fonts/` (OFL), попадают в standalone через `outputFileTracingIncludes`.
  - Гейты зелёные: `pnpm test` (95 passed, 3 skip — guarded live), `pnpm lint`, `tsc --noEmit`, `pnpm build` (route в выводе, без утечки тяжёлых либ в клиентский бандл; шрифты подтверждены в `.next/standalone/assets/fonts`).
  - **НЕ выполнено (ручной шаг перед демо):** живой браузерный UAT — скачать PDF и DOCX из редактора, визуально проверить кириллицу в PDF (PT Sans) и в DOCX (системный Times New Roman), проверить 404 на чужой сценарий. Проверено только статикой + smoke-тестами сигнатур (`%PDF`/`PK`).
  - **Технический долг → Plan 8:** rate-limit экспорта 100/день/user (§9) НЕ реализован (отложен вместе с остальной rate-limit инфраструктурой); экспорт берёт последнюю сохранённую версию из БД (несохранённые правки не попадают — отсюда dirty-подсказка); DOCX без полей/стилей страницы (дефолтные margins); экспортные либы пока не помечены `import 'server-only'` (защищены тем, что импортируются только из route/lib).
- **Plan 6 «PII-подсистема + загрузка плана» — ГОТОВ.** Ветка `feat/pii-plan-upload` (от `export-done`), тег `pii-plan-done`. План: `docs/superpowers/plans/2026-05-20-plan-6-pii-plan-upload.md`. Реализовано через subagent-driven (имплементер + spec/quality-ревью на каждую из 14 задач + финальный холистический ревью). Согласованный scope: полный вертикальный срез — ядро `lib/pii` (TDD) + загрузка плана с diff-анонимизацией + парсинг в темы с прогрессом.
  - Что работает: **`lib/pii`** (полностью локально, без сети): `patterns.ts` regex для телефон/email/СНИЛС/паспорт/ИНН/ДР/адрес (**без контрольных сумм**; паспорт/ИНН/ДР — только в контексте ключевого слова), `names.ts` детект ФИО (словарь `names.json` ~80 имён + патроним/фамилия-суффиксы; **`\b` не работает на кириллице → Unicode-lookaround `(?<![А-ЯЁа-яё])`**), `detect.ts` объединение + жадное снятие пересечений, `anonymize.ts` детерминированные плейсхолдеры `[Тип_N]` (одно значение → один плейсхолдер), `index.ts` барель `detectAndAnonymize`. **`lib/parse/index.ts`** — pdf-parse/mammoth/TextDecoder в памяти (без /tmp), guard'ы 5 МБ + magic-bytes (`%PDF-`/`PK`). **`lib/plan/parse-topics.ts`** — эвристический разбор (маркер ИЛИ дата; дата извлекается ДО маркера). Таблицы `work_plans`/`plan_topics` (миграция `0005`, обе с `user_id`). Server actions `app/app/plans/actions.ts`: `analyzePlanAction` (parse+PII-diff, без сохранения) + `savePlanAction` (**анонимизация пересчитывается на сервере — клиентским полям не доверяем**; consent-гейт на сохранение необезличенного с серверной проверкой `consent==='on'`). UI: `upload-form.tsx` (зона загрузки, diff original→placeholder, чекбокс согласия с точным текстом «…отправлены во внешний сервис GigaChat»), `/app/plans` (список + прогресс «закрыто N/M», бейдж обезличен/без), `/app/plans/[id]` (темы, ✓готов / «Сгенерировать» → `/app/new?topic=&planTopicId=`). Интеграция: `/app/new` префил из query (через `Suspense`+`useSearchParams`) + `generateScenarioAction` проставляет `source_plan_topic_id` (с проверкой владения темой). Навбар: ссылка «Планы»; дашборд: карточки прогресса по планам.
  - Гейты зелёные: `pnpm test` (130 passed / 3 skip — guarded live), `pnpm lint`, `tsc --noEmit`, `pnpm build` (все роуты в выводе). Изоляция по `user_id` подтверждена холистическим ревью на ВСЕХ запросах к `work_plans`/`plan_topics`/`scenarios`; `lib/pii` без сетевых импортов.
  - **НЕ выполнено (ручной шаг перед демо):** живой браузерный UAT — загрузить реальный PDF/DOCX плана с ПДн, проверить diff-модалку, путь согласия, разбор на темы и счётчик прогресса. Проверено только статикой/юнит-тестами (PII/parse/topics плотно покрыты TDD; server actions — интеграционная склейка без юнит-тестов по плану). Миграция `0005` применена к общей dev-БД (docker-сокет закрыт в sandbox, but Postgres уже поднят).
  - **Технический долг → Plan 8 / след. фазы:** мягкий PII-warning при сохранении сценария (§6 точка 2) НЕ реализован; повторный строгий PII-чек при opt-in shared — следующая фаза (Likes/Shared); pre-match по `shared_scenarios` — там же; календарь и лендинг — отдельные фазы; rate-limit загрузок 20/день (§9) НЕ реализован; `lib/parse` `ParseInput.mimeType` не используется (kind по расширению); словарь имён маленький (~80) — расширяемый.
- **Plan 7 «Community-loop» — ГОТОВ.** Ветка `feat/community-loop` (от `pii-plan-done`), тег `community-loop-done` (13 коммитов). План: `docs/superpowers/plans/2026-05-20-plan-7-community-loop.md`. Реализовано через subagent-driven (имплементер + spec/quality-ревью на каждую из 12 задач + финальное холистическое ревью, вердикт **Ship**). Согласованный scope: полный Community-loop + страница `/app/library` с семантическим поиском + подмешивание shared как `GOOD_EXAMPLES` в RAG-промпт генерации.
  - Что работает: таблицы `likes` (unique `(user_id, scenario_id)` → лайк не задваивается) + `shared_scenarios` (`anonymized_content` jsonb, `embedding` vector(1024), `like_count`, unique `source_scenario_id`, индекс по direction; миграция `0006`). **`lib/community`** (TDD): `serialize.ts` (обход всех строковых полей `ScenarioContent`), `pii-gate.ts` (**строгий повторный PII-чек**: анонимизация content → ре-детекция; если что-то осталось — `{clean:false}`, шаринг блокируется), `prematch.ts` (`embed(direction+grade+topic+format)` → поиск в shared с фильтрами `direction/grade±2/format` + порог `SIMILARITY_THRESHOLD` env деф. 0.78, deps-injection как `retrieve.ts`), `share-target.ts` (create/increment/noop для `like_count`), `copy.ts` (маппер shared→новый `scenarios` с `source_shared_id`). Server actions в `app/app/scenarios/[id]/actions.ts`: `likeScenarioAction` (upsert лайка; **PII-gate ДО любой записи в shared**; на остаточный PII → `{piiBlocked:true}` с перечислением типов; create вставляет shared с эмбеддингом, increment бампит `like_count` исходной записи для копий) + `useSharedAsIsAction` (**«использовать как есть» = КОПИЯ** под текущим `userId`, оригинал не трогается). UI: `LikeShareControls` в редакторе (чекбокс «Поделиться с сообществом» + лайк, surface ошибки PII), `SharedCard` (карточка с ❤ like_count + «Использовать как есть»), двухшаговый flow в `/app/new` (Подобрать похожие → карточки pre-match / «Сгенерировать новый», при отсутствии совпадений — сразу генерация), страница `/app/library` (семантический поиск по shared, `LIBRARY_SIMILARITY_THRESHOLD` деф. 0.5 мягче pre-match; пустой запрос → топ по `like_count`), ссылка «Библиотека» в навбаре, карточка «Библиотека сообщества: N» на дашборде. RAG-промпт: `prompt.ts` секция `[GOOD_EXAMPLES]` (только title + список названий этапов — экономия токенов), `generate.ts` подтягивает top-2 shared (best-effort, **prematch инъектируется через `GenerateDeps.prematch`** → юнит-тесты не ходят в сеть).
  - Гейты зелёные: `pnpm test` (146 passed / 3 skip — guarded live), `pnpm lint`, `tsc --noEmit`, `pnpm build` (все роуты, включая `/app/library`). Холистическое ревью: PII-инвариант (нет пути необезличенного content в `shared_scenarios`) и изоляция по `user_id` (единственная публичная таблица — `shared_scenarios`, у неё нет `user_id` намеренно; копии/лайки строго под сессией) подтверждены; SQL параметризован, vector-литералы только из числового вывода `embed()`.
  - **НЕ выполнено (ручные шаги перед демо):** живая калибровка `SIMILARITY_THRESHOLD` на ≥20 реальных запросах (`scripts/calibrate-threshold.ts` — НЕ написан, отдельный ручной артефакт); браузерный UAT (лайк → opt-in шаринг с реальными ПДн в content → проверка блокировки; pre-match карточки; «использовать как есть»; поиск в `/app/library`); реальный ingest корпуса для наполнения shared. Проверено только статикой/юнит-тестами (чистая логика — TDD; server actions/UI — без юнит-тестов, проверены tsc/lint/build + холистическое ревью). БД с реальным Postgres не прогонялась (нет в sandbox).
  - **Технический долг → Plan 8:** `useSharedAsIsAction` без транзакции (scenario + начальный version-row двумя стейтментами) и без rate-limit (спам копий, но без LLM-токенов); rate-limit на поиск `/api/search` 60 RPM (§9) НЕ реализован; `like_count` модель агрегации простая (инкремент только при первом opt-in лайке копии — лайки оригинала не суммируются между авторами); `anonymized_content` приводится `as`-кастом без рантайм-валидации формы (защищено try/catch в местах чтения).
- **Plan 8 «Streaming генерации (SSE) + Rate-limit» — ГОТОВ.** Ветка `feat/streaming-ratelimit` (от `community-loop-done`), тег `streaming-ratelimit-done`. План: `docs/superpowers/plans/2026-05-20-plan-8-streaming-ratelimit.md`. Реализовано через subagent-driven (имплементер + spec-review на ключевых + финальное холистическое ревью). Согласованный scope (бандл): двухэтапный SSE-стрим генерации + полная rate-limit инфраструктура. ВНЕ scope: календарь, лендинг, CSRF logout, AUTH_URL derive, мягкий PII-warning при сохранении, калибровка порога.
  - **Стриминг:** `lib/gigachat/sse.ts` (`parseSSEBuffer` — чистый парсер SSE-буфера, TDD); `lib/gigachat/client.ts` + `chatCompletionStream` (async-генератор, `stream:true`, reader+TextDecoder, существующий `chatCompletion` цел, TDD с ReadableStream-стабом); `lib/scenario/partial.ts` (`parsePartialJson` — дополнение оборванного JSON без внешней зависимости, TDD 8 кейсов); `lib/scenario/schema.ts` `skeletonSchema`/`ScenarioSkeleton` + `lib/scenario/prompt.ts` `buildSkeletonMessages`/`buildDetailsMessages` (`PROMPT_VERSION=v2-stream-2026-05-20`, TDD); `lib/scenario/stream.ts` `streamScenario(input, deps)` (async-генератор `StreamEvent`: phase/skeleton/stage/done/error; RAG+prematch best-effort как в `generate.ts`; skeleton-stream→валидация+repair→details-stream→валидация+repair→нормализация→`save`; **все deps инъектируются — юнит-тесты без сети/БД**, spec-review ✅); route `app/api/generate/stream/route.ts` (POST SSE, auth→401, zod→400, **rate-limit→429 ДО открытия стрима**, `save`-замыкание = insert scenarios+versions+generations+embedding с изоляцией `user_id`, `sourcePlanTopicId` резолвится фильтром по userId); клиент `components/generation/GenerationStream.tsx` (fetch reader + `parseSSEBuffer`, прогресс-бар фаз, пульсация каркаса, `done`→push на сценарий, 429/error→сообщение+retry); `/app/new` переключён на стрим (`generateScenarioAction` УДАЛЁН как осиротевший, prematch-флоу сохранён).
  - **Rate-limit:** таблица `rate_buckets` (generic `subject` text = userId ИЛИ ip — осознанное отклонение от `user_id fk` ради login-лимита по IP; PK `(key,subject,window_start)`; миграция `0007`); `lib/ratelimit/window.ts` (`windowStartFor`+`isWhitelisted`, TDD); `lib/ratelimit/index.ts` `checkRateLimit(check, deps)` (чистая логика над инъектируемым `RateStore`, whitelist-байпас по `DEMO_USER_EMAILS`, **lazy cleanup** DELETE>24ч на каждый вызов — без cron, TDD с in-memory store) + `lib/ratelimit/store.ts` `dbStore` (drizzle, `onConflictDoUpdate` инкремент; динамический import → юнит-тесты не тянут БД). Подключено на 5 точек §9: генерация `MAX_GENERATIONS_PER_DAY` (деф.10)/день + whitelist, login 5/15мин/IP, upload 20/день, export 100/день, search 60/мин.
  - Гейты зелёные на каждом коммите: `pnpm test` (173 passed / 3 skip — +27 новых), `pnpm lint`, `tsc --noEmit`, `pnpm build` (роут `/api/generate/stream` в выводе). Миграция `0007` применена к dev-БД.
  - **НЕ выполнено (ручной шаг перед демо):** живой браузерный UAT стрима против реального GigaChat (skeleton→details, прогресс-бар, fallback при обрыве) — проверено только статикой/юнит-тестами; стрим эмитит `skeleton` единым событием после сбора каркаса (не реалтайм по дельтам — упрощение по плану), `stage`-события синхронны после валидации деталей; usage-токены в стрим-режиме не парсятся (`meta.usage=null`).
  - **Технический долг → след. фазы:** реалтайм-прогресс по этапам внутри details-стрима; rate-limit на `regenerateActivityAction`/`useSharedAsIsAction`/prematch (LLM/спам — пока без лимита); login keyed по `x-forwarded-for` (за прокси корректно, при прямом подключении — `unknown`); остаток DoD §12 — календарь, лендинг, CSRF logout, AUTH_URL derive, мягкий PII-warning, калибровка `SIMILARITY_THRESHOLD`, проверка `russian` tsv в Docker.
- **Plan 9 «Demo-readiness» — ГОТОВ (финальная фаза).** Ветка `feat/demo-readiness` (от `streaming-ratelimit-done`), тег `demo-readiness-done` (15 коммитов). План: `docs/superpowers/plans/2026-05-20-plan-9-demo-readiness.md`. Worktree `.claude/worktrees/feat-demo-readiness`. Реализовано через subagent-driven (имплементер + spec-review на ключевых задачах: PII-save, изоляция календаря; + финальное холистическое ревью **READY TO MERGE** + `security-review` **SAFE TO DEMO**). Согласованный scope (полный demo-bundle): security/PII-polish + календарь поводов + лендинг + демо-prep.
  - **Security/PII-polish:** `lib/auth/origin.ts` (`isSameOrigin`/`assertSameOrigin`, TDD) — same-origin CSRF-guard на `/app/logout` (fail-closed, 403 при чужом/отсутствующем Origin); `lib/auth/base-url.ts` (`baseUrlFrom`/`baseUrlFromRequest`, TDD) — redirect-base из заголовков запроса (путь фиксированный `/` → не open-redirect); rate-limit на `regenerateActivityAction` (`MAX_REGEN_PER_DAY` деф.40), `useSharedAsIsAction` (`MAX_COPY_PER_DAY` деф.50), `prematchAction` (`MAX_PREMATCH_PER_DAY` деф.60) — subject=userId, whitelist через `DEMO_USER_EMAILS`; **мягкий PII-warning при сохранении** (§6 п.2) `lib/pii/scenario-scan.ts` (TDD) → `saveScenarioAction` возвращает `piiWarning` НО `ok:true` (неблокирующий), баннер в редакторе; строгий opt-in-share gate НЕ тронут.
  - **Календарь поводов:** статический `lib/calendar-events.ts` (25 дат, БЕЗ LLM, TDD-инварианты); таблица `calendar_events` (миграция `0008`, FK cascade users+scenarios, индекс `(user_id, event_date)`); `lib/calendar/events.ts` (`bindScenarioToDate`/`listUserEvents`/`unbindEvent`, **изоляция по `user_id` во всех read/delete**, DI-стаб в тестах без БД, TDD); `app/app/calendar/actions.ts` (bind проверяет владение сценарием ДО записи + берёт title из owned-строки, unbind scoped по userId); страница `/app/calendar` + `components/calendar/CalendarGrid.tsx` (месячная сетка в порядке учебного года Сен→Авг, секция «Ваши занятия на датах»); кнопка «На дату» в редакторе (`meta.id`, onClick+useTransition); 3-й источник темы «Календарь поводов» в `/app/new` (управляемый topic, переход с `?topic=&calendarDate=` префилит и выбирает вкладку); ссылка «Календарь» в навбаре.
  - **Лендинг:** `app/page.tsx` перезаписан + `components/landing/*` (LandingNavbar/Hero/Features/HowItWorks/Audience/Cta/Footer) — адаптация из `design_example/` на существующие токены (brand/neutral/accent/warm, Inter+Onest, shadow-card/hover/brand, lucide-иконки), весь текст русский, CTA → `/register`/`/login`. **НЕ импортирует `design_example` и не коммитит его** (untracked).
  - **Демо-prep:** `scripts/calibrate-threshold.ts` (24 запроса → распределение top-sim для `SIMILARITY_THRESHOLD`, ручной прогон); `scripts/seed-demo.ts` (идемпотентный демо-аккаунт, guard от прогона в prod через `ALLOW_PROD_SEED`); `docs/qa.md` (UAT чек-лист + ручные шаги).
  - Гейты зелёные: `pnpm test` (192 passed / 3 skip — +19 новых), `pnpm lint` (exit 0), `tsc --noEmit`, `pnpm build`. Миграция `0008` применена к dev-БД (smoke зелёный).
  - **Решение по scope:** отдельная вкладка «Из плана» в `/app/new` НЕ добавлена — поток покрыт переходом со страницы планов через `?planTopicId=` (зафиксировано как backlog в `docs/qa.md`).
  - **НЕ выполнено (ручные шаги перед демо, требуют живого окружения):** браузерный визуальный QA лендинга и календаря; живая калибровка `SIMILARITY_THRESHOLD` (`pnpm calibrate`, нужна наполненная `shared_scenarios`); проверка словаря `russian` в `to_tsvector` на Docker-Postgres (fallback `PG_TSV_LANG=simple`); E2E golden-path; `pnpm seed:demo` + добавление email в `DEMO_USER_EMAILS`. Чек-лист — `docs/qa.md`.
  - **Вне scope кода (демо-артефакты):** скринкаст 5–7 мин, презентация 8 блоков.
- **Plan 10 «Admin-панель статистики» — ГОТОВ.** Ветка `feat/admin-stats` (от `master`, т.е. после мержа demo-readiness), тег `admin-stats-done` (13 коммитов). Спека: `docs/superpowers/specs/2026-05-20-admin-stats-design.md`, план: `docs/superpowers/plans/2026-05-20-plan-10-admin-stats.md`. Worktree `.claude/worktrees/feat-admin-stats`. Реализовано через subagent-driven (имплементер + spec-review на ключевых: events-emit, admin-гейт; + холистическое ревью **READY TO MERGE**). Подход: read-only дашборд поверх существующих таблиц (история уже копится через `created_at`) + events-лог для эфемерных метрик.
  - **Роли:** `users.role text NOT NULL DEFAULT 'user'` (миграция `0009`), прокинут через `authorize`→JWT→session (`auth.ts`, тип `Session.user.role`); `lib/admin/guard.ts` `isAdmin(session)` (TDD); страница `/app/admin` редиректит не-админов на `/app` ДО любых запросов; ссылка «Админ» в навбаре только для admin; скрипт `pnpm set:admin <email>` (`scripts/set-admin.ts`).
  - **Статистика:** `lib/admin/stats.ts` — агрегаты по ВСЕМ юзерам (намеренно непроскоуплено, единственная точка чтения чужих данных, гейтится только страницей-админкой): `generationStats` (объём/успех/ошибки/задержка/30д), `contentStats` (топ-темы + распределения по направлению/классу/формату/длительности), `userStats` (всего/активные/новые 30д/топ по генерациям), `communityStats` (лайки/расшарено/топ по like_count/покрытие планов через `source_plan_topic_id`), `eventStats`. Чистые хелперы `lib/admin/format.ts` (`barPercent`/`successRate`, TDD). UI: `components/admin/{SectionCard,KpiCard,BarList,StatTable}` + страница на карточках/таблицах/CSS-bar'ах (без chart-либ).
  - **Events-лог:** таблица `events (id, user_id null, type, meta jsonb, created_at)` (миграция `0009`, индекс `(type, created_at)`); `lib/events/log.ts` `logEvent(type, opts, db?)` — **best-effort** (try/catch, не валит поток, инъекция db, TDD); эмит на успешный экспорт (`meta:{format}`), успешный логин (в `authorize`, динамический import), непустой поиск в библиотеке (`meta:{query}`).
  - Гейты зелёные: `pnpm test` (206 passed / 3 skip — +14 новых), `pnpm lint` (exit 0), `tsc --noEmit`, `pnpm build` (роут `/app/admin` = `ƒ` dynamic). Миграция `0009` применена к dev-БД (smoke зелёный). SQL-инъекций нет (`sql.raw` только с литералами имён колонок).
  - **Технический долг / решения:** `topUsers`/`topShared` — all-time (остальные KPI — 30д, осознанно); экспорты/логины/поиск копятся в `events` только С МОМЕНТА внедрения (ретроспективы до — нет); нет интерактивного date-фильтра, экспорта статистики, управления ролями из UI (только CLI).
  - **Ручной шаг:** назначить первого админа `pnpm set:admin <email>` на проде/деве.
  - **Побочный фикс:** тест `tests/lib/auth/base-url.test.ts` стал детерминированным через `vi.stubEnv` (раньше зависел от ambient `AUTH_URL`; `delete` заменён, т.к. Biome `noDelete`).

### Пост-milestone изменения (на master, вне нумерованных планов)

- **Аудитория «СПО» — ГОТОВО** (коммит `d696958`, спека `docs/superpowers/specs/2026-05-21-spo-audience-design.md`). На РоВ кроме классов 1–11 есть категория СПО. Решение: СПО — полноценная аудитория, хранится **sentinel-числом `grade=12`** в существующей `integer`-колонке (БЕЗ миграции). `lib/scenario/options.ts`: `SPO_GRADE=12`, `formatGrade` (→ «СПО»/«N класс») и `formatGradeForPrompt` (→ «обучающиеся СПО…»). zod `grade` max 11→12. Везде, где класс печатается (форма `/app/new`, редактор, дашборд, экспорт `document-model`, admin-статистика), — через `formatGrade`. Промпты через `formatGradeForPrompt`, `PROMPT_VERSION` → `v4-spo-2026-05-21`. **Pre-match:** для СПО точное `grade=12` (не диапазон ±2, чтобы не смешать с 10–11). **RAG retrieve:** для СПО эффективный `grade=11` (методичек с `grade_min/max=12` нет — тянем старшие). Гейты: tsc, 214 тестов, lint, build. Ручной шаг — живая генерация СПО против GigaChat в браузере.
- **Security/деплой-хардеринг** (коммиты `904c337`, `7e5146a`) — после инцидента с заражением прод-VPS (Mirai-бот через интернет-доступный `next start`; root НЕ получен; вредонос в `~/quarantine`):
  - **next `15.0.3 → 15.5.18`** (закрыт обход middleware-аутентификации), **drizzle-orm `0.36.4 → 0.45.2`** (закрыта SQL-инъекция).
  - **Postgres bind `127.0.0.1:5433`** (в `docker-compose.yml`); пароль вынесен в gitignored `.env` как `${POSTGRES_PASSWORD}` (в репо секрета нет).
  - `.gitignore` ужесточён: `.env`, `.env.*` (кроме `.env.example`), `*.tsbuildinfo`.
  - Секреты ротированы: `GIGACHAT_AUTH_KEY`, `AUTH_SECRET`, пароль БД.
- **Контекст генерации + библиотека сообщества (2026-05-21, на master):**
  - **Расширены справочники `lib/scenario/options.ts` под бриф:** `FORMATS` → 8 (добавлены `киноклуб`, `дебаты`, `проектная сессия`); `DURATIONS` → `20/30/40/60`; `DIRECTIONS` → 11 (супермножество: 8 канонических ФГОС + дословные лейблы брифа `Семейные ценности`, `Профориентация`, `Здоровый образ жизни`). Схема (`z.enum`) и форма `/app/new` строятся из массивов; промпт принимает формат как текст — новые форматы работают без правок промпта; `format` хранится как `text` (без DB-enum).
  - **Редактор:** добавлены add/delete этапов и активностей (`lib/scenario/edit-ops.ts` + кнопки в `editor.tsx`, TDD); защита инвариантов схемы (нельзя удалить последний этап/активность).
  - **Библиотека сообщества наполнена:** `scripts/seed-shared.ts` (+ `pnpm seed:shared`) — seed-владелец `library@planwise.local` (источники изолированы, не для логина), 14 сценариев-источников → обезличенные копии в `shared_scenarios` через прод-логику (`strictPiiCheck` → `embed`). Покрытие: все 8 форматов, новые направления, длительности 20/30/40/45/60. Идемпотентно. Все 14 с эмбеддингом (2560d).
  - **Калибровка порога:** `scripts/calibrate-threshold.ts` починен (был баг хойстинга статического `import db` выше `config()` → `DATABASE_URL is not set`; переведён на ленивый импорт). Прогон: 24 запроса, top-sim 0.69–0.89 (медиана 0.795). `SIMILARITY_THRESHOLD` выставлен **`0.72`** (в `.env.local` и `.env.production` на проде) — дефолт 0.78 отсекал ~25% релевантных. Значение — стартовое, уточнять живым UAT.
- **Живой сквозной UAT golden-path (2026-05-21, на master):** прогнан против прода `176.108.252.98` через Claude-in-Chrome под аккаунтом `UAT` (бэклог-пункт #3 закрыт). Подтверждено вживую против GigaChat: генерация+двухэтапный SSE-стрим (~25 c, прогресс-бар фаз), редактор (правка, ↑/↓ с блокировкой на границах, 🎲-регенерация активности, save+reload персист), неблокирующий PII-warning при сохранении, opt-in shared со строгим PII-gate (контент обезличивается ДО публикации — утечки сырого телефона нет), экспорт PDF (`%PDF`, 50 КБ) и DOCX (`PK`, 10.6 КБ) с подтверждённой кириллицей, календарь + «На дату» + «убрать», 3-й источник темы из повода, лендинг. Чек-лист и отметки — `docs/qa.md §2–3`; трекер — `docs/backlog.md` (#3 → «Сделано»). **Найдено и записано в backlog:** #16 — адрес-детектор PII даёт ложное срабатывание (мангл текста, «формирование…взаимопомощи» → «форм[Адрес_1]имопомощи»; переredact в безопасную сторону, утечки нет); #17 — mobile-адаптив лендинга (закрыт, см. ниже). Тестовые прод-данные после прогона убраны.
- **Замер latency регенерации + раздельные метрики (2026-05-21, на master):** добавлена колонка `generations.kind text NOT NULL DEFAULT 'full'` (`'full'|'regen'`, **миграция `0011`**). `regenerateActivityAction` теперь засекает `Date.now()` и пишет реальный `latencyMs` + `kind:'regen'` (раньше `null`); полные генерации идут с дефолтом `kind:'full'`. `lib/admin/stats.ts`: `avgLatencyMs` заменён на `avgLatencyFullMs`/`avgLatencyRegenMs` (`avg(latency_ms) FILTER (WHERE kind=…)`). `/app/admin`: две KPI-карточки — «Среднее время генерации» (full) и «Среднее время 🎲-регенерации» (в секундах), сетка KPI → `md:grid-cols-3 lg:grid-cols-5`. Гейты: tsc, biome, build, 230 тестов. **Задеплоено на прод 2026-05-21 (`pnpm db:migrate` выполнен, рестарт), работает** — KPI наполняются. **На будущее:** этот деплой требовал миграции (одного `git pull && build && restart` мало — иначе INSERT в `generations` упал бы на отсутствующей колонке `kind`).
- **UI-багфиксы пачкой (2026-05-21, на master):** #15 — прилипшая кнопка «Создать сценарий» на `/app`: карточка-ссылка библиотеки была `<Link>` (`display:inline`), на который `space-y-6`'s `margin-top` не действует → наезд; фикс `className="block"` на `<Link href="/app/library">` (`app/app/page.tsx`). #16 — адрес-детектор PII (`lib/pii/patterns.ts`) ловил `ул`/`пер` внутри слов («рег**ул**ярное», «о**пер**ативный», «**ул**ыбка»); фикс: lookbehind-граница слова `(?<![А-Яа-яёЁA-Za-z])` + точка обязательна у сокращений `ул\.`/`пер\.` (TDD — новый тест на ложняки в `tests/lib/pii/patterns.test.ts`). #18 — hover у нативной file-кнопки на `/app/plans` (`upload-form.tsx`): `hover:file:bg-brand-100` + `cursor-pointer`. Гейты: tsc, biome, build, 231 тест.
- **Бэклог-фиксы (2026-05-23, на master):**
  - **#20 — переход из плана к готовому сценарию темы.** На `/app/plans/[id]` бейдж «✓ готов» был неинтерактивным `<span>`. Обёрнут в `<Link href={href}>` (href к `/app/scenarios/{id}` уже вычислялся выше, но в ветке «готов» не использовался) + `hover:bg-accent-100`. Гейты: tsc, biome зелёные.
  - **#10 — словарь `russian` для tsv ПРОВЕРЕН.** На живом Docker-Postgres (`kc-postgres`, pgvector pg16) конфиг `russian` присутствует и стеммит корректно (`Дети обсуждают семейные ценности` → `дет обсужда семейн ценност`, стоп-слова отброшены). BM25-часть гибридного поиска (`0.3·ts_rank`) работает на полном русском стемминге; fallback `PG_TSV_LANG=simple` (без стемминга) НЕ нужен. `PG_TSV_LANG=russian` в `.env.local`.
  - **#19 — смещённый тултип валидации файла на `/app/plans`.** Убран нативный `required` у `<input type=file>` (`app/app/plans/upload-form.tsx`) — он давал браузерный pop-up, привязанный к узкому file-контролу в `flex`-строке (всплывал не у инпута). Серверная валидация в `analyzePlanAction` (`actions.ts:39` → «Выберите файл плана.») уже существовала и рендерится инлайн под инпутом через `analyze.error`. Гейты: tsc, biome зелёные.
  - **#6 — вкладка «Из плана» в `/app/new`.** Добавлен третий источник темы `'plan'` в переключатель `manual|calendar|plan` (`app/app/new/page.tsx`): вкладка появляется только при заходе с `?planTopicId=` (со страницы плана), активна по умолчанию, показывает заметку «Тема взята из плана… сценарий закроет эту тему в плане». Поток привязки (скрытый `planTopicId`, авто-проставление `source_plan_topic_id`) был и раньше — изменение чисто визуальное, для очевидности жюри. Гейты: tsc, biome зелёные.
- **Mobile-навбар лендинга (2026-05-21, на master):** `components/landing/LandingNavbar.tsx` переведён в client-компонент (`'use client'` + `useState`). До этого на `<md` якорные ссылки (`hidden md:flex`) и «Войти» (`hidden sm:inline-flex`) были скрыты без альтернативы — на телефоне в шапке оставались только логотип и «Начать». Добавлен бургер-тоггл (`md:hidden`, иконки `Menu`/`X` из lucide) с выпадающим меню: якорные ссылки + «Войти» (последняя `sm:hidden`, чтобы не дублировать видимую на sm-md диапазоне), закрывается по клику на пункт. Остальные секции лендинга уже были адаптивны (сетки `grid-cols-1`→`sm:`/`lg:`). Гейты: tsc, biome, build зелёные. **Задеплоено на прод 2026-05-21.** Остаточный нюанс: визуальный reflow на узком viewport на глаз не подтверждён (в подключённом десктоп-Chrome не форсируется ширина <экрана) — глянуть в DevTools device-mode / на телефоне (backlog #17).
- **РоВ-уровень генерации — per-block пайплайн (2026-05-23, СМЕРЖЕНО в master, backlog #21):** реализация через subagent-driven (имплементер + spec/quality-ревью по каждой задаче + финальное холистическое ревью **READY TO MERGE**). Спека `docs/superpowers/specs/2026-05-23-rov-quality-blocks-design.md`, план `docs/superpowers/plans/2026-05-23-rov-quality-blocks.md`. Цель — дотянуть объём/качество до методичек РоВ (~15-18 КБ; реальный РоВ-`1s` = ~18-20 КБ / 9-11 плотных блоков по ~800-1000 ток.).
  - **Корень проблемы:** GigaChat «насыщает» ~800 ток./вызов; прежний per-stage пайплайн (1 вызов на этап) упирался в ~5 КБ. Решение — **1 вызов на БЛОК** (активность): объём масштабируется числом блоков.
  - **Что сделано:** каркас несёт КОНТЕНТ-ПЛАН `blocks: [{type, focus}]` на этап (`skeletonStageSchema`, optional) — распределяет смыслы по блокам без повторов; `lib/scenario/context.ts` `buildRunningContext` — катящаяся сводка готовых блоков (~200 симв./блок) прокидывается в каждый следующий per-block вызов (связность); `lib/scenario/quality.ts` `checkBlock` (пороги: длина ≥`MIN_BLOCK_CHARS` деф.600, ≥2 реплик «Учитель:» для engage/main, ≥3 вопроса для discussion) + `checkScenario` (warnings: объём, дубли этапов, непокрытые coreMeanings) — детерминированный гейт БЕЗ LLM-судьи, тонкие блоки перегенерируются до `MAX_BLOCK_RETRIES` (деф.2) с заострённым корректором; `buildBlockMessages` (per-block промпт, `PROMPT_VERSION=v6-rov-blocks-2026-05-23`); `stream.ts` переписан на per-block цикл (плоская очередь блоков, гейт-ретраи, событие `{type:'block',index,total}`, `meta.thinBlocks`/`qualityWarnings`); удалены осиротевшие `buildStageDetailsMessages`/`buildDetailsMessages`/`stageActivitiesSchema`; UI `GenerationStream.tsx` — прогресс «Блок i из N» + бар. `buildMessages`/`generate.ts` оставлены (их использует `gen-seed.ts`); `seed-shared.ts` наследует пайплайн без правок.
  - **Без миграций** (контент в jsonb; `blocks` каркаса — промежуточный план, в БД не сохраняется отдельным полем). Гейты зелёные: `pnpm test` (272 passed / 3 skip), `tsc --noEmit`, `pnpm lint`, `pnpm build` (роут `/api/generate/stream`).
  - **Живой UAT ВЫПОЛНЕН (2026-05-23)** против реального GigaChat (тема «Дружба и взаимовыручка», 6 класс, беседа, 30 мин): **14.6 КБ** (против прежних ~5 КБ — рост ~3×), 44.7 c, 5 блоков (основная часть — 3 блока / 3876 симв.), 2 ценности + 3 смысла, **0 тонких блоков** (все прошли гейт с первого раза), текст — настоящий РоВ-стиль (многоходовые «Учитель: …» с конкретными жизненными кейсами + «Ответы обучающихся.»). ~80% верхней планки РоВ — для добивки до 18-20 КБ крутить env-пороги/нудж промпта на больше блоков.
  - **Критический фикс из живого UAT (коммит `bd8d91f`):** GigaChat возвращает `"adaptations": {}` (пустой объект) → каркас не проходил `skeletonSchema` и **генерация падала целиком**. На моках не всплывало. Починено: каркасные `adaptations` → `.partial().optional()`, недостающие `simpler`/`harder` доводятся дефолтами по-полю при сборке; строгая проверка остаётся на финальном `scenarioContentSchema`. **Урок:** статика (моки) не заменяет живой прогон — этот баг ловится только реальным ответом модели.
  - **Деплой:** миграций нет → `cd /home/nikit/planwise && git pull && docker compose up -d --build`. После деплоя опц. пере-сид библиотеки новым пайплайном: `pnpm exec tsx scripts/seed-shared.ts --force` против прод-БД. Тюнинг env-порогов `MIN_BLOCK_CHARS`/`MAX_BLOCK_RETRIES`/`MIN_SCENARIO_CHARS` — без передеплоя кода.
  - **Пере-сид библиотеки ВЫПОЛНЕН (2026-05-23, на dev-БД):** `seed-shared.ts --force` перегенерил все 14 сценариев новым per-block пайплайном (контент 5.7–10.9 КБ символов ≈ 10-20 КБ байт, РоВ-уровень), все с эмбеддингами 2560d. **На проде пере-сид НЕ прогонялся** — сделать после деплоя.
  - **Критический фикс из пере-сида (коммит `50c2b6e`, на master):** для формата «дебаты» GigaChat ставит `"type":"debate"` в `blocks` каркаса → строгий enum `skeletonBlockSchema` отбраковывал весь каркас («Невалидный каркас»). Фикс: `parseSkeleton` (в `stream.ts`) коэрсит `blocks[].type` через `coerceActivityType` (как `parseBlock` для активностей); `debate`/`debates`/`дебаты → discussion` добавлены в `coerce.ts` MAP. **Урок (повтор):** баг всплыл только на живом прогоне (формат «дебаты»), не на моках.
  - **Презентация (`presentation/build.js`, worktree-ветка) актуализирована:** добавлен слайд «Как рождается сценарий: генерация по блокам» (per-block нарратив + результаты живого замера 14.6 КБ), strip пайплайна на слайде 5 → «Каркас+план → Блоки ×N → Гейт качества», эмбеддинги `1024d → 2560d`. Артефакт `~/Planwise-Klassniy-Chas.pptx` пересобран.
- **Регенерация активности на per-block (2026-05-23, ветка `feat/regenerate-per-block`, backlog #22 + #31):** реализация через subagent-driven (имплементер + spec-ревью по каждой задаче). Спека `docs/superpowers/specs/2026-05-23-regenerate-per-block-design.md`, план `docs/superpowers/plans/2026-05-23-regenerate-per-block.md`.
  - **Было:** 🎲-регенерация одной активности шла через старый `buildActivityMessages` — без роли этапа, контекста соседей и гейта качества → блок мельче остального РоВ-сценария, и введение/рефлексия «уплывали» в игру (#31).
  - **Стало:** выделен `lib/scenario/block-gen.ts` (`parseBlock` + `generateBlockWithGate`) — общий per-block шаг с гейтом, переиспользуется генерацией (`stream.ts` рефакторнут на него, поведение генерации не изменилось) И регенерацией. `regenerate.ts` переписан на `buildBlockMessages` (роль этапа + `coreMeanings` + катящийся контекст соседних активностей через `buildRunningContext`) + `generateBlockWithGate`. **Тип выбирает учитель** (`<select>` у 🎲 в `editor.tsx`, дефолт = текущий тип) и итоговый `type` **форсится** по выбору (`coerceActivityType(targetType)`) — гарантия, что введение/рефлексия не станут игрой. `regenerateActivityAction` принимает 4-й параметр `type`, реконструирует skeleton из сохранённого `content` и собирает контекст соседей; изоляция `user_id` и rate-limit `MAX_REGEN_PER_DAY` — без изменений.
  - **Без миграций.** Гейты: `pnpm test` (275 passed / 3 skip), `tsc --noEmit`, `pnpm lint`, `pnpm build`. Удалены осиротевшие `buildActivityMessages`/`ACTIVITY_SCHEMA_HINT`/`extractJson`/`tryParse`.
  - **Ручной UAT (перед мержем/демо):** 🎲 на введении с типом «Беседа» → вводная беседа РоВ-глубины (не игра); 🎲 в основной части со сменой типа на «Игра» → игра. Латентность регенерации теперь до ~3 вызовов (гейт-ретраи).
- **Фактологичность (#23) + ролевой пол хронометража (#24) (2026-05-23, ветка `feat/facts-chronometry`):** реализация через subagent-driven (имплементер + spec-ревью). Спека/план: `docs/superpowers/{specs,plans}/2026-05-23-facts-and-chronometry*`.
  - **#23 (галлюцинации) — без LLM-судьи** (прототип показал ненадёжность): (а) правило в `buildBlockMessages` — НЕ выдумывать конкретику (даты/имена/цитаты/статистику/названия) без опоры на `[RELEVANT_METHODOLOGY]`, иначе подавать гипотетически («представим…»); `PROMPT_VERSION → v7-facts-2026-05-23`. (б) постоянный неблокирующий баннер «⚠ Сценарий создан ИИ. Проверьте факты…» в редакторе (`editor.tsx`). (в) дисклеймер-абзац об ИИ в конце экспорта (`lib/export/document-model.ts` → PDF и DOCX). Снижает риск + честно предупреждает; не «решение» проблемы фактов.
  - **#24 (хронометраж):** `normalizeChronometry` переписан на «пропорция → пол → добор до суммы»: каждый этап ≥`MIN_STAGE_MINUTES` (деф.3, но не выше ⌊target/n⌋ — влезает в короткие занятия), сумма точно = длительности, валидные входы не меняются. Починен баг раннего выхода `current===target` (рефлексия 1 мин теперь поднимается до пола). Затрагивает генерацию И регенерацию (обе зовут `normalizeChronometry`), контракт не изменён.
  - **Без миграций.** Гейты: `pnpm test` (279 passed / 3 skip), `tsc --noEmit`, `pnpm lint`, `pnpm build`. Ручной UAT: реалистичный тайминг (рефлексия ≥3) + баннер + дисклеймер в экспорте.
- **Мягкий фильтр направления в RAG (#25, 2026-05-24, ветка `feat/rag-soft-direction`):** реализация через subagent-driven. Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-rag-soft-direction*`. Корень: жёсткий фильтр `chunk_meta->>'direction' = X` в `lib/rag/retrieve.ts` ИСКЛЮЧАЛ весь РоВ-корпус (510 чанков, `direction=null`) — fallback на direction=null срабатывал только при `rows < topK`, а у ~половины направлений ≥3 seed-чанка (Патриотическое=4, Трудовое=4…) → fallback не запускался, генерация опиралась на тонкие seed вместо методичек РоВ. Фикс: мягкий фильтр `(= X OR IS NULL)` (РоВ-корпус всегда в кандидатах) + удалён избыточный fallback. Направление остаётся в эмбеддинге запроса (`"{direction} {topic}"`) + cosine/BM25. **Живой UAT (прод-БД):** `retrieveChunks({direction:'Патриотическое',grade:4,topic:'День народного единства'})` → 3 чанка «Разговоры о важном» (раньше исключались). Без миграций. Гейты: 279 passed, tsc, lint, build.
- **Усиление детерминированного гейта качества (#26, 2026-05-24, ветка `feat/quality-gate-deterministic`):** реализация через subagent-driven. Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-quality-gate-deterministic*`. Закрыты дыры `checkBlock` БЕЗ LLM-судьи (прототип показал ненадёжность): (а) каждая реплика «Учитель:» (split по маркеру, преамбула отброшена) должна иметь содержимое ≥`MIN_TEACHER_TURN_CHARS` (деф.40) — пустая/обрывочная не проходит; (б) каждый вопрос discussion должен содержать «?» и быть ≥`MIN_QUESTION_CHARS` (деф.15) — отсекает «?»/«как?». Сигнатура `checkBlock` не изменена → применяется и в генерации, и в регенерации через `generateBlockWithGate`. Пороги низкие/env-тюнятся (нормальный РоВ-вывод не отбраковывают). Тест-фикстуры с игрушечными вопросами обновлены на реалистичные (иначе dense-блоки заворачивались). Без миграций. Гейты: 281 passed, tsc, lint, build. **Этим закрыта серия улучшений качества генерации (#21→#22/#31→#23/#24→#25→#26).**
- **Загрузка своего материала как основы сценария (#29, 2026-05-24, ветка `feat/user-material`):** реализация через subagent-driven (имплементер + spec/quality-ревью по каждой задаче). Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-user-material-source*`. **Разовая** привязка к одной генерации (НЕ персистентная библиотека, НЕ ingest в RAG под user_id — отвергнутые альтернативы), материал — **первичная опора** (выше методичек).
  - **Поток:** на `/app/new` опц. сворачиваемая секция «Свой материал» = ОТДЕЛЬНАЯ sibling-`<form action={materialAction}>` (не вложена в основную форму — невалидный HTML; `useActionState`). Файл (PDF/DOCX/TXT ≤5 МБ) → `analyzeMaterialAction` (`app/app/new/material-actions.ts`, зеркалит `analyzePlanAction`: auth → rate-limit `key:'material'` `MAX_MATERIAL_PER_DAY` деф.20 → `parseFile` → `detectAndAnonymize` → diff, БЕЗ записи в БД). UI показывает diff найденных ПДн + чекбокс согласия (точный текст про GigaChat), по умолчанию обезличивание. Кнопка генерации добавляет в payload `material:{text:original, consent}` (исходный текст, НЕ клиентский обезличенный — сервер пересчитывает).
  - **`lib/material/`** (TDD): `chunk.ts` (`chunkMaterial` — разбивка по абзацам, упаковка ~800 ток. по chars/3), `retrieve.ts` (`selectRelevantMaterial` — параграф-чанкинг → cap `MATERIAL_MAX_CHUNKS` деф.40 → `embed([query,...chunks])` один вызов → **чистый cosine** → top-K `MATERIAL_TOP_K` деф.5 в пределах `MATERIAL_MAX_CHARS` деф.6000; сбой embed → fallback на cap по символам; `embed` инъектируется → тесты без сети; эмбеддинги эфемерны, в БД НЕ пишутся), `prepare.ts` (`prepareMaterial(raw, consent)` — consent → сырой, иначе `detectAndAnonymize`).
  - **Интеграция:** `/api/generate/stream` route после auth+rate-limit best-effort готовит материал (`prepareMaterial` → `selectRelevantMaterial`, динамический import, ошибка не валит генерацию) → `input.userMaterial`. Поле `userMaterial?:string` (≤20k) добавлено в `generationInputSchema` (хранится в `inputContext` для трассируемости, как `work_plans.raw_text`). `streamScenario` пробрасывает в `buildSkeletonMessages`/`buildBlockMessages` → секция `[TEACHER_MATERIAL]` ВЫШЕ `[RELEVANT_METHODOLOGY]` + правило фактов расширено («нет в [TEACHER_MATERIAL] ИЛИ методичках»). `PROMPT_VERSION=v8-material-2026-05-24`.
  - **Безопасность:** сервер НЕ доверяет клиентскому обезличенному тексту — заново гоняет `detectAndAnonymize` (consent строго `=== true`); rate-limit с whitelist `DEMO_USER_EMAILS`; материал только в `inputContext` собственного сценария юзера. **Без миграций.**
  - Гейты: `pnpm test` (294 passed / 3 skip — +13 новых: chunk 4, retrieve 4, prepare 2, prompt 3), `pnpm lint` (0 ошибок), `tsc --noEmit`, `pnpm build` (роуты `/app/new` 6.66 КБ + `/api/generate/stream`).
  - **НЕ выполнено (ручной шаг перед демо, живой GigaChat):** браузерный UAT — загрузить PDF/DOCX/TXT с ПДн → проверить diff и счётчик → генерация БЕЗ согласия (нет сырых ПДн, контент опирается на материал) и С согласием → большой файл (cap чанков, латентность) → регресс генерации без материала. Проверено статикой/юнит-тестами.
  - **Технический долг:** при выборе файла без клика «Проанализировать» материал просто не используется (безопасно, но не форсится анализ); регенерация активности (`regenerateActivityAction`) материал не видит (эфемерен); токены оцениваются эвристикой chars/3.
- **Read-only шаринг сценария по ссылке (#30а, 2026-05-24, ветка `feat/share-link`):** реализация через subagent-driven (имплементер + spec/quality-ревью + финальное холистическое ревью **READY TO MERGE**). Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-readonly-share-link*`. Согласованный scope — ТОЛЬКО read-only ссылка; (б) история версий и (в) og-image остаются в #30.
  - **Модель:** колонка `scenarios.share_token text` (nullable **unique**, **миграция `0012_solid_celestials`**). Включение → неугадываемый токен (`lib/share/token.ts`, `randomBytes(24)` base64url ≈192 бит); отзыв → `share_token = NULL` (старая ссылка → 404). Без отдельной таблицы.
  - **Actions** (`app/app/scenarios/[id]/actions.ts`): `enableShareLinkAction` (изоляция `id AND user_id` на load И update; идемпотентно; возвращает `{token, piiWarning}` — мягкий `scanScenarioPii`, НЕ блокирует), `disableShareLinkAction`, `copyScenarioByTokenAction` (копия чужого расшаренного под текущим `userId`, копия БЕЗ токена).
  - **Публичная страница** `app/s/[token]/page.tsx` (вне `/app` → middleware не гейтит, без auth): поиск строго по `share_token`, рендер через `buildScenarioDocument` + `components/share/ScenarioReadOnly.tsx`. **Наружу отдаётся ТОЛЬКО `content` + 5 мета-полей** (нет `inputContext`/`userMaterial`/`user_id`/email — подтверждено ревью). Логотип + CTA `/register`; кнопки экспорта; «Скопировать себе» только при сессии; `generateMetadata` → title.
  - **Публичный экспорт** `app/api/share/[token]/export/route.ts` (без auth): rate-limit `key:'share-export'` по токену (`MAX_SHARE_EXPORT_PER_DAY` деф.200), поиск по токену, тот же `renderScenarioExport`.
  - **UI**: `ShareLinkControls` в редакторе (включить/копировать/отозвать + PII-баннер), `CopyToMyAccount` на публичной странице. Страница `[id]/page.tsx` отдаёт `initialShareToken`.
  - Гейты: `pnpm test` (296 passed / 3 skip — +2 token), `pnpm lint` (0 ошибок), `tsc`, `pnpm build` (роуты `/s/[token]`, `/api/share/[token]/export`). **Деплой требует `db:migrate`** (миграция 0012 — сервис `migrate` применит автоматически при `docker compose up -d --build`).
  - **НЕ выполнено (ручной UAT перед демо):** включить ссылку → открыть в приватном окне (read-only, PDF/DOCX, CTA) → отозвать → 404 → «скопировать себе» залогиненным. **Долг:** `copyScenarioByTokenAction` без rate-limit (как `useSharedAsIsAction`, без LLM-стоимости).
- **Прод-окружение (АКТУАЛЬНО на 2026-05-24 — домен + HTTPS):** VPS `176.108.252.98` (2 vCPU / 4 ГБ + swap 4G), домен **`https://plan-wise.ru`** (регистратор/DNS — Jino; A-записи `@`+`www` → IP). **App и БД запускаются через `docker compose` из `/home/nikit/planwise`** (сервисы `db` + одноразовый `migrate` + `app`). `app` слушает `127.0.0.1:3000`, наружу — **nginx с HTTPS на `:443`** (`proxy_pass localhost:3000`; редирект 80→443). TLS — **Let's Encrypt** (`certbot --nginx`, серт на `plan-wise.ru`+`www.plan-wise.ru`, автопродление через systemd-timer `certbot.timer`). Образ собирается из исходников **в контейнере** (`docker compose up --build`), серты «Минцифры» вшиты (`NODE_OPTIONS=--use-system-ca`) → TLS к GigaChat без обхода. Секреты приложения — в gitignored **`.env`** (туда сведены `.env.production` + `POSTGRES_PASSWORD`; **`AUTH_URL=https://plan-wise.ru`** — при смене домена обязательно обновить, иначе Auth.js ломает логин/редиректы). `restart: unless-stopped` на `app`+`db` → переживают ребут. Фаервол **ufw** (`22`, `80`, `443`).
  - **Грабли certbot (2026-05-24):** запускать выпуск ОДНОЙ строкой и неинтерактивно — `sudo certbot --nginx --non-interactive --agree-tos -m <email> --no-eff-email --redirect -d plan-wise.ru -d www.plan-wise.ru`. Интерактивный EFF-промпт + вставка многострочного блока с кириллицей в stdin роняли certbot `UnicodeDecodeError`. В окружении VPS задан `HTTPS_PROXY=http://127.0.0.1:10809` (локальный прокси; исходящие к ACME/наружу идут через него — рабочее).
  - **Деплой новой версии:** `cd /home/nikit/planwise && git pull && docker compose up -d --build` (миграции прогоняются сервисом `migrate` автоматически, идемпотентно).
  - **Откат на systemd:** unit `deploy/planwise.service` остался установлен но `disable`-нут — `docker compose down && sudo systemctl enable --now planwise`. (Раньше прод был на `next start`+systemd; миграция на Docker 2026-05-23, данные сохранены — volume `planwise_kc-pgdata` переиспользован, проверено `docs=88/chunks=529/users=4/shared=14`.)
  - **Бэкап БД перед операциями:** `docker exec kc-postgres pg_dump -U kc kc | gzip > ~/kc-backup-$(date +%F-%H%M).sql.gz`.
  - GitHub-remote: `git@github.com:Caspernik-dev/s21_planwise.git` (ветка `master`).
- **Публичный релизный репозиторий + чистая сборка (2026-05-28):** отдельный публичный репозиторий `git@github.com:Caspernik-dev/planwise.git` (ветка `main`) — чистый снимок ТОЛЬКО файлов для запуска/работы (284 файла, орфанный коммит без истории). **Исключены:** `CLAUDE.md`, `docs/`, `presentation/`, `design_logo.md`, бриф, `design_example/`, артефакты, секреты. Включены: исходники (`app/components/lib/db/content/assets/public/scripts/tests`), все конфиги, `Dockerfile`/`docker-compose.yml`/`deploy/`, `.env.example`, `README.md` (почищен от ссылок на удалённые `docs/`/`CLAUDE.md`; добавлен верхний блок «Самый простой запуск»: `.env` → `docker compose up -d --build` → браузер).
  - **Рабочая копия релизной сборки (git-репо) — `/tmp/planwise-release`** (это origin→`Caspernik-dev/planwise`; здесь правим README/доки и коммитим). **Тестовый прогон-стек — `/home/nikit/planwise_release`** (без `.git`, с рабочим `.env` для локального запуска; имя docker-проекта = `planwise_release` → отдельный пустой том, конфликтует портами/именем `kc-postgres` с рабочим стеком — запускать только остановив рабочий). `/tmp/*` эфемерен — при правке доков пересоздать из `master` через `git archive` если папка пропала.
  - **Пуш в этот репо мне блокирует auto-mode классификатор** (внешний remote ≠ доверенный `s21_planwise`) — коммитить могу, пушит пользователь вручную: `cd /tmp/planwise-release && git push origin main`.
- **Docker self-host + переезд прода на Docker (2026-05-23, СМЕРЖЕНО в master, коммиты `9162bea`/`86d157b`/`23e452c`/`c8bfcb1`):** реализован «Поток 1» — runtime Docker-образ + compose, чтобы отдавать развёртывание сторонним людям (закрывает backlog #9). Прод переехал на Docker (см. блок «Прод-окружение» выше). Изначально велось в worktree `.claude/worktrees/feat-docker-offline`.
  - **Что вошло:** `Dockerfile` (runtime-образ из standalone-бандла + отдельный `migrator`-стейдж на tsx для drizzle-миграций); `docker-compose.yml` (сервисы `app` + `migrate` с авто-миграцией перед стартом, `DATABASE_URL → db:5432`, Postgres-bind сохранён); `deploy/certs/` — серты «Минцифры» (root+sub) вшиты в образ через `NODE_OPTIONS=--use-system-ca` → **TLS к GigaChat без обхода проверки** (не нужен `GIGACHAT_INSECURE_TLS`); `.dockerignore`; `next.config` `outputFileTracingRoot=process.cwd()` (фикс вложенного standalone при сборке из worktree).
  - **Смоук образа зелёный:** `/`=200 (лендинг), `/login`=200, `/app`→307 login, TLS к GigaChat OK.
  - **✅ Полноценный «clone + docker compose up» (фикс 2026-05-23, коммит `c8bfcb1`).** Раскопан истинный корень бага «`next build` в контейнере ломает роуты» (через `systematic-debugging`, не угадывание): **`WORKDIR /app` коллизил с маршрутом `/app`** (исходник `app/app/page.tsx`) — корневая директория с именем «app» путала маппинг роут→модуль в Next App Router, и route `/` отдавал дашборд. Это НЕ «баг детерминизма Next», а имя рабочей директории. **Фикс: `WORKDIR /build`.** Второй корень (падение сборки): `db/index.ts` бросает `DATABASE_URL is not set` на импорте, а Next импортирует модули роутов на «collect page data» → решено плейсхолдерами `DATABASE_URL`/`AUTH_SECRET` на build-стадии (postgres-js ленив, коннекта на сборке нет). Dockerfile переписан на честную multi-stage сборку (deps→builder→migrator→runner), `next build` ВНУТРИ контейнера; `.dockerignore` исключает `.next` целиком. Проверено `docker build` из исходников: `/`=лендинг (статика 1.68kB), `/login`=200, `/app`→307, TLS к GigaChat=401 (handshake с вшитым сертом, без обхода). **Теперь репа собирается у любого одной командой `docker compose up`.**
  - **Прод по-прежнему на systemd** (`next start`), Docker-образ его не заменял — это отдельный артефакт для раздачи. Не смержен в master.
  - **Поток 2 «Переключаемая LLM через env» — ГОТОВ** (коммиты `86d157b`, `23e452c` на той же ветке). Цель: получатель легко вписывает ключ GigaChat ИЛИ переключается на локальную/OpenAI-совместимую модель (Ollama/LM Studio/vLLM) без правок кода. Находка: **GigaChat API уже OpenAI-совместим по форме** (chat `{model,messages,temperature,max_tokens,stream}`, embeddings `{model,input}`) — отличается только авторизация. Поэтому абстракция вышла маленькой: `lib/gigachat/config.ts` — `LLM_PROVIDER` (`gigachat` деф.|`openai`) + generic-алиасы `LLM_API_BASE`/`LLM_MODEL`/`LLM_EMBED_MODEL`/`LLM_API_KEY` с fallback на `GIGACHAT_*` (прод не ломается); `lib/gigachat/token.ts` — при `provider=openai` отдаёт статический `LLM_API_KEY` без OAuth (пустой допустим для keyless Ollama); `client.ts`/`embeddings.ts` НЕ менялись. TDD: новый `config.test.ts` + ветка в `token.test.ts` (257 passed, tsc, biome). `.env.example` — два режима + `POSTGRES_PASSWORD`; README — раздел «Развёртывание через Docker (self-host)»; `docker-compose` `app.env_file → .env` (один файл секретов). **⚠️ Caveat (задокументирован):** чат-модель меняется свободно, но смена **embed-модели** ломает RAG — колонки `vector(2560)` под GigaChat; полный офлайн требует миграцию размерности + пере-ingest. Эмбеддинги по умолчанию остаются на GigaChat.
  - **Поток 3 (офлайн с вшитым Ollama + пере-ingest) — ОТМЕНЁН по решению пользователя.** Сами Ollama в образ не пакуем; получатель при желании поднимает локальную LLM у себя и прописывает креды (Поток 2 это уже позволяет).
  - **Осталось (опционально):** живой `docker compose up` end-to-end с реальным Postgres + `.env` (проверена только `docker build` стадия + рантайм app с dummy-БД); опционально — запушить образ в registry, чтобы отдавать без локальной сборки. Но базовая цель (self-host из исходников одной командой) — достигнута.
- **PDF-экспорт в фирменном стиле + вирусный промо-блок (#39, 2026-05-25, коммит `f366529` на master):** `lib/export/to-pdf.tsx` переработан под фирстиль Planwise. Шапка (fixed) с лого-маркой — вкомпонована SVG-примитивами `Svg`/`Path` из `public/logo-mark.svg` (без растеризации, без новых ассетов) + водяной знак «Planwise» (brand-700) и подзаголовок. Заголовок brand-800; мета-блок — карточка brand-50 (brand-100 рамка, brand-700 лейблы); подзаголовки с акцентной полоской brand-500; фирменные буллеты. **Дисклеймер об ИИ вынесен в fixed-футер** (на каждой странице) + номер страницы. Палитра вписана вручную из `design_example` (Tailwind в react-pdf не работает). DOCX оставлен нейтральным (по ТЗ опц.).
  - **Грабли react-pdf v4 (найдено через systematic-debugging + растеризацию):** (1) `lineHeight` на `Page` ломает отрисовку `fixed`-absolute футера (исчезает) → межстрочный навешен на текстовые блоки тела, НЕ на Page (см. коммент в файле); (2) **субсеттинг шрифта нестабильно роняет глифы в составных документах** — у промо отваливались первые латинские глифы («Planwise»→«anwise», «plan-wise.ru»→«lan-wise.ru»), видно в реальных вьюерах (не только copy-слой). Баг content-sensitive (в одном тесте в fixed-хедере «fixed»→«fxed»), подгонкой текста/раскладки НЕ лечится. **Решение — запекать промо в PNG** (см. ниже).
  - **Вирусный промо-блок** («Создано в Planwise за пару минут» + текст + QR на `plan-wise.ru`) — чтобы коллега, увидевший распечатку, перешёл сам. **Запечён в статичный PNG** (текст+QR) и вставляется одной `<Image>` — картинка иммунна к багу субсеттинга. **Пришпилен к низу ПОСЛЕДНЕЙ страницы** (`position:absolute`, у Image глифового бага нет); **если не влезает — НЕ добавляется** (без лишней страницы): `renderScenarioPdf` рендерит дважды для замера (без промо vs промо-картинка в потоке), сравнивает число страниц — влезло → финал с absolute внизу; не влезло → версия без промо. Подтверждено визуально + граничным кейсом (без промо=3 стр., в потоке=4 → итог 3, промо опущено).
  - **Генерация статичных ассетов (оба контента фиксированы → генерим один раз, коммитим):** `scripts/gen-qr.ts` (`pnpm gen:qr`, dev-dep `qrcode`) → `assets/qr-planwise.png` (300×300, dark `#0e4f30`); `scripts/gen-promo.ts` (`pnpm gen:promo`, dev-dep `@resvg/resvg-js`) рендерит SVG со встроенным PT Sans (`fontFiles`) + QR base64 → `assets/promo-card.png` (1000×232). Обе либы — **dev** (рантайм/бандл чисты, RAM не растёт). При смене текста промо/URL — перегенерить `pnpm gen:qr && pnpm gen:promo`. Ассеты подключены в `next.config.ts` `outputFileTracingIncludes` (`./assets/**`) для ОБОИХ экспорт-роутов — **заодно починен пропуск ассетов у `/api/share/[token]/export`**. Подтверждено: `promo-card.png`+`qr-planwise.png`+шрифты в `.next/standalone/assets`. Без миграций. Гейты: 307 тестов, tsc, biome, build. **Деплой:** `git pull && docker compose up -d --build` (миграций нет).
- **Страница «Что нового» / changelog (2026-05-25, на master):** публичная статическая `/changelog` со списком версий сервиса (что добавлено/исправлено/улучшено). Контент — массив `CHANGELOG: ChangelogEntry[]` в `lib/changelog.ts` (`{version, date, changes:[{kind,text}]}`, новые сверху, правится руками как `lib/calendar-events.ts`; **БД/миграций нет**). Версии — семантические `vМАЖОР.МИНОР.ПАТЧ` (фича → +минор, багфикс → +патч). Страница `app/changelog/page.tsx` — server-компонент, переиспользует `LandingNavbar`+`Footer`, карточки версий с бейджами по типу: feature=brand, fix=warm, improvement=accent (метки «Добавлено/Исправлено/Улучшено» из `CHANGE_KIND_LABEL`). Вне `/app` → middleware не гейтит (matcher `/app/:path*`), собирается как статическая `○`. Ссылка «Что нового» добавлена в футер лендинга (`components/landing/Footer.tsx`). **Новый релиз = добавить объект в начало `CHANGELOG`.** Гейты: tsc, biome, build зелёные. **Деплой:** `git pull && docker compose up -d --build` (миграций нет).
- **Оценка сгенерированного сценария (#44, 2026-05-25, на master):** 👍/👎 + опц. отзыв по качеству генерации (питает будущий #57 admin quality dashboard). **Миграция `0013`:** `generations.rating smallint` (1=👍 / −1=👎 / null=не оценено) + `feedback text`. `rateGenerationAction(scenarioId, rating, feedback?)` (`app/app/scenarios/[id]/actions.ts`) — находит **последнюю `full`-генерацию** сценария с изоляцией `user_id`, проставляет оценку (валидация `rating∈{1,−1}`, feedback тримится и обрезается до 1000 симв., пустой → null); оценка относится к сценарию в целом, регенерации (`kind='regen'`) отдельно не оцениваются. **Без rate-limit** (дешёвая DB-запись, без LLM). Виджет `components/generation/RatingControls.tsx` рядом с лайком/шарингом в редакторе; при 👎 раскрывается необязательное поле отзыва, 👍 сохраняется мгновенно; страница `[id]/page.tsx` подтягивает текущую оценку (последняя full-генерация) и прокидывает в редактор. Без TDD (тонкая server-action склейка, как `likeScenarioAction`). Гейты: `pnpm test` (307 passed / 3 skip), tsc, biome, build. **Деплой требует `db:migrate`** (миграция 0013 — сервис `migrate` применит при `docker compose up -d --build`). Ручной UAT: 👍/👎 → персист после reload → отзыв при 👎.
- **Доработка оценки + счётчик регенераций в админке (2026-05-25, на master, расширение #44 / дополнение #7):** **без миграций** (поверх существующих `generations.rating/feedback` и `generations.kind`).
  - **Оценка:** поле отзыва теперь раскрывается при ЛЮБОЙ оценке (и 👍, и 👎), а не только при 👎; всегда необязательное. Блок `RatingControls` переоформлен в `Card` и перенесён из правого верхнего хедера ВНИЗ страницы сценария (перед закреплённой панелью сохранения) — `editor.tsx`. Server-action `rateGenerationAction` не менялся.
  - **Счётчик регенераций (админка):** `lib/admin/stats.ts` — `generationStats` доп. поле `regenTotal` (`count(*) FILTER (WHERE kind='regen')`), `userStats` доп. поле `topRegenUsers` (топ-10 по числу `kind='regen'` через JOIN users). В `/app/admin`: KPI «🎲-регенераций блоков» (сетка KPI → `lg:grid-cols-6`) + таблица «Топ пользователей по 🎲-регенерациям». Это НЕ #57 (там meta-метрики качества `thinBlocks`/`qualityWarnings`).
  - Гейты: `pnpm test` (307 passed / 3 skip), tsc, biome (изменённых файлов), build. Changelog: improvement к v1.7.0.
- **PPTX-парсинг загружаемых файлов (2026-05-27, на master, критерий 3 кейса):** реализовано через brainstorming → writing-plans → subagent-driven (имплементер + spec-review + quality-review). Спека `docs/superpowers/specs/2026-05-27-pptx-parsing-design.md`, план `docs/superpowers/plans/2026-05-27-pptx-parsing.md`. **Без миграций.**
  - Загрузка плана (`/app/plans`) и своего материала (`/app/new`) теперь принимает `.pptx` наравне с PDF/DOCX/TXT — проходит общий путь `parseFile` (лимит 5 МБ, magic-bytes, PII-анонимизация, изоляция). `lib/parse/index.ts`: `Kind` += `'pptx'`; PPTX валидируется как OOXML-архив (ZIP, magic `PK` — общая проверка с DOCX, своё сообщение об ошибке); новая `parsePptx` (jszip через **dynamic import**, не в бандле) — берёт только `ppt/slides/slideN.xml`, **численная** сортировка слайдов, извлечение текста из `<a:t>` (regex `<a:t[^>]*>` — терпит атрибуты `xml:space="preserve"`, иначе терялся текст реальных PowerPoint-файлов), разэкранирование XML-сущностей, слайды через `\n\n`. **jszip** добавлен прямой зависимостью (был транзитивным через mammoth). UI: `accept=".pptx"` + тексты-подсказки в `plans/upload-form.tsx`, `plans/page.tsx`, `new/page.tsx`. Changelog: **v1.8.0** (feature).
  - **Вне scope (YAGNI):** заметки докладчика, мастер-слайды, таблицы/диаграммы — только видимый текст слайдов.
  - **Урок (повтор):** баг с атрибутами `<a:t>` поймал quality-review, не моки — на реальных PowerPoint-файлах большинство ранов идёт с `xml:space="preserve"`.
  - Гейты: `pnpm test` (319 passed / 3 skip — после rebase на коммит коллеги по PII-падежам), tsc, lint (наши файлы; pre-existing biome-ошибки в `presentation/build.js` не трогали), build. **Ручной UAT перед демо:** загрузить реальный `.pptx` → проверить извлечение текста, PII-diff, генерацию.
- **PII: подавление одиночной детекции имён-омонимов (2026-05-27, на master):** реализовано через brainstorming → writing-plans → subagent-driven (имплементер + spec/quality-ревью). Спека `docs/superpowers/specs/2026-05-27-pii-homonym-names-design.md`, план `docs/superpowers/plans/2026-05-27-pii-homonym-names.md`. **Без миграций.** Контекст: коллега добавил в `lib/pii/names.ts` детекцию косвенных падежей (коммит `e740561`), что усилило over-redaction (#16) — слова-ценности `вера/надежда/любовь/роман/лев` редактились как ПДн.
  - **Решение (точечное, только правило 3 «одиночное имя»):** `AMBIGUOUS_NAMES = ['Вера','Надежда','Любовь','Роман','Лев']` → `AMBIGUOUS_NAME_FORMS` (их падежные формы через существующую `buildNameForms`) → гард `&& !AMBIGUOUS_NAME_FORMS.has(m[1])` в правиле 3. **Правила 1 (ФИО+отчество) и 2 (имя+фамилия) не тронуты** → `Вера Иванова`, `Веры Ивановой`, `Романа Иванова`, `Льва Николаевича`, `Любови Петровне` детектятся как раньше; одиночные `Любовь к Родине`/`Вера в себя`/`Веру` → больше не редактятся.
  - **Осознанный компромисс:** редкое одиночное «ученица Вера» без фамилии больше не обезличивается (имя без фамилии слабо идентифицирует; калечение сценариев о ценностях — заметнее). Сильные идентификаторы (телефон/email/СНИЛС/ФИО) не затронуты.
  - **Заодно почищен lint коллеги:** 7 biome-ошибок форматирования (trailing whitespace, финальные newline) + рефактор `SURNAME` на шаблонную литералу (`useTemplate`/`noUnusedTemplateLiteral`) — source regex байт-идентичен, тесты-гарды зелёные. Теперь все `lib/pii/`+тесты проходят biome.
  - **Вне scope (YAGNI / backlog-кандидаты):** `Виктория`(победа)/`Марина`(морской пейзаж) как омонимы — заглавные почти всегда имена, исключены; контекст-гард по словам-маркерам.
  - Гейты: `pnpm test` (323 passed / 3 skip), tsc, biome (все наши файлы чисты), build. Changelog: improvement к v1.8.0. **Ручной UAT перед демо:** сценарий о ценностях («Любовь к Родине») → слова-ценности не обезличиваются; материал с реальным ФИО → ФИО скрывается.
- **UI-история версий сценария (2026-05-27, на master, критерий 4 кейса):** реализовано через brainstorming → spec → inline. Спека `docs/superpowers/specs/2026-05-27-version-history-design.md`. **Без миграций** — `scenario_versions` уже копит снапшоты `content` на каждый save/генерацию/копию (своего `user_id` нет, изоляция через принадлежность `scenario_id` юзеру). Закрыт пункт 4 `docs/requirements-compliance.md`.
  - **Server actions** (`app/app/scenarios/[id]/actions.ts`): `listVersionsAction` (лёгкий список последних 30 `{id, createdAt}` без `content`, desc по дате, проверка владения через `loadOwned`); `getVersionAction` (content для предпросмотра, **двойная изоляция** — сценарий принадлежит юзеру И `WHERE version.id=? AND scenario_id=?`); `restoreVersionAction` (берёт content снапшота → в одной транзакции `UPDATE scenarios` + `INSERT scenario_versions` = **откат = новая версия, текущее состояние не теряется**, можно отменить откат; `revalidatePath`; без LLM → без rate-limit). Все три возвращают дискриминированный `{ok}`-результат.
  - **UI:** `components/scenario/VersionHistory.tsx` — кнопка «История» в шапке редактора (рядом с PDF/DOCX) → fixed-overlay-панель справа: список версий слева («Версия N • дата/время», новейшая помечена «текущая»), read-only предпросмотр справа через переиспользование `buildScenarioDocument(content, meta)` + `<ScenarioReadOnly>`, кнопка «Восстановить» с `confirm()`. После отката редактор подменяет локальный `content` (`applyRestored` → dirty=false, уже сохранено), панель закрывается. `Meta` в `editor.tsx` расширен `topic` (нужен для `buildScenarioDocument`), прокинут из `page.tsx` (`scenario.topic`).
  - Гейты: `pnpm test` (323 passed / 3 skip), tsc, biome, build (`/app/scenarios/[id]` 8.1 КБ). Changelog: feature к v1.8.0. Без юнит-тестов (тонкая server-action склейка, как `rateGenerationAction`).
  - **Откат — inline-подтверждение (НЕ нативный `confirm()`):** при живом UAT нативный `confirm()` намертво вешал браузерную автоматизацию (CDP не видит модальное окно браузера) и был бы неудобен в скринкасте. Заменён на двухшаговую кнопку прямо в панели («Восстановить эту версию» → «Да, восстановить» / «Отмена», состояние `confirming`).
  - **Живой UAT ВЫПОЛНЕН (2026-05-27, прод `plan-wise.ru`, аккаунт UAT):** список версий ✅, предпросмотр старой версии ✅, откат ✅ (откатил Версию 2 → заголовок сменился, статус «Версия восстановлена», персист подтверждён в БД: каждый откат пишет новый снапшот). Тестовый артефакт убран. **Долг:** пруннинг снапшотов не реализован (копятся на каждый save/откат, показываем последние 30) — кандидат в backlog.
- **Полноэкранный режим показа сценария (2026-05-27, на master, критерий 5 кейса):** реализовано через brainstorming → spec → TDD (хелпер) + inline (UI). Спека `docs/superpowers/specs/2026-05-27-presentation-mode-design.md`. **Без миграций, без server actions** — чистая клиентская обёртка над `ScenarioContent`. Закрыт пункт 5 `docs/requirements-compliance.md`.
  - **Хелпер** `lib/scenario/slides.ts` (TDD, 5 тестов): `buildSlides(content, meta) → Slide[]` — титульный слайд (название + бейджи `direction/formatGrade/мин/format`), затем по одному слайду на этап; контент тезисный: у активности есть `questions` → буллеты вопросов, иначе → `text` активности. Решение по гранулярности (этап=слайд) и контенту (тезисы) согласовано с пользователем.
  - **UI** `components/scenario/PresentationMode.tsx` — кнопка «Показ» в шапке редактора → fixed-overlay на весь экран поверх **текущего** `content` (несохранённые правки видны), best-effort `requestFullscreen`. Навигация: ←/→/Space — листать, Esc — выход; экранные «Назад»/«Далее» + счётчик «N / M». Крупный шрифт, фирменная палитра, длинный текст активности — прокрутка внутри overlay. `editor.tsx`: кнопка между PDF/DOCX и «История».
  - Гейты: `pnpm test` (328 passed / 3 skip — +5), tsc, biome, build. Changelog: feature к v1.8.0.
  - **Навигация: стрелки ←/→, пробел, И PageUp/PageDown** (презентационные кликеры шлют Page Up/Down) + экранные «Назад»/«Далее» + Esc. Адаптивные шрифты/отступы под телефон (`text-3xl sm:text-5xl` и т.п.), overlay `fixed inset-0` накрывает viewport и без системного fullscreen (iOS Safari).
  - **Живой UAT ВЫПОЛНЕН (2026-05-27, прод, аккаунт UAT):** «Показ» → overlay на весь экран ✅, титул+бейджи ✅, слайды этапов с вопросами/текстом ✅, навигация → и экранными кнопками ✅, блокировка «Назад»/«Далее» на границах ✅, Esc-выход ✅. **Вне scope (YAGNI):** показ на `/s/[token]`, заметки докладчика, таймер этапа, анимации.
- **Видимая квота генераций + очередь к GigaChat (2026-05-30, ветка `feat/quota-badge-gigachat-queue`, ожидает merge в master, закрывает backlog #33/#34 + #64):** реализовано через brainstorming → spec → writing-plans → subagent-driven (10 задач, имплементер + spec/quality-review + финальное холистическое **READY TO MERGE**). Спека `docs/superpowers/specs/2026-05-30-quota-badge-and-gigachat-queue-design.md`, план `docs/superpowers/plans/2026-05-30-quota-badge-and-gigachat-queue.md`. **Без миграций.**
  - **Бейдж квоты:** `lib/ratelimit/usage.ts` `getDailyGenerationUsage(userId, email, role)` — чистое чтение существующей `rate_buckets` (`current('generate', userId, ws)`), без `cleanup`/`increment`; TDD 6 кейсов; **админ (`role='admin'`) или `DEMO_USER_EMAILS` → `{unlimited:true}`**. Новый флаг `RateCheck.bypass` в `checkRateLimit` (TDD) — ранний return ДО whitelist; прокинут в `app/api/generate/stream/route.ts` как `bypass: session.user.role==='admin'` (whitelist через `email` сохранён). Server-компонент `components/nav/QuotaBadge.tsx` — `N/limit` нейтральный → жёлтый ≤3 → красный 0; `∞` для unlimited; `title` со временем сброса (`resetAt.toLocaleTimeString('ru-RU')`). `app/app/layout.tsx` считает usage рядом с `auth()`, прокидывает в `AppNavbar`. Без поллинга — обновление на server-навигации (после генерации идёт `router.push` → пере-рендер).
  - **Очередь GigaChat:** `lib/gigachat/concurrency.ts` `withGigaChatSlot(fn, {onQueued, signal})` — in-memory семафор `GIGACHAT_MAX_CONCURRENCY` (деф.1, env→5 на новом тарифе) + FIFO до `GIGACHAT_QUEUE_MAX` (деф.10) + таймаут `GIGACHAT_QUEUE_TIMEOUT_MS` (деф.300_000) + AbortSignal-снятие + `QueueOverflowError`/`QueueTimeoutError`. Singleton модуля (прод = один Node-процесс), `__resetForTests` (TDD 6 кейсов с `vi.useFakeTimers`). Гейтит ВСЕ вызовы GigaChat: `chatCompletion` (тривиально), `chatCompletionStream` (channel-pattern `chunks[]`+`notify` — **слот удерживается весь стрим**, иначе следующий запрос пойдёт параллельно с консумом ридера и упрётся в 429 GigaChat), `embed` (per-batch). Расширен тип `Choice.delta?:{content?:string}` в `types.ts` для стрим-кадров.
  - **SSE-индикатор «вы N-й в очереди»:** `streamScenario` (`lib/scenario/stream.ts`) расширен новым `StreamEvent {type:'queued', position}` + `error.code?: 'queue_overflow'|'queue_timeout'`. `onQueued` прокинут в первый user-visible вызов (`chatStream` для skeleton); `pendingQueued`-буфер выгружается до и во время первых chunk'ов. Inner-catch в RAG/prematch re-throw'ит `instanceof QueueOverflowError|QueueTimeoutError` (иначе try/catch их глотал бы — best-effort fallback не должен скрывать перегрузку). Outer catch маппит на коды; route отдаёт стандартный SSE. UI `components/generation/GenerationStream.tsx`: новый state `queuePosition`; при `queued` показывает «⏳ Вы N-й в очереди» (warm-палитра, `animate-pulse`) вместо прогресс-фаз; при `phase` — переключение. Error-варианты с кодами — отдельные сообщения «Сервис перегружен» / «Очередь не освободилась за 5 минут».
  - **Конфигурация:** новые env в `.env.example` — `GIGACHAT_MAX_CONCURRENCY=1`, `GIGACHAT_QUEUE_MAX=10`, `GIGACHAT_QUEUE_TIMEOUT_MS=300000`. Changelog: **v1.9.0** (две feature).
  - **Известные ограничения (зафиксированы в холистическом ревью):** (а) `GIGACHAT_QUEUE_TIMEOUT_MS` лимитирует ТОЛЬКО ожидание в очереди — не время самого in-flight стрима. При `MAX_CONCURRENCY=1` тяжёлая генерация (~45с) сериализует всех. ОК для текущего масштаба; при росте — поднимать concurrency после переключения на новый тариф GigaChat. (б) Бейдж не «тикает» в реальном времени — обновление на server-навигации, что нормально. (в) Очередь — process-local; при горизонтальном масштабировании понадобится Redis (отмечено).
  - Гейты: `pnpm test` (341 passed / 3 skip — +13), `tsc --noEmit`, `pnpm lint` (исходники чисты), `pnpm build`. **Деплой:** миграций нет → `git pull && docker compose up -d --build`. **Ручной UAT перед мержем** (5 шагов из спеки): обычный юзер → бейдж `10/10` → генерация → `9/10`; админ → `∞`; две вкладки параллельно → вторая «Вы 2-й в очереди»; `MAX_GENERATIONS_PER_DAY=3` → исчерпать → 429; `GIGACHAT_QUEUE_MAX=1` → три вкладки → третья «Сервис перегружен».
- **Соответствие сценариев ФГОС/ФОП — P1+P2 (2026-05-30, ветка `feat/fgos-fop-compliance`, ожидает merge в master, **READY TO MERGE**):** реализовано через brainstorming → spec → writing-plans → subagent-driven (13 задач, имплементер + spec/quality-review на каждой + финальное холистическое **READY TO MERGE**). Опорный документ — `docs/fgos-fop-compliance.md` (исследование нормативной базы): «соответствие ФГОС» = отражение в сценарии 4 сущностей (8 направлений, личностные результаты по уровню, трёхчастная структура, санитарные лимиты). Покрыты приоритеты P1 (личностные результаты + методическая шапка экспорта) и P2 (возрастной кап + проверка рефлексии); P3 (маркетинг лендинга) — оставлен на отдельный микропатч. Спека `docs/superpowers/specs/2026-05-30-fgos-fop-compliance-p1-p2-design.md`, план `docs/superpowers/plans/2026-05-30-fgos-fop-compliance-p1-p2.md`. **Без миграций** (поле живёт в jsonb `content`, optional).
  - **Каталог личностных результатов** (`lib/scenario/personal-results.ts`): статический `CATALOG: Record<Level, Record<CanonicalDirection, string[]>>` — 24 ячейки (3 уровня × 8 канонических направлений), по 2–8 **дословных** формулировок из текстов ФГОС НОО (приказ № 286 п. 41), ФГОС ООО (№ 287 п. 42), ФГОС СОО (№ 413 в ред. № 732, п. 7.1). Источник в комментарии каждой ячейки. Гражданско-патриотический блок ФГОС расщеплён на `Гражданское` (гражданская идентичность, противодействие экстремизму) и `Патриотическое` (Родина, символы, традиции) — один общий пункт намеренно дублируется. `getCatalog(level, direction)` принимает любое UI-направление и маппит на каноническое через `canonicalDirection` (`lib/scenario/levels.ts` — `gradeToLevel(1..12)→НОО/ООО/СОО`, СПО=12→СОО; 11 UI-направлений → 8 канонических: Семейные ценности→Духовно-нравственное, Профориентация→Трудовое, ЗОЖ→Физическое и здоровье). `validateAgainstCatalog` нормализует пробелы (`\s+ → ` `, trim) и пропускает только дословные совпадения. `selectPersonalResults` дедуплицирует, обрезает до 5, добирает первыми из каталога до MIN=3 при пустом/невалидном входе.
  - **Поток в стриме генерации:** `lib/scenario/prompt.ts` (`PROMPT_VERSION → v9-personal-results-2026-05-30`) в `buildSkeletonMessages` вкладывает секцию `[PERSONAL_RESULTS_CATALOG]` (нумерованный список 5–8 формулировок) с инструкцией «выбери 3–5, верни ДОСЛОВНО, без правок». В `lib/scenario/stream.ts` СРАЗУ после `parseSkeleton` (до per-block цикла) — `skeleton.personalResults = selectPersonalResults(skeleton.personalResults, getCatalog(...))`. Гарантия: в `ScenarioContent.personalResults` НИКОГДА не попадает формулировка вне каталога ФГОС, минимум 3 даже если LLM ничего валидного не вернула. На редактировании учителем whitelist НАМЕРЕННО не применяется (поле в БД — `string[]`; учитель может править под свой класс; whitelist — только защита выхода LLM).
  - **Схема** (`lib/scenario/schema.ts`): `personalResults: z.array(z.string().min(1)).max(8).optional()` в `scenarioContentSchema` (строгий) + `personalResults: z.array(z.string()).optional()` в `skeletonSchema` (мягкий — LLM может прислать мусор, отфильтрует `selectPersonalResults`). Старые сценарии в БД без поля валидируются как раньше — `optional`. Возрастной кап через `.superRefine` на `generationInputSchema`: cap=`grade === 1 ? 35 : 45` (СанПиН 1.2.3685-21), issue на `path: ['durationMin']`, сообщение содержит «1 класс» или `formatGrade(grade)`. Ловится на сервере во ВСЕХ путях: `/api/generate/stream`, server actions, прямой POST.
  - **Проверка рефлексии** (`lib/scenario/quality.ts`, расширение `checkScenario`): два неблокирующих warning'а — (а) «В сценарии нет этапа рефлексии (заключительная часть)» если `stages.every(s => s.kind !== 'reflection')`; (б) «В этапе рефлексии нет вопросов для обратной связи» если активности этапа не содержат ни `questions?.length > 0`, ни `'?'` в `text`. Попадают в `meta.qualityWarnings` и существующий неблокирующий баннер редактора.
  - **Методическая шапка экспорта** (`lib/export/document-model.ts`): `metaTable` расширен до 6 фиксированных строк (Тема / Направление воспитания / Класс / уровень / Длительность / Формат / Цель занятия) + 2 опциональных (Формируемые ценности / Оборудование). Класс/уровень = `${formatGrade(grade)} (${levelLabel(gradeToLevel(grade))})` → «6 класс (ООО)». Цель = `goals[0]` + ` (и др.)` при >1 цели. После «Основных смыслов» и ДО хода занятия — отдельный блок «Планируемые личностные результаты» (heading L2 + bullets, рендерится только при непустом массиве). **Дублирующий блок «Материалы» в теле документа УДАЛЁН** — данные теперь в шапке как «Оборудование» (старые тесты обновлены, чтобы это отразить). PDF, DOCX и share-страница `/s/[token]` (через `ScenarioReadOnly`+`buildScenarioDocument`) — наследуют новый рендер автоматически.
  - **UI:**
    - Редактор `app/app/scenarios/[id]/editor.tsx`: новая `<Card>` «Планируемые личностные результаты» между «Цели» и «Формируемые ценности» (де-факто между Целями и Материалами в стиле остальных полей). Подсказка «Из ФГОС {уровень}, направление «{направление}»». Каждый пункт — `<Textarea rows={2}>` + кнопка ✕, добавление через «+ Добавить». Совместимо со старыми сценариями (`?? []`).
    - Форма `/app/new` (`app/app/new/page.tsx`): `<select name="grade">` и `<select name="durationMin">` теперь управляемые. При переключении класса с большой длительностью — авто-снап к наибольшей валидной опции из `DURATIONS=[20,30,40,60]` ≤ cap (для grade=1 cap=35 → snap до 30, а не до невалидного 35 — fix-коммит `4068a4e` поверх основной задачи). Опции `<option>` фильтруются `DURATIONS.filter(d => d <= cap)`.
  - **Changelog (по решению пользователя — приписать в v1.9.0, НЕ создавать v1.10.0):** v1.9.0 в `lib/changelog.ts` дополнен 4 пунктами (1 improvement про шапку экспорта добавлен ещё в Task 9, остальные 3 — в Task 13): feature про блок личностных результатов в сценариях; improvement про возрастной кап; improvement про warning рефлексии.
  - Гейты зелёные на каждом коммите: `pnpm test` (383 passed / 3 skip, +25 новых), `pnpm exec tsc --noEmit`, `pnpm exec biome check` (затронутые файлы чисты; 15 pre-existing warnings про `any`/`noArrayIndexKey` в моках/UI с inline-комментами — не наши), `pnpm build` (роуты `/app/scenarios/[id]`, `/app/new`, `/api/generate/stream` в выводе). 13 атомарных коммитов (по одному на задачу + fix snap).
  - **Деплой:** миграций нет → `git pull && docker compose up -d --build`.
  - **НЕ выполнено (ручной UAT перед мержем):** живая генерация против GigaChat — 6 класс / Патриотическое / «День народного единства» / 30 мин: проверить, что каркас возвращает 3–5 личностных результатов ИЗ каталога ООО, шапка PDF/DOCX показывает «6 класс (ООО)» + блок результатов; старый сценарий без `personalResults` открывается без падений; форма `/app/new` блокирует 1кл/60мин на UI и сервере (`curl -X POST .../api/generate/stream` с `grade=1, durationMin=45` → 400).
  - **Известное ограничение (важно при человеческом ревью каталога):** дословность формулировок НОО (приказ № 286) и ООО (приказ № 287) проверялась через WebFetch, часть источников отказала (paywall/JS) — финальная сверка дословности этих 16 ячеек требует 5-минутного человеческого прохода по consultant.ru (`cons_doc_LAW_389561` / `cons_doc_LAW_389560`). СОО полностью совпадает с `normativ.kontur.ru/501748` (verified). Структурно каталог корректен, формулировки выглядят аутентично-нормативно, но строгое слово-в-слово стоит подтвердить глазами. Риск низкий (whitelist всё равно работает; формулировки правдоподобные), но критичен для «методически точного» позиционирования.
  - **Не вошло в scope (backlog):** P3 (маркетинг — заменить «соответствует ФГОС» на «с опорой на ФГОС/ФОП» на лендинге; не использовать госсимволику); метаданное поле `personalResultsFallback: boolean` в `GenerationMeta` (упомянуто в спеке как опциональное, не реализовано); UI-disable кнопки «+ Добавить» при достижении `max(8)` (косметика, при save zod всё равно блокирует).

### Конвенции работы
- **Перед каждым коммитом+пушем (по умолчанию без соавторства) ОБЯЗАТЕЛЬНО проверить, отражены ли пользовательски-заметные изменения в странице изменений `lib/changelog.ts`** (новые функции/исправления/улучшения). Если нет — сначала добавить запись (новый объект в начало `CHANGELOG` или пункт в текущую версию), и только потом коммитить и пушить. Чисто внутренние правки (рефакторинг, тесты, доки, CLAUDE.md) в changelog не вносить.
- **Один коммит на задачу плана.** Атомарность для отката.
- **TDD для `lib/pii`, `lib/rag/score`, валидаторов и любой нетривиальной логики.** Тесты сначала.
- Перед коммитом каждой задачи — `superpowers:verification-before-completion`.
- Сложные баги — `superpowers:systematic-debugging`, не угадывать.
- Перед мержем фазы — `superpowers:requesting-code-review`. Перед демо — `security-review`.
- При параллельных фазах (например 4 ‖ 5) — `superpowers:using-git-worktrees`.

---

## Available environment (inventory as of 2026-05-18)

### Installed plugins
- **superpowers@claude-plugins-official** v5.1.0 — installed 2026-05-18, path: `~/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0`.
  Ships skills, hooks, agents (`AGENTS.md`), scripts. Skills become available after CLI restart.

### Plugin skills (superpowers)
- `brainstorming`
- `dispatching-parallel-agents`
- `executing-plans`
- `finishing-a-development-branch`
- `receiving-code-review`
- `requesting-code-review`
- `subagent-driven-development`
- `systematic-debugging`
- `test-driven-development`
- `using-git-worktrees`
- `using-superpowers`
- `verification-before-completion`
- `writing-plans`
- `writing-skills`

### Built-in / preinstalled skills
- Code & review: `code-review:code-review`, `review`, `security-review`, `simplify`, `init`, `frontend-design:frontend-design`, `claude-api`
- Documents (Anthropic): `pptx`, `pdf`, `docx`, `xlsx`, `skill-creator`, `consolidate-memory`, `setup-cowork`
- Obsidian: `obsidian-markdown`, `json-canvas`, `obsidian-bases`, `defuddle`, `obsidian-cli`
- Harness/config: `update-config`, `keybindings-help`, `fewer-permission-prompts`, `loop`, `schedule`

### MCP servers available
- `computer-use` — desktop control (tiered: browsers=read, terminals/IDE=click, other=full). Requires `request_access`.
- `Claude_in_Chrome` — DOM-aware browser automation
- `ccd_session_mgmt`, `ccd_session` — session/chapter management
- `mcp-registry` — search/suggest connectors
- `scheduled-tasks` — cron-like scheduled agents

### Deferred tools (load via ToolSearch before use)
TodoWrite, WebFetch, WebSearch, CronCreate/List/Delete, EnterPlanMode/ExitPlanMode, EnterWorktree/ExitWorktree, Monitor, NotebookEdit, PushNotification, RemoteTrigger, TaskOutput/TaskStop, plus all `mcp__*` tools listed above.

### Memory location
`~/.claude/projects/-home-nikit-planwise/memory/` — index in `MEMORY.md`.
