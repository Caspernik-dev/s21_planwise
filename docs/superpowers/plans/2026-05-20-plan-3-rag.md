# Plan 3 «RAG» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать генератору сценариев опору на корпус методичек: эмбеддинги GigaChat, таблицы `rag_documents`/`rag_chunks` + `scenarios.embedding`, структурный чанкинг, гибридный retrieval (0.7·cosine + 0.3·BM25) с диверсификацией, seed-корпус и ingest «Разговоров о важном», а затем подмешать топ-чанки в промпт генерации.

**Architecture:** Расширяем существующий GigaChat-клиент функцией `embed()` (батчинг 32, идемпотентность по sha256 хэшу чанка). Векторы храним через Drizzle `customType` (`vector(1024)`) + `tsvector`-колонку с GIN/HNSW-индексами в кастомной миграции. Чистая логика чанкинга и гибридного скоринга — отдельные модули с TDD. Retrieval — один SQL-запрос (cosine `<=>` + `ts_rank`) с post-фильтрацией (нормализация BM25, диверсификация ≤2 чанка/документ, fallback при пустом результате). Ingest seed-корпуса строит и проверяет всю инфраструктуру, ничего не блокируя; ingest razgovor — отдельной задачей по sitemap. На последнем шаге `generate.ts` вызывает retrieval и кладёт чанки в промпт.

**Tech Stack:** Next.js 15 / TypeScript, Drizzle ORM + postgres-js, PostgreSQL 16 + pgvector (HNSW + GIN tsvector), GigaChat `EmbeddingsGigaR` (1024 dims), Vitest, tsx-скрипты, `pdf-parse`, `gray-matter`.

---

## Контекст: что уже есть (Plan 2, ветка `generation-v0-done`)

Реальные интерфейсы, на которые опирается план (НЕ переписывать, только расширять):

**`lib/gigachat/config.ts`** — `getGigaConfig()` → `{ authKey, scope, oauthUrl, apiBase, model, insecureTls }`. Читает `GIGACHAT_*` env. `apiBase` по умолчанию `https://gigachat.devices.sberbank.ru/api/v1`.

**`lib/gigachat/token.ts`** — `getAccessToken(): Promise<string>` с кэшем (margin 60с), `__resetTokenCacheForTests()`.

**`lib/gigachat/tls.ts`** — `ensureInsecureTls(insecure: boolean)`.

**`lib/gigachat/client.ts`** — `chatCompletion(messages, opts)`. Образец стиля: дергает `getGigaConfig()` → `ensureInsecureTls()` → `getAccessToken()` → `fetch(\`${cfg.apiBase}/chat/completions\`)`, бросает `Error` при `!res.ok`.

**`lib/gigachat/types.ts`** — `GigaMessage`, `ChatCompletionResponse`, `OAuthResponse`, `ChatResult`.

**`lib/scenario/schema.ts`** — `scenarioContentSchema` (zod), `ScenarioContent`, `generationInputSchema`, `GenerationInput` (`{ direction, grade, topic, durationMin, format }`), `GenerationMeta` (`{ model, promptVersion, repaired, normalized, usage, latencyMs }`).

**`lib/scenario/prompt.ts`** — `PROMPT_VERSION`, `buildMessages(input: GenerationInput): ChatMessage[]`. Собирает system+user. **Точка интеграции RAG.**

**`lib/scenario/generate.ts`** — `generateScenario(input, deps?): Promise<{ content, meta }>`. `deps.chat` инъектируется в тестах. **Точка интеграции RAG.**

**`lib/scenario/options.ts`** — `DIRECTIONS` (8 направлений), `FORMATS`, `GRADES`.

**`db/index.ts`** — `export const db = drizzle(client, { schema })`, postgres-js, `export type DB`.

**`db/schema.ts`** — таблицы `users`, `accounts`, `sessions`, `verificationTokens`, `scenarios`, `scenarioVersions`, `generations`. Все id — `text(...).$defaultFn(() => crypto.randomUUID())`. `scenarios` уже есть, нужно добавить колонку `embedding`.

**`db/migrate.ts`** — грузит `.env.local` через dotenv, `migrate(db, { migrationsFolder: './db/migrations' })`.

**`drizzle.config.ts`** — `schema: './db/schema.ts'`, `out: './db/migrations'`, dialect postgresql.

**Миграции:** `db/migrations/0000_*.sql` (auth), `0001_enable_pgvector.sql` (`CREATE EXTENSION vector`), `0002_wise_raza.sql` (scenarios/versions/generations). pgvector уже стоит.

**Тесты:** `tests/**/*.test.ts`, alias `@` → корень. Мок GigaChat = `vi.stubGlobal('fetch', fetchMock)` с ветками на `/oauth` и `/chat/completions` (см. `tests/lib/gigachat/client.test.ts`). DB-смоук — реальная БД (`tests/smoke/db.test.ts`).

**Скрипты:** паттерн `import { config } from 'dotenv'; config({ path: '.env.local' }); config()` затем async `main().catch(...)`. Запуск через `tsx`.

**package.json:** есть `zod`, `drizzle-orm`, `postgres`, `dotenv`, `tsx`. **НЕТ:** `pdf-parse`, `gray-matter`. Линтер — Biome (`pnpm lint`). Тесты — `pnpm test`.

**Env (`.env.example`):** `DATABASE_URL`, `GIGACHAT_AUTH_KEY`, `GIGACHAT_SCOPE`, `PG_TSV_LANG=russian`, `SIMILARITY_THRESHOLD=0.78`. Реальные ключи — в `.env.local` (скопирован в worktree).

### Жёсткие ограничения проекта (из CLAUDE.md)
- LLM/эмбеддинги — **только внешний GigaChat API**, никаких локальных моделей/токенайзеров (tiktoken не ставим — оценка токенов эвристикой).
- RAM 4 ГБ — без Puppeteer и тяжёлых ML-зависимостей.
- Пользовательские таблицы — только запросы с `WHERE user_id = ?`. (RAG-таблицы `rag_documents`/`rag_chunks` — **общие, не пользовательские**, поэтому фильтра по user_id у них нет — это нормально и осознанно.)
- UI на русском; здесь UI не трогаем.

### Вне scope этого плана (явно)
- Pre-match по `shared_scenarios` и `scripts/calibrate-threshold.ts` — таблицы `shared_scenarios` ещё нет (отдельный план «Likes/Shared»). `SIMILARITY_THRESHOLD` не калибруем здесь.
- Стриминг (SSE) — остаётся как в Plan 2 (single-shot).
- Подмешивание примеров из shared в промпт — нет таблицы; промпт получает только методички (+опц. user plan в будущем).

---

## File Structure

**Создаём:**
- `lib/gigachat/embeddings.ts` — `embed(texts: string[]): Promise<number[][]>`, батчинг 32.
- `db/types.ts` — Drizzle `customType` для `vector(n)` и `tsvector`.
- `lib/rag/hash.ts` — `chunkHash(text): string` (sha256 hex).
- `lib/rag/chunk.ts` — `chunkStructured(text, opts): Chunk[]` (структурный чанкинг 300–800 токенов).
- `lib/rag/score.ts` — `normalizeBm25`, `combineScore`, `diversify` (чистые функции, TDD).
- `lib/rag/ingest.ts` — `ingestDocument(doc, deps): Promise<{ inserted, skipped }>` (embed батчами + идемпотентный upsert + tsv).
- `lib/rag/retrieve.ts` — `retrieveChunks(query, deps?): Promise<RetrievedChunk[]>` (гибридный SQL + диверсификация + fallback).
- `content/seed-scenarios/*.md` — 12 эталонных сценариев (frontmatter + markdown).
- `scripts/ingest-seed.ts` — ingest seed-корпуса.
- `scripts/ingest-razgovor.ts` — ingest «Разговоров о важном» по sitemap.

**Изменяем:**
- `lib/gigachat/config.ts` — добавить `embedModel`.
- `lib/gigachat/types.ts` — добавить `EmbeddingsResponse`.
- `db/schema.ts` — добавить `scenarios.embedding`, таблицы `ragDocuments`, `ragChunks`.
- `lib/scenario/schema.ts` — `GenerationMeta` добавить `usedChunkIds: string[]`.
- `lib/scenario/prompt.ts` — `buildMessages(input, ragChunks?)`.
- `lib/scenario/generate.ts` — retrieval перед промптом, прокинуть `usedChunkIds`.
- `app/app/new/actions.ts` — populate `scenarios.embedding` после сохранения.
- `package.json` — скрипты `ingest:seed`, `ingest:razgovor`; зависимости.
- `.env.example` — `GIGACHAT_EMBED_MODEL`, `RAG_*`.

---

## Task 1: Зависимости и env

**Files:**
- Modify: `package.json`
- Modify: `.env.example`

- [ ] **Step 1: Установить рантайм-зависимости**

```bash
pnpm add pdf-parse gray-matter
pnpm add -D @types/pdf-parse
```

Expected: `pdf-parse`, `gray-matter` в `dependencies`; `@types/pdf-parse` в `devDependencies`.

- [ ] **Step 2: Добавить скрипты в package.json**

В блок `"scripts"` добавить:

```json
    "ingest:seed": "tsx scripts/ingest-seed.ts",
    "ingest:razgovor": "tsx scripts/ingest-razgovor.ts"
```

- [ ] **Step 3: Добавить env-переменные в .env.example**

Дописать в конец `.env.example`:

```
# RAG / Embeddings
GIGACHAT_EMBED_MODEL=EmbeddingsGigaR
RAG_EMBED_BATCH=32
RAG_TOP_K=3
RAG_CANDIDATES=24
RAG_MAX_PER_DOC=2
```

`PG_TSV_LANG` уже есть в .env.example. Скопировать те же строки в локальный `.env.local` (значения по умолчанию подходят; `GIGACHAT_EMBED_MODEL=EmbeddingsGigaR`).

- [ ] **Step 4: Verify**

Run: `pnpm install && pnpm lint`
Expected: установка без ошибок, lint чистый.

- [ ] **Step 5: Commit**

```bash
git add package.json pnpm-lock.yaml .env.example
git commit -m "chore(rag): add pdf-parse/gray-matter deps, ingest scripts, RAG env vars"
```

---

## Task 2: GigaChat embeddings (`lib/gigachat/embeddings.ts`)

**Files:**
- Modify: `lib/gigachat/config.ts:11-19` (добавить `embedModel` в возвращаемый объект и тип)
- Modify: `lib/gigachat/types.ts` (добавить `EmbeddingsResponse`)
- Create: `lib/gigachat/embeddings.ts`
- Test: `tests/lib/gigachat/embeddings.test.ts`

GigaChat embeddings endpoint: `POST {apiBase}/embeddings`, тело `{ model, input: string[] }`, ответ `{ data: [{ embedding: number[], index: number }, ...] }`. Авторизация — `Bearer <token>` (как в chat). Модель `EmbeddingsGigaR` → 1024 dims. Батч ≤32 строк/запрос.

- [ ] **Step 1: Расширить config**

В `lib/gigachat/config.ts` в тип `GigaConfig` добавить поле `embedModel: string`, в `getGigaConfig()` в возвращаемый объект добавить:

```ts
    embedModel: process.env.GIGACHAT_EMBED_MODEL ?? 'EmbeddingsGigaR',
```

- [ ] **Step 2: Добавить тип ответа**

В `lib/gigachat/types.ts` дописать:

```ts
export type EmbeddingsResponse = {
  data: Array<{ embedding: number[]; index: number }>
  model?: string
}
```

- [ ] **Step 3: Написать падающий тест**

`tests/lib/gigachat/embeddings.test.ts`:

```ts
import { embed } from '@/lib/gigachat/embeddings'
import { __resetTokenCacheForTests } from '@/lib/gigachat/token'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'

beforeEach(() => {
  __resetTokenCacheForTests()
  process.env.GIGACHAT_AUTH_KEY = 'dGVzdDp0ZXN0'
  process.env.GIGACHAT_SCOPE = 'GIGACHAT_API_PERS'
  process.env.GIGACHAT_OAUTH_URL = 'https://oauth.example/api/v2/oauth'
  process.env.GIGACHAT_API_BASE = 'https://giga.example/api/v1'
  process.env.GIGACHAT_EMBED_MODEL = 'EmbeddingsGigaR'
  process.env.RAG_EMBED_BATCH = '32'
})

afterEach(() => vi.unstubAllGlobals())

function stub(embeddingFor: (text: string, idx: number) => number[]) {
  const calls: unknown[][] = []
  const fetchMock = vi.fn(async (url: string, init: { body?: string }) => {
    calls.push([url, init])
    if (String(url).includes('/oauth')) {
      return {
        ok: true,
        status: 200,
        json: async () => ({ access_token: 'tok', expires_at: Date.now() + 1_800_000 }),
      }
    }
    const body = JSON.parse(String(init.body)) as { input: string[] }
    return {
      ok: true,
      status: 200,
      json: async () => ({
        data: body.input.map((t, i) => ({ embedding: embeddingFor(t, i), index: i })),
      }),
    }
  })
  vi.stubGlobal('fetch', fetchMock)
  return { fetchMock, calls }
}

describe('embed', () => {
  it('returns one vector per input string in order', async () => {
    stub((_t, i) => [i, i, i])
    const out = await embed(['a', 'b', 'c'])
    expect(out).toEqual([
      [0, 0, 0],
      [1, 1, 1],
      [2, 2, 2],
    ])
  })

  it('batches inputs by RAG_EMBED_BATCH and preserves global order', async () => {
    process.env.RAG_EMBED_BATCH = '2'
    const { calls } = stub((t) => [t.charCodeAt(0)])
    const out = await embed(['a', 'b', 'c', 'd', 'e'])
    const embedCalls = calls.filter((c) => String(c[0]).includes('/embeddings'))
    expect(embedCalls.length).toBe(3) // 2 + 2 + 1
    expect(out).toEqual([[97], [98], [99], [100], [101]])
  })

  it('returns empty array for empty input without calling fetch', async () => {
    const { fetchMock } = stub(() => [1])
    const out = await embed([])
    expect(out).toEqual([])
    expect(fetchMock).not.toHaveBeenCalled()
  })

  it('throws on non-ok response', async () => {
    __resetTokenCacheForTests()
    vi.stubGlobal(
      'fetch',
      vi.fn(async (url: string) => {
        if (String(url).includes('/oauth')) {
          return { ok: true, status: 200, json: async () => ({ access_token: 't', expires_at: Date.now() + 1e6 }) }
        }
        return { ok: false, status: 429, text: async () => 'rate limited' }
      }),
    )
    await expect(embed(['x'])).rejects.toThrow(/embeddings failed: 429/)
  })
})
```

- [ ] **Step 4: Запустить тест — убедиться, что падает**

Run: `pnpm exec vitest run tests/lib/gigachat/embeddings.test.ts`
Expected: FAIL — `embed` не определён.

- [ ] **Step 5: Реализовать `lib/gigachat/embeddings.ts`**

```ts
import { getGigaConfig } from './config'
import { ensureInsecureTls } from './tls'
import { getAccessToken } from './token'
import type { EmbeddingsResponse } from './types'

function batchSize(): number {
  const n = Number(process.env.RAG_EMBED_BATCH ?? '32')
  return Number.isFinite(n) && n > 0 ? Math.floor(n) : 32
}

async function embedBatch(texts: string[]): Promise<number[][]> {
  const cfg = getGigaConfig()
  ensureInsecureTls(cfg.insecureTls)
  const token = await getAccessToken()

  const res = await fetch(`${cfg.apiBase}/embeddings`, {
    method: 'POST',
    headers: {
      Authorization: `Bearer ${token}`,
      'Content-Type': 'application/json',
      Accept: 'application/json',
    },
    body: JSON.stringify({ model: cfg.embedModel, input: texts }),
  })

  if (!res.ok) {
    const text = typeof res.text === 'function' ? await res.text() : ''
    throw new Error(`GigaChat embeddings failed: ${res.status} ${text}`)
  }

  const data = (await res.json()) as EmbeddingsResponse
  const sorted = [...data.data].sort((a, b) => a.index - b.index)
  return sorted.map((d) => d.embedding)
}

export async function embed(texts: string[]): Promise<number[][]> {
  if (texts.length === 0) return []
  const size = batchSize()
  const out: number[][] = []
  for (let i = 0; i < texts.length; i += size) {
    const batch = texts.slice(i, i + size)
    out.push(...(await embedBatch(batch)))
  }
  return out
}
```

- [ ] **Step 6: Запустить тест — убедиться, что проходит**

Run: `pnpm exec vitest run tests/lib/gigachat/embeddings.test.ts`
Expected: PASS (4 теста).

- [ ] **Step 7: Commit**

```bash
git add lib/gigachat/embeddings.ts lib/gigachat/config.ts lib/gigachat/types.ts tests/lib/gigachat/embeddings.test.ts
git commit -m "feat(gigachat): add embeddings client with batching by RAG_EMBED_BATCH"
```

---

## Task 3: Схема БД — vector customType, RAG-таблицы, scenarios.embedding

**Files:**
- Create: `db/types.ts`
- Modify: `db/schema.ts`
- Create (generate): `db/migrations/0003_*.sql`
- Create (custom): `db/migrations/0004_rag_indexes.sql`

`rag_documents`/`rag_chunks` — **общие** таблицы (не пользовательские), фильтра по user_id нет. `chunk_meta` хранит `{ source, document_title, direction, grade_min, grade_max, section_kind, stage_idx? }` (числовые `grade_min`/`grade_max` нужны для SQL-фильтра по классу из spec §7).

- [ ] **Step 1: Создать `db/types.ts` (customType для vector и tsvector)**

```ts
import { customType } from 'drizzle-orm/pg-core'

// pgvector. Хранится как строка '[v1,v2,...]'. dimensions фиксируем при объявлении.
export const vector = customType<{
  data: number[]
  driverData: string
  config: { dimensions: number }
}>({
  dataType(config) {
    return `vector(${config?.dimensions ?? 1024})`
  },
  toDriver(value: number[]): string {
    return `[${value.join(',')}]`
  },
  fromDriver(value: string): number[] {
    return value
      .slice(1, -1)
      .split(',')
      .filter((s) => s.length > 0)
      .map(Number)
  },
})

// Полнотекстовый вектор PG. Заполняется через to_tsvector в SQL при ingest.
export const tsvector = customType<{ data: string; driverData: string }>({
  dataType() {
    return 'tsvector'
  },
})
```

- [ ] **Step 2: Расширить `db/schema.ts`**

В начало файла добавить импорт:

```ts
import { vector, tsvector } from './types'
```

В таблицу `scenarios` (после `generationMeta`, перед `createdAt`) добавить колонку:

```ts
  embedding: vector('embedding', { dimensions: 1024 }),
```

В конец файла добавить таблицы:

```ts
export const ragDocuments = pgTable('rag_documents', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => crypto.randomUUID()),
  source: text('source').notNull(), // 'seed' | 'razgovor'
  title: text('title').notNull(),
  gradeRange: text('grade_range'), // '5-7', 'SPO' и т.п. (как в URL)
  direction: text('direction'),
  rawUrl: text('raw_url').notNull().unique(), // ключ идемпотентности; для seed = 'seed://<file>'
  createdAt: timestamp('created_at', { mode: 'date' }).notNull().defaultNow(),
})

export const ragChunks = pgTable('rag_chunks', {
  id: text('id')
    .primaryKey()
    .$defaultFn(() => crypto.randomUUID()),
  documentId: text('document_id')
    .notNull()
    .references(() => ragDocuments.id, { onDelete: 'cascade' }),
  chunkText: text('chunk_text').notNull(),
  chunkHash: text('chunk_hash').notNull().unique(), // sha256 для идемпотентности
  chunkMeta: jsonb('chunk_meta')
    .$type<{
      source: string
      document_title: string
      direction: string | null
      grade_min: number
      grade_max: number
      section_kind: string
      stage_idx?: number
    }>()
    .notNull(),
  embedding: vector('embedding', { dimensions: 1024 }).notNull(),
  tsv: tsvector('tsv'),
  createdAt: timestamp('created_at', { mode: 'date' }).notNull().defaultNow(),
})
```

- [ ] **Step 3: Сгенерировать миграцию таблиц/колонок**

Run: `pnpm db:generate`
Expected: создан `db/migrations/0003_*.sql` с `ALTER TABLE scenarios ADD COLUMN embedding vector(1024)`, `CREATE TABLE rag_documents`, `CREATE TABLE rag_chunks` (включая `embedding vector(1024)` и `tsv tsvector`). Прочитать сгенерированный SQL и убедиться, что типы `vector(1024)`/`tsvector` присутствуют.

- [ ] **Step 4: Создать кастомную миграцию для индексов**

Run: `pnpm exec drizzle-kit generate --custom --name rag_indexes`
Expected: создан пустой `db/migrations/0004_rag_indexes.sql` (зарегистрирован в `meta/_journal.json`).

Заполнить его содержимым:

```sql
-- HNSW для cosine-поиска по rag_chunks.embedding (pgvector >=0.5, есть в pgvector/pgvector:pg16)
CREATE INDEX IF NOT EXISTS rag_chunks_embedding_hnsw
  ON rag_chunks USING hnsw (embedding vector_cosine_ops);

-- GIN для полнотекстового ts_rank по tsv
CREATE INDEX IF NOT EXISTS rag_chunks_tsv_gin
  ON rag_chunks USING gin (tsv);

-- Частые фильтры по классу/направлению из chunk_meta
CREATE INDEX IF NOT EXISTS rag_chunks_grade_idx
  ON rag_chunks (((chunk_meta->>'grade_min')::int), ((chunk_meta->>'grade_max')::int));
```

- [ ] **Step 5: Применить миграции на чистой БД и проверить**

Run:
```bash
pnpm db:up && sleep 3 && pnpm db:migrate
```
Expected: «Done.» без ошибок.

- [ ] **Step 6: Smoke-тест схемы**

`tests/smoke/rag-schema.test.ts`:

```ts
import { db } from '@/db'
import { sql } from 'drizzle-orm'
import { describe, expect, it } from 'vitest'

describe('rag schema', () => {
  it('rag_documents and rag_chunks exist', async () => {
    const r = await db.execute(
      sql`SELECT table_name FROM information_schema.tables WHERE table_name IN ('rag_documents','rag_chunks')`,
    )
    expect(r.length).toBe(2)
  })

  it('rag_chunks.embedding is a vector column', async () => {
    const r = await db.execute(
      sql`SELECT udt_name FROM information_schema.columns WHERE table_name='rag_chunks' AND column_name='embedding'`,
    )
    expect((r[0] as { udt_name: string }).udt_name).toBe('vector')
  })

  it('scenarios.embedding column exists', async () => {
    const r = await db.execute(
      sql`SELECT column_name FROM information_schema.columns WHERE table_name='scenarios' AND column_name='embedding'`,
    )
    expect(r.length).toBe(1)
  })
})
```

Run: `pnpm exec vitest run tests/smoke/rag-schema.test.ts`
Expected: PASS (3 теста).

- [ ] **Step 7: Commit**

```bash
git add db/types.ts db/schema.ts db/migrations tests/smoke/rag-schema.test.ts
git commit -m "feat(db): add rag_documents/rag_chunks, scenarios.embedding, vector/tsvector types + indexes"
```

---

## Task 4: Хэш чанка для идемпотентности (`lib/rag/hash.ts`)

**Files:**
- Create: `lib/rag/hash.ts`
- Test: `tests/lib/rag/hash.test.ts`

- [ ] **Step 1: Падающий тест**

`tests/lib/rag/hash.test.ts`:

```ts
import { chunkHash } from '@/lib/rag/hash'
import { describe, expect, it } from 'vitest'

describe('chunkHash', () => {
  it('returns a 64-char hex sha256', () => {
    const h = chunkHash('привет мир')
    expect(h).toMatch(/^[0-9a-f]{64}$/)
  })

  it('is deterministic for identical input', () => {
    expect(chunkHash('один и тот же текст')).toBe(chunkHash('один и тот же текст'))
  })

  it('differs for different input', () => {
    expect(chunkHash('a')).not.toBe(chunkHash('b'))
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/rag/hash.test.ts`
Expected: FAIL — `chunkHash` не определён.

- [ ] **Step 3: Реализовать**

`lib/rag/hash.ts`:

```ts
import { createHash } from 'node:crypto'

export function chunkHash(text: string): string {
  return createHash('sha256').update(text, 'utf8').digest('hex')
}
```

- [ ] **Step 4: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/rag/hash.test.ts`
Expected: PASS (3 теста).

- [ ] **Step 5: Commit**

```bash
git add lib/rag/hash.ts tests/lib/rag/hash.test.ts
git commit -m "feat(rag): add sha256 chunkHash for ingest idempotency"
```

---

## Task 5: Структурный чанкинг (`lib/rag/chunk.ts`)

**Files:**
- Create: `lib/rag/chunk.ts`
- Test: `tests/lib/rag/chunk.test.ts`

Чанкинг по заголовкам (markdown `#`/`##`/`###` или строки-заголовки по ключевым словам «Цель/Ход/Этап/Рефлексия/Материалы»). Длина 300–800 токенов; токены оцениваем эвристикой (локальный токенайзер запрещён): `estimateTokens = ceil(chars/3)`. Слишком большая секция режется по абзацам (пустая строка), слишком маленькая склеивается со следующей. `section_kind`: `goal|stage|reflection|materials|other`. У `stage`-секций — инкрементный `stage_idx`.

- [ ] **Step 1: Падающий тест**

`tests/lib/rag/chunk.test.ts`:

```ts
import { chunkStructured, estimateTokens } from '@/lib/rag/chunk'
import { describe, expect, it } from 'vitest'

const para = (label: string, chars: number) => `${label} ` + 'я'.repeat(chars)

describe('estimateTokens', () => {
  it('approximates ~chars/3', () => {
    expect(estimateTokens('я'.repeat(300))).toBe(100)
  })
})

describe('chunkStructured', () => {
  it('splits by markdown headings and labels section_kind', () => {
    const text = [
      '## Цель',
      para('развить', 1200),
      '## Ход занятия. Этап 1',
      para('вступление', 1200),
      '## Рефлексия',
      para('обсуждение', 1200),
    ].join('\n\n')
    const chunks = chunkStructured(text)
    const kinds = chunks.map((c) => c.sectionKind)
    expect(kinds).toContain('goal')
    expect(kinds).toContain('stage')
    expect(kinds).toContain('reflection')
    const stage = chunks.find((c) => c.sectionKind === 'stage')
    expect(stage?.stageIdx).toBe(1)
  })

  it('keeps every chunk under the 800-token max by splitting large sections', () => {
    const huge = ['## Ход занятия', para('часть', 9000)].join('\n\n')
    const chunks = chunkStructured(huge)
    expect(chunks.length).toBeGreaterThan(1)
    for (const c of chunks) {
      expect(estimateTokens(c.text)).toBeLessThanOrEqual(800)
    }
  })

  it('merges tiny adjacent sections to reach the 300-token min when possible', () => {
    const text = [
      '## Материалы',
      para('бумага', 150),
      '## Заметка',
      para('ещё', 150),
      '## Заметка 2',
      para('и ещё', 900),
    ].join('\n\n')
    const chunks = chunkStructured(text)
    // первые две крошечные секции должны слиться, а не остаться двумя огрызками
    expect(chunks.length).toBeLessThan(3)
  })

  it('detects keyword headings in plain (non-markdown) text', () => {
    const text = ['Цель:', para('a', 1000), 'Рефлексия:', para('b', 1000)].join('\n\n')
    const chunks = chunkStructured(text)
    expect(chunks.map((c) => c.sectionKind)).toEqual(
      expect.arrayContaining(['goal', 'reflection']),
    )
  })

  it('returns no empty chunks', () => {
    const chunks = chunkStructured('## Цель\n\n\n\n## Рефлексия\n\nтекст')
    for (const c of chunks) expect(c.text.trim().length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/rag/chunk.test.ts`
Expected: FAIL — модуль не существует.

- [ ] **Step 3: Реализовать `lib/rag/chunk.ts`**

```ts
export type SectionKind = 'goal' | 'stage' | 'reflection' | 'materials' | 'other'

export type Chunk = {
  text: string
  sectionKind: SectionKind
  stageIdx?: number
}

const MIN_TOKENS = 300
const MAX_TOKENS = 800

export function estimateTokens(text: string): number {
  return Math.ceil(text.length / 3)
}

const MD_HEADING = /^\s{0,3}#{1,6}\s+(.+?)\s*$/
const KEYWORD_HEADING =
  /^\s*(цель|задачи|ход\s+занятия|этап\s*\d*|основная\s+часть|вовлечение|рефлекси\w*|материалы|оборудование)\b.*$/i

type RawSection = { title: string; body: string; kind: SectionKind; stageIdx?: number }

function classify(title: string): SectionKind {
  const t = title.toLowerCase()
  if (/рефлекси/.test(t)) return 'reflection'
  if (/цель|задач/.test(t)) return 'goal'
  if (/материал|оборудован/.test(t)) return 'materials'
  if (/ход\s+занятия|этап|основная\s+часть|вовлечение/.test(t)) return 'stage'
  return 'other'
}

function isHeading(line: string): string | null {
  const md = line.match(MD_HEADING)
  if (md) return md[1].trim()
  if (KEYWORD_HEADING.test(line.trim())) return line.replace(/[:#]+\s*$/, '').trim()
  return null
}

function splitSections(text: string): RawSection[] {
  const lines = text.split('\n')
  const sections: RawSection[] = []
  let curTitle = 'Введение'
  let curBody: string[] = []
  let stageCounter = 0

  const flush = () => {
    const body = curBody.join('\n').trim()
    const kind = classify(curTitle)
    const sec: RawSection = { title: curTitle, body, kind }
    if (kind === 'stage') {
      stageCounter += 1
      sec.stageIdx = stageCounter
    }
    if (body.length > 0 || kind !== 'other') sections.push(sec)
  }

  for (const line of lines) {
    const heading = isHeading(line)
    if (heading) {
      flush()
      curTitle = heading
      curBody = []
    } else {
      curBody.push(line)
    }
  }
  flush()
  return sections.filter((s) => s.body.trim().length > 0)
}

function splitByParagraph(body: string): string[] {
  const paras = body.split(/\n\s*\n/).map((p) => p.trim()).filter(Boolean)
  const out: string[] = []
  let buf = ''
  for (const p of paras) {
    const candidate = buf ? `${buf}\n\n${p}` : p
    if (estimateTokens(candidate) > MAX_TOKENS && buf) {
      out.push(buf)
      buf = p
    } else {
      buf = candidate
    }
    while (estimateTokens(buf) > MAX_TOKENS) {
      // одиночный абзац длиннее лимита — режем по символам
      const maxChars = MAX_TOKENS * 3
      out.push(buf.slice(0, maxChars))
      buf = buf.slice(maxChars)
    }
  }
  if (buf.trim()) out.push(buf)
  return out
}

export function chunkStructured(text: string): Chunk[] {
  const sections = splitSections(text)
  const chunks: Chunk[] = []

  let pending: RawSection | null = null
  const emit = (sec: RawSection) => {
    for (const piece of splitByParagraph(sec.body)) {
      if (piece.trim().length === 0) continue
      chunks.push({ text: piece.trim(), sectionKind: sec.kind, stageIdx: sec.stageIdx })
    }
  }

  for (const sec of sections) {
    if (pending) {
      // склеиваем крошечную предыдущую секцию с текущей
      const merged: RawSection = {
        title: pending.title,
        body: `${pending.body}\n\n${sec.body}`,
        kind: pending.kind,
        stageIdx: pending.stageIdx,
      }
      if (estimateTokens(merged.body) < MIN_TOKENS) {
        pending = merged
      } else {
        emit(merged)
        pending = null
      }
      continue
    }
    if (estimateTokens(sec.body) < MIN_TOKENS) {
      pending = sec
    } else {
      emit(sec)
    }
  }
  if (pending) emit(pending)
  return chunks
}
```

- [ ] **Step 4: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/rag/chunk.test.ts`
Expected: PASS (6 тестов). Если какой-то тест на границах не сходится — править реализацию, НЕ тест (тесты фиксируют контракт 300–800 + слияние крошечных).

- [ ] **Step 5: Commit**

```bash
git add lib/rag/chunk.ts tests/lib/rag/chunk.test.ts
git commit -m "feat(rag): structural chunking 300-800 tokens with section_kind/stage_idx"
```

---

## Task 6: Гибридный скоринг и диверсификация (`lib/rag/score.ts`)

**Files:**
- Create: `lib/rag/score.ts`
- Test: `tests/lib/rag/score.test.ts`

Чистые функции (SQL отдаёт сырые `cosine` и `bm25`): нормализация BM25 в [0,1] (min-max по кандидатам), `combineScore = 0.7·cosine + 0.3·bm25norm`, диверсификация ≤`maxPerDoc` чанков из одного документа, отсортировано по итоговому score, ограничено `topK`.

- [ ] **Step 1: Падающий тест**

`tests/lib/rag/score.test.ts`:

```ts
import { combineScore, diversify, normalizeBm25, rankAndDiversify } from '@/lib/rag/score'
import { describe, expect, it } from 'vitest'

describe('normalizeBm25', () => {
  it('min-max normalizes to [0,1]', () => {
    expect(normalizeBm25([2, 4, 0])).toEqual([0.5, 1, 0])
  })
  it('returns zeros when all equal (avoid div-by-zero)', () => {
    expect(normalizeBm25([3, 3, 3])).toEqual([0, 0, 0])
  })
  it('handles empty', () => {
    expect(normalizeBm25([])).toEqual([])
  })
})

describe('combineScore', () => {
  it('weights 0.7 cosine + 0.3 bm25', () => {
    expect(combineScore(1, 0)).toBeCloseTo(0.7)
    expect(combineScore(0, 1)).toBeCloseTo(0.3)
    expect(combineScore(0.5, 0.5)).toBeCloseTo(0.5)
  })
})

describe('diversify', () => {
  it('limits to maxPerDoc chunks per documentId, preserving order', () => {
    const items = [
      { documentId: 'A', score: 0.9 },
      { documentId: 'A', score: 0.8 },
      { documentId: 'A', score: 0.7 },
      { documentId: 'B', score: 0.6 },
    ]
    const out = diversify(items, 2)
    expect(out.map((x) => x.score)).toEqual([0.9, 0.8, 0.6])
  })
})

describe('rankAndDiversify', () => {
  it('combines, sorts desc, diversifies, and takes topK', () => {
    const candidates = [
      { id: '1', documentId: 'A', cosine: 0.9, bm25: 10 },
      { id: '2', documentId: 'A', cosine: 0.85, bm25: 8 },
      { id: '3', documentId: 'A', cosine: 0.8, bm25: 6 },
      { id: '4', documentId: 'B', cosine: 0.5, bm25: 4 },
      { id: '5', documentId: 'C', cosine: 0.4, bm25: 2 },
    ]
    const out = rankAndDiversify(candidates, { topK: 3, maxPerDoc: 2 })
    expect(out.map((x) => x.id)).toEqual(['1', '2', '4'])
    expect(out[0].score).toBeGreaterThan(out[1].score)
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/rag/score.test.ts`
Expected: FAIL — модуль не существует.

- [ ] **Step 3: Реализовать `lib/rag/score.ts`**

```ts
export function normalizeBm25(values: number[]): number[] {
  if (values.length === 0) return []
  const min = Math.min(...values)
  const max = Math.max(...values)
  if (max === min) return values.map(() => 0)
  return values.map((v) => (v - min) / (max - min))
}

export function combineScore(cosine: number, bm25norm: number): number {
  return 0.7 * cosine + 0.3 * bm25norm
}

export function diversify<T extends { documentId: string }>(items: T[], maxPerDoc: number): T[] {
  const counts = new Map<string, number>()
  const out: T[] = []
  for (const item of items) {
    const n = counts.get(item.documentId) ?? 0
    if (n >= maxPerDoc) continue
    counts.set(item.documentId, n + 1)
    out.push(item)
  }
  return out
}

export type Candidate = { id: string; documentId: string; cosine: number; bm25: number }
export type Scored = Candidate & { score: number }

export function rankAndDiversify(
  candidates: Candidate[],
  opts: { topK: number; maxPerDoc: number },
): Scored[] {
  const bm25norm = normalizeBm25(candidates.map((c) => c.bm25))
  const scored: Scored[] = candidates.map((c, i) => ({
    ...c,
    score: combineScore(c.cosine, bm25norm[i]),
  }))
  scored.sort((a, b) => b.score - a.score)
  return diversify(scored, opts.maxPerDoc).slice(0, opts.topK)
}
```

- [ ] **Step 4: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/rag/score.test.ts`
Expected: PASS (8 тестов).

- [ ] **Step 5: Commit**

```bash
git add lib/rag/score.ts tests/lib/rag/score.test.ts
git commit -m "feat(rag): hybrid score (0.7 cosine + 0.3 bm25) with diversification"
```

---

## Task 7: Ingest-ядро (`lib/rag/ingest.ts`)

**Files:**
- Create: `lib/rag/ingest.ts`
- Test: `tests/lib/rag/ingest.test.ts`

`ingestDocument` принимает метаданные документа + полный текст + инъектируемые зависимости (`embed`, `db`). Чанкает, считает хэши, **пропускает уже существующие чанки** (идемпотентность по `chunk_hash`), эмбеддит только новые, апсертит документ по `rawUrl`, вставляет чанки с `chunk_meta` и `tsv = to_tsvector(:lang, text)`. Возвращает `{ inserted, skipped }`. Юнит-тест мокает `embed` и использует фейковый `db` (записывает вызовы) — без реальной БД, чтобы тест был быстрым; реальная вставка проверяется в Task 8 (seed) через smoke.

- [ ] **Step 1: Падающий тест**

`tests/lib/rag/ingest.test.ts`:

```ts
import { ingestDocument } from '@/lib/rag/ingest'
import { describe, expect, it, vi } from 'vitest'

function fakeDeps(existingHashes: string[] = []) {
  const insertedChunks: Array<{ chunkHash: string }> = []
  const insertedDocs: Array<{ rawUrl: string }> = []
  const embed = vi.fn(async (texts: string[]) => texts.map(() => Array(1024).fill(0.1)))

  const db = {
    existingHashesFor: vi.fn(async () => new Set(existingHashes)),
    upsertDocument: vi.fn(async (doc: { rawUrl: string }) => {
      insertedDocs.push(doc)
      return 'doc-id'
    }),
    insertChunk: vi.fn(async (chunk: { chunkHash: string }) => {
      insertedChunks.push(chunk)
    }),
  }
  return { embed, db, insertedChunks, insertedDocs }
}

const longText = [
  '## Цель',
  'я'.repeat(1200),
  '## Ход занятия. Этап 1',
  'е'.repeat(1200),
  '## Рефлексия',
  'о'.repeat(1200),
].join('\n\n')

describe('ingestDocument', () => {
  it('chunks, embeds only new chunks, and inserts them with doc metadata', async () => {
    const { embed, db, insertedChunks } = fakeDeps()
    const res = await ingestDocument(
      {
        source: 'seed',
        title: 'Тест',
        direction: 'Гражданское',
        gradeRange: '5-7',
        gradeMin: 5,
        gradeMax: 7,
        rawUrl: 'seed://test.md',
        text: longText,
        lang: 'russian',
      },
      { embed, db },
    )
    expect(res.inserted).toBeGreaterThan(0)
    expect(res.skipped).toBe(0)
    expect(embed).toHaveBeenCalledTimes(1)
    expect(insertedChunks[0].chunkHash).toMatch(/^[0-9a-f]{64}$/)
  })

  it('skips chunks whose hash already exists (idempotent)', async () => {
    const { embed: e1, db: db1 } = fakeDeps()
    await ingestDocument(
      { source: 'seed', title: 'Т', direction: null, gradeRange: '5-7', gradeMin: 5, gradeMax: 7, rawUrl: 'seed://t.md', text: longText, lang: 'russian' },
      { embed: e1, db: db1 },
    )
    const allHashes = db1.insertChunk.mock.calls.map((c) => c[0].chunkHash)

    const { embed: e2, db: db2 } = fakeDeps(allHashes)
    const res = await ingestDocument(
      { source: 'seed', title: 'Т', direction: null, gradeRange: '5-7', gradeMin: 5, gradeMax: 7, rawUrl: 'seed://t.md', text: longText, lang: 'russian' },
      { embed: e2, db: db2 },
    )
    expect(res.inserted).toBe(0)
    expect(res.skipped).toBe(allHashes.length)
    expect(e2).not.toHaveBeenCalled() // нечего эмбеддить
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/rag/ingest.test.ts`
Expected: FAIL — модуль не существует.

- [ ] **Step 3: Реализовать `lib/rag/ingest.ts`**

```ts
import { chunkStructured } from './chunk'
import { chunkHash } from './hash'

export type IngestDoc = {
  source: string
  title: string
  direction: string | null
  gradeRange: string | null
  gradeMin: number
  gradeMax: number
  rawUrl: string
  text: string
  lang: string
}

export type IngestDb = {
  existingHashesFor(hashes: string[]): Promise<Set<string>>
  upsertDocument(doc: {
    source: string
    title: string
    direction: string | null
    gradeRange: string | null
    rawUrl: string
  }): Promise<string>
  insertChunk(chunk: {
    documentId: string
    chunkText: string
    chunkHash: string
    chunkMeta: Record<string, unknown>
    embedding: number[]
    lang: string
  }): Promise<void>
}

export type IngestDeps = {
  embed: (texts: string[]) => Promise<number[][]>
  db: IngestDb
}

export async function ingestDocument(
  doc: IngestDoc,
  deps: IngestDeps,
): Promise<{ inserted: number; skipped: number }> {
  const chunks = chunkStructured(doc.text)
  if (chunks.length === 0) return { inserted: 0, skipped: 0 }

  const withHash = chunks.map((c) => ({ ...c, hash: chunkHash(c.text) }))
  // дедуп внутри документа (одинаковый текст в двух секциях)
  const seen = new Set<string>()
  const unique = withHash.filter((c) => (seen.has(c.hash) ? false : (seen.add(c.hash), true)))

  const existing = await deps.db.existingHashesFor(unique.map((c) => c.hash))
  const toInsert = unique.filter((c) => !existing.has(c.hash))
  const skipped = unique.length - toInsert.length

  if (toInsert.length === 0) return { inserted: 0, skipped }

  const documentId = await deps.db.upsertDocument({
    source: doc.source,
    title: doc.title,
    direction: doc.direction,
    gradeRange: doc.gradeRange,
    rawUrl: doc.rawUrl,
  })

  const embeddings = await deps.embed(toInsert.map((c) => c.text))

  for (let i = 0; i < toInsert.length; i++) {
    const c = toInsert[i]
    await deps.db.insertChunk({
      documentId,
      chunkText: c.text,
      chunkHash: c.hash,
      chunkMeta: {
        source: doc.source,
        document_title: doc.title,
        direction: doc.direction,
        grade_min: doc.gradeMin,
        grade_max: doc.gradeMax,
        section_kind: c.sectionKind,
        ...(c.stageIdx !== undefined ? { stage_idx: c.stageIdx } : {}),
      },
      embedding: embeddings[i],
      lang: doc.lang,
    })
  }

  return { inserted: toInsert.length, skipped }
}
```

- [ ] **Step 4: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/rag/ingest.test.ts`
Expected: PASS (2 теста).

- [ ] **Step 5: Реализовать боевую реализацию `IngestDb` (`lib/rag/ingest-db.ts`)**

Это адаптер к Drizzle (используется скриптами; не покрываем юнит-тестом — проверяется smoke в Task 8). `tsv` пишем через `to_tsvector(lang, text)` сырым SQL; `embedding` через customType (массив).

`lib/rag/ingest-db.ts`:

```ts
import { db } from '@/db'
import { ragChunks, ragDocuments } from '@/db/schema'
import { inArray, sql } from 'drizzle-orm'
import type { IngestDb } from './ingest'

export const drizzleIngestDb: IngestDb = {
  async existingHashesFor(hashes) {
    if (hashes.length === 0) return new Set()
    const rows = await db
      .select({ chunkHash: ragChunks.chunkHash })
      .from(ragChunks)
      .where(inArray(ragChunks.chunkHash, hashes))
    return new Set(rows.map((r) => r.chunkHash))
  },

  async upsertDocument(doc) {
    const [row] = await db
      .insert(ragDocuments)
      .values({
        source: doc.source,
        title: doc.title,
        direction: doc.direction,
        gradeRange: doc.gradeRange,
        rawUrl: doc.rawUrl,
      })
      .onConflictDoUpdate({
        target: ragDocuments.rawUrl,
        set: { title: doc.title, direction: doc.direction, gradeRange: doc.gradeRange },
      })
      .returning({ id: ragDocuments.id })
    return row.id
  },

  async insertChunk(chunk) {
    const vec = `[${chunk.embedding.join(',')}]`
    await db.execute(sql`
      INSERT INTO rag_chunks (id, document_id, chunk_text, chunk_hash, chunk_meta, embedding, tsv)
      VALUES (
        ${crypto.randomUUID()},
        ${chunk.documentId},
        ${chunk.chunkText},
        ${chunk.chunkHash},
        ${JSON.stringify(chunk.chunkMeta)}::jsonb,
        ${vec}::vector,
        to_tsvector(${chunk.lang}::regconfig, ${chunk.chunkText})
      )
      ON CONFLICT (chunk_hash) DO NOTHING
    `)
  },
}
```

- [ ] **Step 6: Lint + commit**

Run: `pnpm lint`
Expected: чисто.

```bash
git add lib/rag/ingest.ts lib/rag/ingest-db.ts tests/lib/rag/ingest.test.ts
git commit -m "feat(rag): idempotent ingest core + drizzle ingest adapter (tsv via to_tsvector)"
```

---

## Task 8: Seed-корпус — генерация через GigaChat + ingest

**Files:**
- Create: `lib/scenario/to-markdown.ts` (сериализатор `ScenarioContent` → markdown)
- Test: `tests/lib/scenario/to-markdown.test.ts`
- Create: `scripts/gen-seed.ts` (генерация 12 эталонов через GigaChat — **ручной шаг**)
- Create: `scripts/ingest-seed.ts` (ingest файлов в БД — **ручной шаг**)
- Create: `content/seed-scenarios/*.md` (артефакт ручного прогона `gen-seed`, коммитится в репо)
- Test: `tests/smoke/rag-ingest-seed.test.ts` (guarded, opt-in)

**Решения пользователя по этой задаче:**
- Тела сценариев НЕ пишем руками — генерируем существующим `generateScenario` (с **отключённым** retrieval, чтобы не было циклической зависимости от ещё не загруженного seed) и сериализуем в markdown с frontmatter. Это «родной» продукту контент.
- `gen-seed` и `ingest-seed` — **ручные сетевые шаги** (тратят токены GigaChat). В CI (`pnpm test`) НЕ участвуют; smoke на данные — guarded `skipIf(!process.env.RAG_LIVE)`. Реализуем код и юнит-тест сериализатора; реальный прогон — отдельно, документируется.

`content/seed-scenarios/*.md` — артефакт: после ручного прогона `gen-seed` готовые 12 файлов коммитятся, чтобы `ingest-seed` был воспроизводим без повторной генерации.

- [ ] **Step 1: Падающий тест сериализатора**

`tests/lib/scenario/to-markdown.test.ts`:

```ts
import { scenarioToMarkdown } from '@/lib/scenario/to-markdown'
import { describe, expect, it } from 'vitest'

const content = {
  title: 'Дружба в классе',
  goals: ['сформировать представление о дружбе', 'развить навык договариваться'],
  materials: ['карточки', 'ватман'],
  stages: [
    {
      kind: 'engage' as const,
      title: 'Вовлечение',
      duration_min: 5,
      activities: [{ type: 'discussion' as const, text: 'Вспомните, как помог друг.', questions: ['Что ты почувствовал?'] }],
    },
    {
      kind: 'reflection' as const,
      title: 'Рефлексия',
      duration_min: 5,
      activities: [{ type: 'discussion' as const, text: 'Закончите фразу «друг — это...».' }],
    },
  ],
  adaptations: { simpler: 'упростить вопросы', harder: 'добавить дебаты' },
}

const meta = { title: 'Дружба в классе', direction: 'Духовно-нравственное', gradeRange: '1-2', gradeMin: 1, gradeMax: 2 }

describe('scenarioToMarkdown', () => {
  it('emits YAML frontmatter with required keys', () => {
    const md = scenarioToMarkdown(content, meta)
    expect(md).toMatch(/^---\n/)
    expect(md).toContain('title: Дружба в классе')
    expect(md).toContain('direction: Духовно-нравственное')
    expect(md).toContain('grade_range: 1-2')
    expect(md).toContain('grade_min: 1')
    expect(md).toContain('grade_max: 2')
  })

  it('renders Цель / structural stage headings / Рефлексия / Материалы', () => {
    const md = scenarioToMarkdown(content, meta)
    expect(md).toContain('## Цель')
    expect(md).toContain('## Ход занятия. Этап 1. Вовлечение')
    expect(md).toContain('## Рефлексия')
    expect(md).toContain('## Материалы')
  })

  it('includes activity text and questions', () => {
    const md = scenarioToMarkdown(content, meta)
    expect(md).toContain('Вспомните, как помог друг.')
    expect(md).toContain('Что ты почувствовал?')
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/scenario/to-markdown.test.ts`
Expected: FAIL — модуль не существует.

- [ ] **Step 3: Реализовать `lib/scenario/to-markdown.ts`**

```ts
import type { ScenarioContent } from './schema'

export type SeedMeta = {
  title: string
  direction: string
  gradeRange: string
  gradeMin: number
  gradeMax: number
}

export function scenarioToMarkdown(content: ScenarioContent, meta: SeedMeta): string {
  const fm = [
    '---',
    `title: ${meta.title}`,
    `direction: ${meta.direction}`,
    `grade_range: ${meta.gradeRange}`,
    `grade_min: ${meta.gradeMin}`,
    `grade_max: ${meta.gradeMax}`,
    '---',
    '',
  ]

  const body: string[] = []
  body.push('## Цель', '', content.goals.map((g) => `- ${g}`).join('\n'), '')

  content.stages.forEach((stage, i) => {
    const isReflection = stage.kind === 'reflection'
    const heading = isReflection ? '## Рефлексия' : `## Ход занятия. Этап ${i + 1}. ${stage.title}`
    body.push(heading, '')
    for (const act of stage.activities) {
      body.push(act.text)
      if (act.questions?.length) {
        body.push('', `Вопросы: ${act.questions.join(' ')}`)
      }
      body.push('')
    }
  })

  body.push('## Материалы', '', content.materials.map((m) => `- ${m}`).join('\n'), '')
  body.push('## Адаптация', '', `Проще: ${content.adaptations.simpler}`, '', `Сложнее: ${content.adaptations.harder}`, '')

  return fm.join('\n') + body.join('\n')
}
```

- [ ] **Step 4: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/scenario/to-markdown.test.ts`
Expected: PASS (3 теста).

- [ ] **Step 5: Написать `scripts/gen-seed.ts` (ручной сетевой шаг)**

Матрица 12 контекстов (направление строго из `DIRECTIONS`):

```ts
import { config } from 'dotenv'
config({ path: '.env.local' })
config()

import { mkdir, writeFile } from 'node:fs/promises'
import { join } from 'node:path'
import { generateScenario } from '@/lib/scenario/generate'
import type { GenerationInput } from '@/lib/scenario/schema'
import { scenarioToMarkdown } from '@/lib/scenario/to-markdown'

const DIR = join(process.cwd(), 'content', 'seed-scenarios')

const MATRIX: Array<{
  file: string
  input: GenerationInput
  gradeRange: string
  gradeMin: number
  gradeMax: number
}> = [
  { file: '01-druzhba-1-2.md', gradeRange: '1-2', gradeMin: 1, gradeMax: 2, input: { direction: 'Духовно-нравственное', grade: 2, topic: 'Дружба и взаимопомощь в классе', durationMin: 30, format: 'классный час' } },
  { file: '02-rodina-1-2.md', gradeRange: '1-2', gradeMin: 1, gradeMax: 2, input: { direction: 'Патриотическое', grade: 2, topic: 'Моя малая Родина', durationMin: 30, format: 'беседа' } },
  { file: '03-priroda-3-4.md', gradeRange: '3-4', gradeMin: 3, gradeMax: 4, input: { direction: 'Экологическое', grade: 4, topic: 'Береги природу родного края', durationMin: 30, format: 'классный час' } },
  { file: '04-trud-3-4.md', gradeRange: '3-4', gradeMin: 3, gradeMax: 4, input: { direction: 'Трудовое', grade: 4, topic: 'Все профессии важны', durationMin: 30, format: 'игра' } },
  { file: '05-zdorovie-3-4.md', gradeRange: '3-4', gradeMin: 3, gradeMax: 4, input: { direction: 'Физическое и здоровье', grade: 4, topic: 'Режим дня и здоровье', durationMin: 30, format: 'квиз' } },
  { file: '06-grazhdanin-5-7.md', gradeRange: '5-7', gradeMin: 5, gradeMax: 7, input: { direction: 'Гражданское', grade: 6, topic: 'Права и обязанности школьника', durationMin: 45, format: 'классный час' } },
  { file: '07-nauka-5-7.md', gradeRange: '5-7', gradeMin: 5, gradeMax: 7, input: { direction: 'Познавательное', grade: 6, topic: 'Наука вокруг нас', durationMin: 45, format: 'квиз' } },
  { file: '08-iskusstvo-5-7.md', gradeRange: '5-7', gradeMin: 5, gradeMax: 7, input: { direction: 'Эстетическое', grade: 6, topic: 'Искусство в нашей жизни', durationMin: 45, format: 'мастерская' } },
  { file: '09-pamyat-8-9.md', gradeRange: '8-9', gradeMin: 8, gradeMax: 9, input: { direction: 'Патриотическое', grade: 9, topic: 'Память о Великой Отечественной войне', durationMin: 45, format: 'классный час' } },
  { file: '10-vybor-professii-8-9.md', gradeRange: '8-9', gradeMin: 8, gradeMax: 9, input: { direction: 'Трудовое', grade: 9, topic: 'Как выбрать профессию', durationMin: 45, format: 'беседа' } },
  { file: '11-cennosti-10-11.md', gradeRange: '10-11', gradeMin: 10, gradeMax: 11, input: { direction: 'Духовно-нравственное', grade: 11, topic: 'Нравственный выбор и ответственность', durationMin: 45, format: 'беседа' } },
  { file: '12-volonterstvo-10-11.md', gradeRange: '10-11', gradeMin: 10, gradeMax: 11, input: { direction: 'Гражданское', grade: 11, topic: 'Волонтёрство и помощь другим', durationMin: 45, format: 'классный час' } },
]

async function main() {
  await mkdir(DIR, { recursive: true })
  for (const item of MATRIX) {
    // retrieve отключён: seed-генерация не должна зависеть от ещё не загруженного корпуса
    const { content } = await generateScenario(item.input, { retrieve: async () => [] })
    const md = scenarioToMarkdown(content, {
      title: content.title,
      direction: item.input.direction,
      gradeRange: item.gradeRange,
      gradeMin: item.gradeMin,
      gradeMax: item.gradeMax,
    })
    await writeFile(join(DIR, item.file), md, 'utf8')
    console.log(`written ${item.file} (${content.title})`)
  }
  console.log('\nГотово. Проверь файлы глазами перед коммитом и ingest.')
  process.exit(0)
}

main().catch((e) => {
  console.error(e)
  process.exit(1)
})
```

(Зависит от Task 10: `generateScenario` принимает `deps.retrieve`. Если Task 10 ещё не выполнен на момент запуска — выполнить Task 10 раньше или временно вызвать без retrieve. В рамках subagent-driven порядок задач линейный, к Task 8 кода генерации ещё нет RAG — поэтому **этот скрипт пишем, но реальный прогон делаем после Task 10**; код скрипта от этого не меняется.)

- [ ] **Step 6: Написать `scripts/ingest-seed.ts`**

```ts
import { config } from 'dotenv'
config({ path: '.env.local' })
config()

import { readFile, readdir } from 'node:fs/promises'
import { join } from 'node:path'
import matter from 'gray-matter'
import { embed } from '@/lib/gigachat/embeddings'
import { ingestDocument } from '@/lib/rag/ingest'
import { drizzleIngestDb } from '@/lib/rag/ingest-db'

const DIR = join(process.cwd(), 'content', 'seed-scenarios')
const LANG = process.env.PG_TSV_LANG ?? 'russian'

async function main() {
  const files = (await readdir(DIR)).filter((f) => f.endsWith('.md'))
  let totalInserted = 0
  let totalSkipped = 0

  for (const file of files) {
    const raw = await readFile(join(DIR, file), 'utf8')
    const { data, content } = matter(raw)
    const res = await ingestDocument(
      {
        source: 'seed',
        title: String(data.title ?? file),
        direction: data.direction ? String(data.direction) : null,
        gradeRange: data.grade_range ? String(data.grade_range) : null,
        gradeMin: Number(data.grade_min ?? 1),
        gradeMax: Number(data.grade_max ?? 11),
        rawUrl: `seed://${file}`,
        text: content,
        lang: LANG,
      },
      { embed, db: drizzleIngestDb },
    )
    totalInserted += res.inserted
    totalSkipped += res.skipped
    console.log(`${file}: +${res.inserted} chunks, skipped ${res.skipped}`)
  }

  console.log(`\nИтого: вставлено ${totalInserted}, пропущено ${totalSkipped}.`)
  process.exit(0)
}

main().catch((e) => {
  console.error(e)
  process.exit(1)
})
```

- [ ] **Step 7: Guarded smoke-тест данных (opt-in, не в обычном `pnpm test`)**

`tests/smoke/rag-ingest-seed.test.ts`:

```ts
import { db } from '@/db'
import { sql } from 'drizzle-orm'
import { describe, expect, it } from 'vitest'

// Требует ручного прогона gen-seed + ingest-seed. Включается флагом RAG_LIVE=1.
const live = process.env.RAG_LIVE === '1'

describe.skipIf(!live)('seed corpus ingested (live)', () => {
  it('has seed documents', async () => {
    const r = await db.execute(sql`SELECT count(*)::int AS n FROM rag_documents WHERE source='seed'`)
    expect((r[0] as { n: number }).n).toBeGreaterThanOrEqual(12)
  })
  it('has chunks with non-null embedding and tsv', async () => {
    const r = await db.execute(
      sql`SELECT count(*)::int AS n FROM rag_chunks WHERE embedding IS NOT NULL AND tsv IS NOT NULL`,
    )
    expect((r[0] as { n: number }).n).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 8: Verify (без сети) + commit кода**

Run: `pnpm exec vitest run tests/lib/scenario/to-markdown.test.ts && pnpm lint && pnpm exec tsc --noEmit`
Expected: сериализатор зелёный, lint/типы ок. (Smoke `rag-ingest-seed` скипается — это нормально.)

```bash
git add lib/scenario/to-markdown.ts scripts/gen-seed.ts scripts/ingest-seed.ts tests/lib/scenario/to-markdown.test.ts tests/smoke/rag-ingest-seed.test.ts
git commit -m "feat(rag): scenario->markdown serializer + gen-seed/ingest-seed scripts (manual network)"
```

- [ ] **Step 9 (РУЧНОЙ, после Task 10 — НЕ в CI): прогнать генерацию и ingest seed**

> Выполняется человеком/отдельным запуском с ключами GigaChat и поднятой БД. НЕ блокирует зелёные гейты фазы.

```bash
pnpm db:up && pnpm db:migrate && pnpm exec tsx scripts/gen-seed.ts
# просмотреть content/seed-scenarios/*.md глазами (русский, без реальных имён, структура секций)
git add content/seed-scenarios && git commit -m "content(rag): generated seed corpus (12 scenarios)"
pnpm ingest:seed                       # вставляет чанки + эмбеддинги
pnpm ingest:seed                       # повтор → всё skipped (идемпотентность)
RAG_LIVE=1 pnpm exec vitest run tests/smoke/rag-ingest-seed.test.ts   # должен пройти
```
Expected: 12 md-файлов, ingest вставляет >0 чанков, повтор skipped, guarded smoke зелёный.

---

## Task 9: Гибридный retrieval (`lib/rag/retrieve.ts`)

**Files:**
- Create: `lib/rag/retrieve.ts`
- Test: `tests/lib/rag/retrieve.test.ts` (юнит, инъекция db+embed), `tests/smoke/rag-retrieve.test.ts` (живой, опц.)

`retrieveChunks` эмбеддит запрос (`direction + topic`), берёт кандидатов из БД одним SQL (cosine + ts_rank, фильтр по классу и направлению), отдаёт их в `rankAndDiversify`. **Fallback:** если с фильтром по направлению кандидатов < `topK` — повторяем без фильтра направления (spec §5 «Пустой RAG → seed»).

- [ ] **Step 1: Падающий юнит-тест (инъекция зависимостей)**

`tests/lib/rag/retrieve.test.ts`:

```ts
import { retrieveChunks } from '@/lib/rag/retrieve'
import { describe, expect, it, vi } from 'vitest'

const row = (id: string, documentId: string, cosine: number, bm25: number) => ({
  id,
  documentId,
  chunkText: `text-${id}`,
  chunkMeta: { document_title: 'Doc', section_kind: 'stage' },
  cosine,
  bm25,
})

describe('retrieveChunks', () => {
  it('embeds the query once and returns topK diversified chunks', async () => {
    const embed = vi.fn(async () => [[0.1]])
    const queryCandidates = vi.fn(async () => [
      row('1', 'A', 0.9, 5),
      row('2', 'A', 0.8, 4),
      row('3', 'A', 0.7, 3),
      row('4', 'B', 0.6, 2),
    ])
    const out = await retrieveChunks(
      { direction: 'Гражданское', grade: 6, topic: 'дружба', lang: 'russian' },
      { embed, queryCandidates, topK: 3, maxPerDoc: 2, candidates: 24 },
    )
    expect(embed).toHaveBeenCalledTimes(1)
    expect(out.map((c) => c.id)).toEqual(['1', '2', '4']) // ≤2 из A
    expect(out[0].chunkText).toBe('text-1')
  })

  it('falls back to no-direction query when filtered result is smaller than topK', async () => {
    const embed = vi.fn(async () => [[0.1]])
    const queryCandidates = vi.fn(async (args: { direction: string | null }) => {
      if (args.direction) return [row('1', 'A', 0.9, 5)] // мало
      return [row('1', 'A', 0.9, 5), row('2', 'B', 0.8, 4), row('3', 'C', 0.7, 3)]
    })
    const out = await retrieveChunks(
      { direction: 'Гражданское', grade: 6, topic: 'дружба', lang: 'russian' },
      { embed, queryCandidates, topK: 3, maxPerDoc: 2, candidates: 24 },
    )
    expect(queryCandidates).toHaveBeenCalledTimes(2)
    expect(out.length).toBe(3)
  })
})
```

- [ ] **Step 2: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/rag/retrieve.test.ts`
Expected: FAIL — модуль не существует.

- [ ] **Step 3: Реализовать `lib/rag/retrieve.ts`**

```ts
import { db } from '@/db'
import { sql } from 'drizzle-orm'
import { embed as gigaEmbed } from '@/lib/gigachat/embeddings'
import { type Candidate, rankAndDiversify } from './score'

export type RetrieveQuery = {
  direction: string | null
  grade: number
  topic: string
  lang?: string
}

export type RetrievedChunk = {
  id: string
  documentId: string
  chunkText: string
  documentTitle: string
  sectionKind: string
  score: number
}

type CandidateRow = Candidate & {
  chunkText: string
  chunkMeta: { document_title?: string; section_kind?: string }
}

type QueryArgs = {
  qvec: number[]
  grade: number
  topic: string
  lang: string
  direction: string | null
  limit: number
}

export type RetrieveDeps = {
  embed: (texts: string[]) => Promise<number[][]>
  queryCandidates: (args: QueryArgs) => Promise<CandidateRow[]>
  topK: number
  maxPerDoc: number
  candidates: number
}

// Боевой запрос: cosine (1 - <=>) + ts_rank; фильтр по классу и опц. направлению.
async function queryCandidatesLive(args: QueryArgs): Promise<CandidateRow[]> {
  const vec = `[${args.qvec.join(',')}]`
  const dirFilter = args.direction
    ? sql`AND chunk_meta->>'direction' = ${args.direction}`
    : sql``
  const rows = await db.execute(sql`
    SELECT
      id,
      document_id AS "documentId",
      chunk_text AS "chunkText",
      chunk_meta AS "chunkMeta",
      (1 - (embedding <=> ${vec}::vector)) AS cosine,
      ts_rank(tsv, plainto_tsquery(${args.lang}::regconfig, ${args.topic})) AS bm25
    FROM rag_chunks
    WHERE (chunk_meta->>'grade_min')::int <= ${args.grade}
      AND (chunk_meta->>'grade_max')::int >= ${args.grade}
      ${dirFilter}
    ORDER BY (embedding <=> ${vec}::vector) ASC
    LIMIT ${args.limit}
  `)
  return rows as unknown as CandidateRow[]
}

function defaults(): Pick<RetrieveDeps, 'embed' | 'queryCandidates' | 'topK' | 'maxPerDoc' | 'candidates'> {
  return {
    embed: gigaEmbed,
    queryCandidates: queryCandidatesLive,
    topK: Number(process.env.RAG_TOP_K ?? '3'),
    maxPerDoc: Number(process.env.RAG_MAX_PER_DOC ?? '2'),
    candidates: Number(process.env.RAG_CANDIDATES ?? '24'),
  }
}

export async function retrieveChunks(
  query: RetrieveQuery,
  deps: Partial<RetrieveDeps> = {},
): Promise<RetrievedChunk[]> {
  const d = { ...defaults(), ...deps }
  const lang = query.lang ?? process.env.PG_TSV_LANG ?? 'russian'
  const [qvec] = await d.embed([`${query.direction ?? ''} ${query.topic}`.trim()])

  const base: Omit<QueryArgs, 'direction'> = {
    qvec,
    grade: query.grade,
    topic: query.topic,
    lang,
    limit: d.candidates,
  }

  let rows = await d.queryCandidates({ ...base, direction: query.direction })
  if (rows.length < d.topK && query.direction) {
    rows = await d.queryCandidates({ ...base, direction: null })
  }

  const ranked = rankAndDiversify(
    rows.map((r) => ({ id: r.id, documentId: r.documentId, cosine: Number(r.cosine), bm25: Number(r.bm25) })),
    { topK: d.topK, maxPerDoc: d.maxPerDoc },
  )

  const byId = new Map(rows.map((r) => [r.id, r]))
  return ranked.map((r) => {
    const src = byId.get(r.id) as CandidateRow
    return {
      id: r.id,
      documentId: r.documentId,
      chunkText: src.chunkText,
      documentTitle: src.chunkMeta?.document_title ?? '',
      sectionKind: src.chunkMeta?.section_kind ?? 'other',
      score: r.score,
    }
  })
}
```

- [ ] **Step 4: Запустить юнит-тест — PASS**

Run: `pnpm exec vitest run tests/lib/rag/retrieve.test.ts`
Expected: PASS (2 теста).

- [ ] **Step 5: Живой smoke-тест retrieval (опционально, требует seed из Task 8)**

`tests/smoke/rag-retrieve.test.ts`:

```ts
import { retrieveChunks } from '@/lib/rag/retrieve'
import { describe, expect, it } from 'vitest'

// Требует загруженного seed + ключей GigaChat. Включается флагом RAG_LIVE=1, иначе скип (CI остаётся зелёным).
const live = process.env.RAG_LIVE === '1'

describe.skipIf(!live)('live retrieval over seed corpus', () => {
  it('returns up to RAG_TOP_K chunks for a seeded topic', async () => {
    const out = await retrieveChunks({
      direction: 'Гражданское',
      grade: 6,
      topic: 'волонтёрство и помощь другим',
      lang: process.env.PG_TSV_LANG ?? 'russian',
    })
    expect(out.length).toBeGreaterThan(0)
    expect(out.length).toBeLessThanOrEqual(Number(process.env.RAG_TOP_K ?? '3'))
    expect(out[0].chunkText.length).toBeGreaterThan(0)
  }, 30000)
})
```

Run (только при наличии данных/ключей): `RAG_LIVE=1 pnpm exec vitest run tests/smoke/rag-retrieve.test.ts`
Expected: PASS. Без `RAG_LIVE=1` файл скипается — обычный `pnpm test` остаётся зелёным без сети. Реальный прогон — ручной шаг после ingest seed (Task 8 Step 9).

- [ ] **Step 6: Commit**

```bash
git add lib/rag/retrieve.ts tests/lib/rag/retrieve.test.ts tests/smoke/rag-retrieve.test.ts
git commit -m "feat(rag): hybrid retrieval with grade/direction filter, fallback, diversification"
```

---

## Task 10: Интеграция retrieval в промпт генерации

**Files:**
- Modify: `lib/scenario/prompt.ts`
- Modify: `lib/scenario/schema.ts` (`GenerationMeta` + `usedChunkIds`)
- Modify: `lib/scenario/generate.ts`
- Modify: `app/app/new/actions.ts` (populate `scenarios.embedding`)
- Test: `tests/lib/scenario/prompt.test.ts` (расширить), `tests/lib/scenario/generate.test.ts` (расширить)

Промпт получает топ-3 чанка методичек блоком `[RELEVANT_METHODOLOGY]`. `generate.ts` вызывает retrieval перед `buildMessages`, кладёт `usedChunkIds` в meta. Retrieval инъектируется в `deps`, чтобы тесты не ходили в сеть. Сбой retrieval **не валит генерацию** (пустой контекст — допустимый fallback).

- [ ] **Step 1: Расширить `GenerationMeta` в `lib/scenario/schema.ts`**

В тип `GenerationMeta` добавить поле:

```ts
  usedChunkIds: string[]
```

- [ ] **Step 2: Обновить тест промпта**

В `tests/lib/scenario/prompt.test.ts` добавить:

```ts
import { buildMessages } from '@/lib/scenario/prompt'

it('includes RELEVANT_METHODOLOGY block when rag chunks provided', () => {
  const msgs = buildMessages(
    { direction: 'Гражданское', grade: 6, topic: 'дружба', durationMin: 30, format: 'классный час' },
    [
      { text: 'Методический фрагмент про дружбу.', documentTitle: 'Методичка', sectionKind: 'stage' },
    ],
  )
  const user = msgs.find((m) => m.role === 'user')?.content ?? ''
  expect(user).toContain('RELEVANT_METHODOLOGY')
  expect(user).toContain('Методический фрагмент про дружбу.')
})

it('omits methodology block when no chunks provided', () => {
  const msgs = buildMessages({
    direction: 'Гражданское', grade: 6, topic: 'дружба', durationMin: 30, format: 'классный час',
  })
  const user = msgs.find((m) => m.role === 'user')?.content ?? ''
  expect(user).not.toContain('RELEVANT_METHODOLOGY')
})
```

- [ ] **Step 3: Запустить — FAIL**

Run: `pnpm exec vitest run tests/lib/scenario/prompt.test.ts`
Expected: FAIL — `buildMessages` пока принимает 1 аргумент.

- [ ] **Step 4: Обновить `lib/scenario/prompt.ts`**

Добавить тип и второй параметр (НЕ менять `PROMPT_VERSION` обновить на rag-версию):

```ts
export const PROMPT_VERSION = 'v1-rag-2026-05-20'

export type RagChunkForPrompt = { text: string; documentTitle: string; sectionKind: string }
```

В `buildMessages` поменять сигнатуру и собрать блок:

```ts
export function buildMessages(
  input: GenerationInput,
  ragChunks: RagChunkForPrompt[] = [],
): ChatMessage[] {
  // ... system без изменений ...

  const methodology =
    ragChunks.length > 0
      ? [
          '',
          '[RELEVANT_METHODOLOGY] (опирайся на эти фрагменты методичек, но не копируй дословно):',
          ...ragChunks.map((c, i) => `(${i + 1}) [${c.documentTitle}] ${c.text}`),
        ]
      : []

  const user = [
    'Сгенерируй сценарий внеурочного занятия со следующими параметрами:',
    `- Направление воспитания: ${input.direction}`,
    `- Класс: ${input.grade}`,
    `- Тема: ${input.topic}`,
    `- Длительность: ${input.durationMin} минут`,
    `- Формат: ${input.format}`,
    ...methodology,
  ].join('\n')

  return [
    { role: 'system', content: system },
    { role: 'user', content: user },
  ]
}
```

- [ ] **Step 5: Запустить тест промпта — PASS**

Run: `pnpm exec vitest run tests/lib/scenario/prompt.test.ts`
Expected: PASS.

- [ ] **Step 6: Обновить `lib/scenario/generate.ts` (инъекция retrieve + usedChunkIds)**

Добавить в `GenerateDeps` retrieval-функцию и вызвать перед `buildMessages`:

```ts
import { retrieveChunks } from '@/lib/rag/retrieve'
import type { RagChunkForPrompt } from './prompt'

type RetrieveFn = (q: {
  direction: string | null
  grade: number
  topic: string
}) => Promise<Array<{ id: string; chunkText: string; documentTitle: string; sectionKind: string }>>

export type GenerateDeps = { chat?: ChatFn; retrieve?: RetrieveFn }
```

В `generateScenario` после `const started = Date.now()`:

```ts
  const retrieve = deps.retrieve ?? ((q) => retrieveChunks(q))
  let ragChunks: RagChunkForPrompt[] = []
  let usedChunkIds: string[] = []
  try {
    const found = await retrieve({ direction: input.direction, grade: input.grade, topic: input.topic })
    ragChunks = found.map((c) => ({ text: c.chunkText, documentTitle: c.documentTitle, sectionKind: c.sectionKind }))
    usedChunkIds = found.map((c) => c.id)
  } catch (e) {
    console.error('RAG retrieval failed, generating without methodology:', e)
  }

  const messages = buildMessages(input, ragChunks)
```

И в объект `meta` добавить:

```ts
    usedChunkIds,
```

- [ ] **Step 7: Обновить тест генерации (инъекция retrieve, чтобы не ходить в сеть)**

В `tests/lib/scenario/generate.test.ts` во всех вызовах `generateScenario(input, { chat })` добавить `retrieve`, и добавить новый кейс:

```ts
const noRetrieve = async () => []

it('passes retrieved chunks into the prompt and records usedChunkIds', async () => {
  const chat = vi.fn(async () => ({ content: JSON.stringify(validScenario), usage: null }))
  const retrieve = vi.fn(async () => [
    { id: 'c1', chunkText: 'фрагмент', documentTitle: 'Методичка', sectionKind: 'stage' },
  ])
  const { meta } = await generateScenario(validInput, { chat, retrieve })
  expect(meta.usedChunkIds).toEqual(['c1'])
  const sentUser = chat.mock.calls[0][0].find((m: { role: string }) => m.role === 'user').content
  expect(sentUser).toContain('фрагмент')
})

it('still generates when retrieval throws', async () => {
  const chat = vi.fn(async () => ({ content: JSON.stringify(validScenario), usage: null }))
  const retrieve = vi.fn(async () => {
    throw new Error('db down')
  })
  const { content, meta } = await generateScenario(validInput, { chat, retrieve })
  expect(content.title).toBeTruthy()
  expect(meta.usedChunkIds).toEqual([])
})
```

(Существующие кейсы: добавить `retrieve: noRetrieve` в их `deps`, чтобы они не дёргали реальный retrieval. Сверить имена `validInput`/`validScenario` с тем, что уже в файле — переиспользовать существующие фикстуры.)

- [ ] **Step 8: Запустить — PASS**

Run: `pnpm exec vitest run tests/lib/scenario/generate.test.ts tests/lib/scenario/prompt.test.ts`
Expected: PASS.

- [ ] **Step 9: Populate `scenarios.embedding` в `app/app/new/actions.ts`**

После успешной вставки сценария (после `await db.insert(generations)...`, до `redirect`) добавить эмбеддинг входного контекста (мягко, ошибки эмбеддинга не валят сохранение):

```ts
    try {
      const { embed } = await import('@/lib/gigachat/embeddings')
      const [vec] = await embed([`${input.direction} ${input.topic} ${content.title}`])
      await db.execute(
        sql`UPDATE scenarios SET embedding = ${`[${vec.join(',')}]`}::vector WHERE id = ${scenarioId}`,
      )
    } catch (e) {
      console.error('scenario embedding failed (non-fatal):', e)
    }
```

Добавить импорт `import { sql } from 'drizzle-orm'` в начало файла, если его там нет.

- [ ] **Step 10: Полный прогон гейтов**

Run:
```bash
pnpm test && pnpm lint && pnpm exec tsc --noEmit && pnpm build
```
Expected: тесты зелёные, lint чист, типы ок, build проходит.

- [ ] **Step 11: Commit**

```bash
git add lib/scenario/prompt.ts lib/scenario/schema.ts lib/scenario/generate.ts app/app/new/actions.ts tests/lib/scenario
git commit -m "feat(scenario): inject RAG methodology chunks into generation prompt + scenario embedding"
```

---

## Task 11: Ingest «Разговоров о важном» (`scripts/ingest-razgovor.ts`)

**Files:**
- Create: `scripts/ingest-razgovor.ts`

Критический путь до демо. Берём PDF по sitemap (не слепой краул), только варианты `1s` (сценарий) и `2m` (методрекомендации), `grade_range` — прямо из URL. Вежливо: rate-limit между запросами, один проход. Парсинг — `pdf-parse`. `chunk_meta.grade_min/max` — из маппинга grade-band. Идемпотентность — через тот же `ingestDocument` (по `chunk_hash`) + `rawUrl` документа.

Источник (из памяти проекта): домен `разговорыоважном.рф` (≡ `razgovor.edsoo.ru`), `sitemap.xml` → страницы-даты `/{дата}/`, PDF по `/{дата}/{класс}/{вариант}/{файл}.pdf`. Классы: `1-2,3-4,5-7,8-9,10-11,SPO`. Варианты: `1s,2m,4r`. Прочее (видео/zip/pptx) — пропускать.

- [ ] **Step 1: Реализовать скрипт**

```ts
import { config } from 'dotenv'
config({ path: '.env.local' })
config()

import pdf from 'pdf-parse'
import { embed } from '@/lib/gigachat/embeddings'
import { ingestDocument } from '@/lib/rag/ingest'
import { drizzleIngestDb } from '@/lib/rag/ingest-db'

const BASE = process.env.RAZGOVOR_BASE ?? 'https://разговорыоважном.рф'
const SITEMAP = `${BASE}/sitemap.xml`
const LANG = process.env.PG_TSV_LANG ?? 'russian'
const DELAY_MS = Number(process.env.RAZGOVOR_DELAY_MS ?? '1500')
const MAX_DATES = Number(process.env.RAZGOVOR_MAX_DATES ?? '8') // ограничиваем объём для демо
const WANTED_VARIANTS = ['1s', '2m'] // 4r опционально — не берём по умолчанию

const GRADE_MAP: Record<string, { min: number; max: number }> = {
  '1-2': { min: 1, max: 2 },
  '3-4': { min: 3, max: 4 },
  '5-7': { min: 5, max: 7 },
  '8-9': { min: 8, max: 9 },
  '10-11': { min: 10, max: 11 },
  SPO: { min: 10, max: 11 },
}

const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms))

async function fetchText(url: string): Promise<string> {
  const res = await fetch(url, { headers: { 'User-Agent': 'KlassniyChas-RAG-ingest/1.0' } })
  if (!res.ok) throw new Error(`${res.status} ${url}`)
  return res.text()
}

function extractUrls(xml: string): string[] {
  return [...xml.matchAll(/<loc>([^<]+)<\/loc>/g)].map((m) => m[1].trim())
}

// На странице-дате ищем ссылки на PDF нужных классов/вариантов.
function extractPdfLinks(html: string, dateUrl: string): string[] {
  const links = [...html.matchAll(/href="([^"]+\.pdf)"/gi)].map((m) => m[1])
  const abs = links.map((l) => (l.startsWith('http') ? l : new URL(l, dateUrl).toString()))
  return abs.filter((u) => {
    const parts = decodeURIComponent(u).split('/')
    const variant = parts[parts.length - 2]
    const grade = parts[parts.length - 3]
    return WANTED_VARIANTS.includes(variant) && GRADE_MAP[grade] !== undefined
  })
}

function parseUrlMeta(u: string): { gradeRange: string; variant: string } {
  const parts = decodeURIComponent(u).split('/')
  return { gradeRange: parts[parts.length - 3], variant: parts[parts.length - 2] }
}

async function main() {
  const sitemap = await fetchText(SITEMAP)
  const dateUrls = extractUrls(sitemap)
    .filter((u) => /\/\d{2}-\d{2}-\d{4}\/?$/.test(u))
    .slice(0, MAX_DATES)

  let totalInserted = 0
  for (const dateUrl of dateUrls) {
    let html: string
    try {
      html = await fetchText(dateUrl)
    } catch (e) {
      console.warn(`skip date ${dateUrl}: ${e}`)
      await sleep(DELAY_MS)
      continue
    }
    const pdfs = extractPdfLinks(html, dateUrl)
    for (const pdfUrl of pdfs) {
      try {
        const res = await fetch(pdfUrl, { headers: { 'User-Agent': 'KlassniyChas-RAG-ingest/1.0' } })
        if (!res.ok) throw new Error(`${res.status}`)
        const buf = Buffer.from(await res.arrayBuffer())
        const parsed = await pdf(buf)
        const text = parsed.text?.trim()
        if (!text || text.length < 200) {
          console.warn(`skip empty pdf ${pdfUrl}`)
          continue
        }
        const { gradeRange, variant } = parseUrlMeta(pdfUrl)
        const grade = GRADE_MAP[gradeRange]
        const r = await ingestDocument(
          {
            source: 'razgovor',
            title: `Разговоры о важном — ${gradeRange} (${variant}) — ${pdfUrl.split('/').pop()}`,
            direction: null, // у методичек РоВ направление не размечено в URL
            gradeRange,
            gradeMin: grade.min,
            gradeMax: grade.max,
            rawUrl: pdfUrl,
            text,
            lang: LANG,
          },
          { embed, db: drizzleIngestDb },
        )
        totalInserted += r.inserted
        console.log(`${pdfUrl}: +${r.inserted} chunks, skipped ${r.skipped}`)
      } catch (e) {
        console.warn(`skip pdf ${pdfUrl}: ${e}`)
      }
      await sleep(DELAY_MS)
    }
    await sleep(DELAY_MS)
  }
  console.log(`\nИтого вставлено: ${totalInserted}`)
  process.exit(0)
}

main().catch((e) => {
  console.error(e)
  process.exit(1)
})
```

- [ ] **Step 2: Verify (без сети) — lint + типы**

Run: `pnpm lint && pnpm exec tsc --noEmit`
Expected: чисто. (Скрипт сетевой — реальный прогон отложен, см. Step 4.)

- [ ] **Step 3: Commit кода**

```bash
git add scripts/ingest-razgovor.ts
git commit -m "feat(rag): ingest-razgovor script (sitemap-driven, 1s+2m PDFs, rate-limited, idempotent)"
```

- [ ] **Step 4 (РУЧНОЙ, НЕ в CI): сухой прогон с малым лимитом**

> Сетевой шаг, тратит токены GigaChat и обращается к сайту. Выполняется отдельно перед демо.

Run:
```bash
RAZGOVOR_MAX_DATES=1 pnpm ingest:razgovor
```
Expected: скрипт читает sitemap, находит PDF на одной дате, парсит, вставляет чанки (`+N chunks`). Если домен/структура изменились — отладить селекторы (при недоступности сайта зафиксировать и сообщить, **не выдумывать данные**). Повторный прогон → преимущественно `skipped` (идемпотентность).

---

## Task 12: Финальная верификация фазы

**Files:** нет (проверка)

- [ ] **Step 1: Все гейты**

Run:
```bash
pnpm test && pnpm lint && pnpm exec tsc --noEmit && pnpm build
```
Expected: всё зелёное.

- [ ] **Step 2: Чистая БД — миграции с нуля (без сети)**

Run:
```bash
pnpm db:down && pnpm db:up && sleep 3 && pnpm db:migrate
```
Expected: все миграции (включая RAG-таблицы и индексы) применяются с нуля без ошибок.

- [ ] **Step 2b (РУЧНОЙ, опц., НЕ в CI): живой end-to-end**

> Только если выполнен ручной ingest (Task 8 Step 9 / Task 11 Step 4).

```bash
RAG_LIVE=1 pnpm exec vitest run tests/smoke/rag-ingest-seed.test.ts tests/smoke/rag-retrieve.test.ts
```
Expected: guarded smoke зелёные — корпус загружен, retrieval возвращает чанки.

- [ ] **Step 3: Обновить статус в CLAUDE.md**

В разделе «Статус реализации» добавить запись «Plan 3 «RAG» — ГОТОВ» с кратким перечнем (embeddings-клиент, rag-таблицы, чанкинг, гибридный retrieval, seed + razgovor ingest, RAG в промпте) и техдолгом, если остался (например: razgovor direction не размечен → фильтр направления для РоВ-чанков не работает, полагается на cosine; калибровка порога pre-match — в плане Likes/Shared).

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md
git commit -m "docs(claude.md): mark Plan 3 RAG done, record retrieval design and tech-debt"
```

---

## Self-Review (выполнено при написании плана)

**1. Spec §7 coverage:**
- Источники (seed/razgovor/shared) → Task 8 (seed), Task 11 (razgovor). Shared — вне scope (нет таблицы), отмечено.
- Чанкинг структурный 300–800 ток., chunk_meta → Task 5 + Task 7 (мета).
- Embeddings EmbeddingsGigaR 1024, батч 32, идемпотентность по хэшу → Task 2 + Task 4 + Task 7.
- Retrieval гибрид 0.7·cosine+0.3·BM25, фильтр grade/direction, diversification ≤2/doc → Task 6 + Task 9.
- BM25 `to_tsvector('russian')` + fallback `simple` через `PG_TSV_LANG` → Task 7 (ingest tsv), Task 9 (regconfig из env).
- Калибровка порога pre-match → ЯВНО вне scope (нет shared_scenarios), отмечено.

**2. Placeholder scan:** код приведён целиком в каждом шаге; seed-тела — единственный «контентный» элемент, для него дан полный шаблон + матрица покрытия (12 файлов с конкретными шапками).

**3. Type consistency:** `embed(texts): Promise<number[][]>` един везде (Task 2/7/9/10). `chunkStructured → Chunk{text,sectionKind,stageIdx?}` (Task 5) ровно так потребляется в `ingest.ts` (Task 7). `Candidate{id,documentId,cosine,bm25}` (Task 6) совпадает с маппингом в `retrieve.ts` (Task 9). `GenerationMeta.usedChunkIds: string[]` объявлен (Task 10 Step 1) и заполняется (Step 6). `RagChunkForPrompt{text,documentTitle,sectionKind}` един в prompt.ts и generate.ts.
