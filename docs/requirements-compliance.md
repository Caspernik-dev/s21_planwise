# Сверка с требованиями кейса 5 — что не выполнено

Источник критериев: `klassniy-chas-brief.md` (рубрика, 100 баллов). Дата сверки: 2026-05-27.

## Итог по критериям

| # | Критерий | Макс | Статус | Пробелы |
|---|---|---|---|---|
| 1 | Продуктовое исследование + карточка прототипа | 10 | ⚠️ риск | отчёт нет; карточка не заполнена |
| 2 | Воспроизводимость полного сценария | 20 | ✅ | — |
| 3 | Качество генерации + парсинг материалов | 20 | ✅ | — (PPTX-парсинг добавлен, v1.8.0) |
| 4 | Гибкость редактирования + параметры | 10 | ✅ | — (UI-история версий добавлена, v1.8.0) |
| 5 | Форматы вывода + статистика | 10 | 🟡 | нет полноэкранного режима |
| 6 | Техническая реализация + архитектура | 10 | ✅ | — |
| 7 | Безопасность, лимиты, конфиденциальность | 10 | ✅ | — |
| 8 | Оформление презентации + документации | 10 | 🟡 | сверить 8 блоков + ссылки; скринкаст |

## Список невыполненного (приоритет = потенциальные баллы)

### Документы (критерий 1, 10 баллов) — наибольший риск
1. **Краткий отчёт по продуктовому исследованию** — отсутствует. Нужны: выводы, **барьеры**, приоритетные пользовательские сценарии (все три дословно требуются для 100%). Трекер: backlog #12.
2. **Карточка прототипа** — не заполнена. Нужна ссылка/структура карточки из кейса. Трекер: backlog #11.

### Код
3. ~~**PPTX-парсинг** (критерий 3).~~ ✅ ВЫПОЛНЕНО (2026-05-27, v1.8.0) — `lib/parse/index.ts` принимает `.pptx` (jszip, dynamic import) наравне с TXT/PDF/DOCX.
4. ~~**UI-история версий** (критерий 4).~~ ✅ ВЫПОЛНЕНО (2026-05-27, v1.8.0) — кнопка «История» в редакторе → панель со списком снапшотов, read-only предпросмотр, `restoreVersionAction` (откат = новая версия, ничего не теряется). Спека `docs/superpowers/specs/2026-05-27-version-history-design.md`.
5. **Полноэкранный режим показа** (критерий 5). Нет проекторного/presentation-режима для показа сценария на экране в классе. Реализация лёгкая: кнопка «На весь экран» + минималистичный layout поверх существующего `components/share/ScenarioReadOnly.tsx`.

### Презентация / артефакты (критерий 8, 10 баллов)
6. **Скринкаст 5–7 мин** — записать вручную (путь: контекст → генерация → редактирование → экспорт). Трекер: backlog #13.
7. **Презентация** (`presentation/build.js`) — структура из 8 блоков соблюдена, визуализация сильная, но конкретные пробелы:
   - **Ссылки не кликабельны / отсутствуют** (критерий 8 дословно требует кликабельные ссылки на MVP, код, прототип, отчёт). Сейчас `plan-wise.ru` — обычный текст без `hyperlink`; ссылок на GitHub-код, карточку прототипа и отчёт **нет вообще**. Нужно: сделать MVP-ссылку кликабельной + добавить блок кликабельных ссылок (MVP / код / прототип / отчёт). Упирается в пп. 1–2 (карточки и отчёта ещё нет).
   - **Название команды + ФИО** — плейсхолдеры на Slide 2 («____________ • указать в финальной версии»).
   - **Скриншоты MVP** — плейсхолдеры на Slide 6 («скриншоты вставить в финал»), вставить реальные.

## Полностью закрыто (без действий)
- Критерий 2: полный путь ввод→генерация→редактор→экспорт, 3 способа задания темы, живой UAT на проде.
- Критерий 5: PDF (фирменный) + DOCX + admin-дашборд статистики.
- Критерий 6: Next.js монолит ↔ GigaChat ↔ Postgres+pgvector, Docker, README, деплой `plan-wise.ru` (HTTPS).
- Критерий 7: rate-limit на 8 точек + изоляция `user_id` везде + PII-подсистема.
