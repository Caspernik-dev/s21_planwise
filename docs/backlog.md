# Доработки (backlog)

Живой трекер оставшихся задач по проекту «Классный час». Обновлять при закрытии пунктов.
Последнее обновление: 2026-05-25 (анализ пакета предложений → добавлены #40–#63; #39 фирменный PDF сделан).

---

## ✅ Сделано и задеплоено на прод

| # | Доработка | Примечание |
|---|---|---|
| 1 | RAG-ingest «Разговоров о важном» | корпус наполнен (88 docs / 529 chunks, все классы + СПО), эмбеддинги 2560d |
| 2 | Наполнение библиотеки сообщества | 14 сценариев через `pnpm seed:shared`, все с эмбеддингами |
| 4 | Калибровка `SIMILARITY_THRESHOLD` | порог `0.78 → 0.72` (+ починен баг хойстинга в `calibrate-threshold.ts`) |
| 5 | Add/delete этапов и активностей в редакторе | `lib/scenario/edit-ops.ts` + кнопки в `editor.tsx` |
| 8 | systemd-автозапуск + restart-политика Postgres | `deploy/planwise.service`; app и БД переживают ребут |
| 14 | Актуализация README | раздел про автозапуск, СПО, add/delete |
| 7 | Замер `latencyMs` при регенерации + раздельные метрики в админке | 🎲-регенерация теперь пишет реальное время; колонка `generations.kind` ('full'/'regen', миграция `0011`); в `/app/admin` два KPI: «Среднее время генерации» (full) и «Среднее время 🎲-регенерации». **Прод: нужен `pnpm db:migrate`** |
| 15 | Прилипшая кнопка «Создать сценарий» на `/app` | Причина: карточка-ссылка библиотеки была `<Link>` = `display:inline`, на который `space-y-6` (margin-top) не действует → наезд на строку с кнопкой. Фикс: `className="block"` на `<Link href="/app/library">` (`app/app/page.tsx`). |
| 16 | Адрес-детектор PII: ложные срабатывания внутри слов | `ул`/`пер` ловились в «рег**ул**ярное», «о**пер**ативный», «**ул**ыбка». Фикс (`lib/pii/patterns.ts`): lookbehind-граница слова + точка обязательна у сокращений (`ул\.`/`пер\.`). TDD: новый тест на ложняки. |
| 18 | Hover у кнопки «Выберите файл» на `/app/plans` | Добавлены `hover:file:bg-brand-100` + `cursor-pointer` (`upload-form.tsx`). |
| 20 | Из плана нельзя перейти к готовому сценарию темы | Бейдж «✓ готов» на `/app/plans/[id]` обёрнут в `<Link href={href}>` (href к сценарию уже вычислялся) — теперь кликабелен, ведёт на `/app/scenarios/{id}`. |
| 10 | Словарь `russian` в `to_tsvector` на Docker-Postgres | Проверено вживую: конфиг `russian` есть, стемминг работает (`Дети обсуждают семейные ценности` → `дет обсужда семейн ценност`). BM25-часть гибридного поиска на полном русском стемминге, fallback `simple` не нужен. |
| 19 | Нотификация валидации «Выберите файл» не на своём месте на `/app/plans` | Убран нативный `required` у `<input type=file>` (`upload-form.tsx`) — он давал смещённый браузерный тултип. Серверная валидация в `analyzePlanAction` («Выберите файл плана.») уже была и рендерится инлайн под инпутом. |
| 6 | Вкладка «Из плана» в `/app/new` | Добавлена третья вкладка-источник «Из плана» (появляется при заходе с `?planTopicId=`, активна по умолчанию) + поясняющая заметка о привязке темы к плану. Поток `planTopicId` (скрытый input, авто-закрытие темы) был и раньше — теперь привязка видна жюри. `app/app/new/page.tsx`. |
| 3 | **Сквозной живой UAT** golden-path | прогнан против прода 2026-05-21 (живой GigaChat, аккаунт UAT): генерация+стрим, редактор ↑/↓/🎲, save-персист, PII-warning, opt-in shared с обезличиванием, экспорт PDF/DOCX (кириллица подтверждена), календарь+«На дату», 3-й источник темы, лендинг. Детали — `docs/qa.md §2–3`. Остаток — только mobile-адаптив лендинга (см. #17) |
| 21 | **РоВ-уровень генерации (per-block пайплайн)** — СМЕРЖЕНО + живой UAT | Дробление до блока (1 вызов GigaChat на активность) пробивает потолок ~800 ток./вызов; контент-план `blocks` в каркасе распределяет смыслы; катящийся контекст (`buildRunningContext`) держит связность; детерминированный per-block гейт (`lib/scenario/quality.ts`) перегенерирует тонкие блоки. `PROMPT_VERSION=v6-rov-blocks-2026-05-23`, без миграций. **Живой UAT (2026-05-23):** 14.6 КБ против прежних ~5 КБ (рост ~3×), 44.7 c, 5 блоков, 0 тонких, РоВ-стиль подтверждён. Фикс из UAT: пустой `adaptations:{}` от модели валил генерацию — каркасные adaptations сделаны мягкими (`bd8d91f`). Спека/план: `docs/superpowers/{specs,plans}/2026-05-23-rov-quality-blocks*`. **Остаток (опц.):** добивка до 18-20 КБ через env/нудж + `seed-shared --force` на проде. |
| 9 | **Self-host через Docker + переезд прода на Docker** | Раскопан корень бага «next build в контейнере ломает роуты» (`WORKDIR /app` коллизил с маршрутом `/app` → `WORKDIR /build`). Теперь `clone + docker compose up` собирает из исходников. Переключаемая LLM через env (gigachat\|openai). Серты «Минцифры» вшиты → TLS без обхода. **Прод переехал на Docker 2026-05-23**, данные сохранены (volume переиспользован). Деплой: `git pull && docker compose up -d --build`. |
| — | Форматы (8): + киноклуб, дебаты, проектная сессия | по брифу |
| — | Длительности: 20/30/40/60 | по брифу |
| — | Направления (11): супермножество (8 ФГОС + Семейные ценности/Профориентация/Здоровый образ жизни) | по брифу |
| — | Презентация | `presentation/` → `~/Planwise-Klassniy-Chas.pptx`, актуализирована |

---

## 🔴 Качество генерации (критично)

> Найдено при анализе реализации 2026-05-23 (карта кода `lib/scenario/*`, `lib/rag/*`).

| # | Проблема | Где / детали и фикс |
|---|---|---|
| 22 | ✅ **ГОТОВО** — 🎲-регенерация переведена на per-block | Было: `regenerate.ts` на старом `buildActivityMessages`. Стало: `buildBlockMessages` (роль этапа + смыслы + катящийся контекст соседних блоков) + общий `generateBlockWithGate` (тот же гейт качества `checkBlock`, что и генерация). Выделен `lib/scenario/block-gen.ts` (общий для генерации и регенерации). Ветка `feat/regenerate-per-block`. |
| 23 | ✅ **ГОТОВО** — фактологичность: промпт + баннер + дисклеймер | Решение без ненадёжного LLM-судьи: (а) правило в `buildBlockMessages` — НЕ выдумывать конкретику (даты/имена/цитаты/статистику) без опоры на RAG, иначе подавать как гипотетический пример (`PROMPT_VERSION=v7-facts-2026-05-23`); (б) постоянный баннер «⚠ проверьте факты» в редакторе (`editor.tsx`); (в) дисклеймер об ИИ в конце экспорта PDF/DOCX (`document-model.ts`). Снижает риск + честно предупреждает учителя. Ветка `feat/facts-chronometry`. |
| 24 | ✅ **ГОТОВО** — ролевой пол хронометража | `normalizeChronometry` переписан: каждый этап ≥3 мин (`MIN_STAGE_MINUTES`, но не выше равной доли), сумма точно = длительности, минимальная правка валидных входов. Починен баг раннего выхода `current===target` (тонкая рефлексия 1 мин теперь поднимается до пола). TDD. |
| 25 | ✅ **ГОТОВО** — мягкий фильтр направления | Корень оказался серьёзнее: жёсткий фильтр `direction = X` ИСКЛЮЧАЛ весь РоВ-корпус (510 чанков, direction=null) для ~половины направлений (у которых ≥3 seed-чанка → fallback не срабатывал) → генерация опиралась на тонкие seed вместо методичек. Фикс (`lib/rag/retrieve.ts`): мягкий фильтр `(= X OR IS NULL)` + удалён избыточный fallback. Живой UAT: «Патриотическое» теперь возвращает 3 чанка РоВ (раньше исключались). Без миграций. Ветка `feat/rag-soft-direction`. |
| 26 | ✅ **ГОТОВО** — усилены детерминированные проверки (без LLM) | `checkBlock` теперь: (а) каждая реплика «Учитель:» должна иметь содержимое ≥`MIN_TEACHER_TURN_CHARS` (деф.40) — пустая/обрывочная не проходит; (б) каждый вопрос discussion должен содержать «?» и быть ≥`MIN_QUESTION_CHARS` (деф.15) — отсекает «?»/«как?». LLM-судья сознательно НЕ вводился (прототип показал ненадёжность). Пороги env-тюнятся, низкие — нормальный РоВ-вывод не отбраковывают. Ветка `feat/quality-gate-deterministic`. |
| 31 | ✅ **ГОТОВО** — регенерация уважает тип/роль этапа | Решено вместе с #22. Учитель выбирает тип активности в `<select>` у 🎲 (дефолт = текущий тип); итоговый `type` форсится по выбору (`coerceActivityType(targetType)`) — модель не может «увести» введение/рефлексию в игру. Промпт несёт роль этапа (`buildBlockMessages`). |

---

## 🟡 Фичи (код, можно сделать без живого окружения)

| # | Доработка | Объём |
|---|---|---|
| 27 | **🔁 Адаптировать сценарий под другой класс / длительность** | Одна кнопка в редакторе/на карточке: тот же сценарий → пересобрать под другой `grade` или `durationMin`. Переиспользовать каркас (цели/ценности/смыслы), перегенерировать блоки под новую аудиторию. Учителя ведут одну тему в нескольких параллелях в одну неделю — сейчас надо генерить с нуля. |
| 28 | ✅ **ГОТОВО** — поиск по своим сценариям (вошло в #36) | Поле поиска по названию (ILIKE) + фильтры-селекты направление/класс/формат на дашборде; пушатся в URL, серверный компонент перечитывает `searchParams`. Единый список заменил «последние 10» (cap 100, desc). `components/dashboard/ScenarioSearch.tsx` + запрос в `app/app/page.tsx`. |
| 29 | ✅ **ГОТОВО** — загрузка своего материала как основы сценария | Разовая привязка к одной генерации (не персистентная библиотека). На `/app/new` опц. секция «Свой материал»: файл (PDF/DOCX/TXT) → `analyzeMaterialAction` (parse + PII diff + согласие как в планах). Генерация-стрим получает исходный текст + флаг согласия; **сервер пересчитывает анонимизацию** (`prepareMaterial`), эфемерно чанкует+эмбеддит и retrieve-ит релевантные теме куски (`lib/material/*`, эмбеддинги НЕ пишутся в БД), инъектит как `[TEACHER_MATERIAL]` — главный источник (выше методичек, `PROMPT_VERSION=v8-material-2026-05-24`). Без миграций. Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-user-material-source*`. **Ручной UAT перед демо** (живой GigaChat): загрузка файла с ПДн → diff → генерация без/с согласием. |
| 30 | **🔗 Шаринг по ссылке + история версий** | ✅ **(а) ГОТОВО** — read-only шаринг: колонка `scenarios.share_token` (nullable unique, миграция `0012`), enable/disable actions с мягким PII-предупреждением, публичная страница `/s/[token]` (вне `/app`, рендер через `document-model`), публичный экспорт `/api/share/[token]/export` (rate-limit по токену), «Скопировать себе» для залогиненных, CTA на регистрацию. Изоляция и отсутствие утечки приватных полей подтверждены холистическим ревью. Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-readonly-share-link*`. **ОТКРЫТО:** (б) UI истории версий + откат (`scenario_versions` пишутся, UI нет); (в) og-image + `openGraph` в `app/layout.tsx` (превью ссылок). **Долг:** `copyScenarioByTokenAction` без rate-limit (как `useSharedAsIsAction`, без LLM-стоимости). |
| 37 | ✅ **ГОТОВО** — брендинг: логотип + фавикон | Лого `/logo.svg` в `LandingNavbar`, `nav/AppNavbar`, на логине/регистрации и в футере лендинга; фавикон через `app/icon.svg` (Next отдаёт автоматически); ассеты `public/logo*.svg` (+ `logo-dark`/`logo-green`/`logo-mark`); фикс копирования `public/` в Docker-рантайм (иначе 404). Задеплоено: прод отдаёт `/logo.svg`,`/icon.svg`,`/logo-dark.svg` → 200. **Остаток → перенесён в #30:** og-image + `openGraph` в `app/layout.tsx` (нужен для красивых превью при шаринге по ссылке). |
| 36 | ✅ **ГОТОВО** — переработка дашборда `/app` | (1) Поиск по своим сценариям = #28 (выше). (2) Виджет «Ближайшие мероприятия»: чистая логика `lib/dashboard/upcoming.ts` (TDD) — 3 ближайшие будущие темы плана (парсинг `plannedDate` `DD.MM`/`DD.MM.YYYY`, дата без года → ближайшее будущее вхождение); fallback на 3 ближайших повода `CALENDAR_EVENTS` при отсутствии валидных дат плана. Каждое событие — ссылка: на готовый сценарий темы / `/app/new` префил (`?planTopicId=` или `?calendarDate=`). Изоляция по `user_id` на всех запросах. Спека/план: `docs/superpowers/{specs,plans}/2026-05-24-dashboard-redesign*`. Ветка `feat/dashboard-redesign`, без миграций. Гейты: 307 тестов (+11), tsc/lint/build. **Ручной UAT перед демо** (живое окружение). |
| 32 | **🖼 Отправить план занятий картинкой (OCR)** | Разрешить загрузку плана воспитательной работы фото/сканом (JPG/PNG) на `/app/plans` — сейчас только PDF/DOCX/TXT. Нужен OCR. ⚠️ Локальные ML-модели запрещены (RAM-бюджет) → распознавание через ВНЕШНИЙ API: vision-модель GigaChat (если поддерживает изображения) либо сторонний OCR-сервис. Решить способ при планировании. Дальше — тот же PII-diff + разбор на темы, что и для текстовых файлов. **Уточнение по UX подтверждения (продумать в рамках этой задачи, применимо и к текстовым планам):** после загрузки и распознавания НЕ сохранять сразу, а спросить «верно ли поняли?» и показать набор **редактируемых карточек** с распознанными темами/датами — пользователь может поправить любую (название темы, дату, удалить лишнюю/добавить) → затем кнопка **«Подтвердить»**. То есть либо правит и подтверждает, либо подтверждает как есть. Особенно важно для OCR, где распознавание шумит. |
| 40 | **👁 Предпросмотр перед экспортом** | Сейчас экспорт — кнопка «Скачать PDF/DOCX» без превью; пользователь не видит, как выйдет документ. Модалка `ScenarioPreviewModal` с HTML-рендером бланка (print-CSS) ИЛИ `@react-pdf/renderer` → BlobURL → `<iframe>`. Кнопка «Скачать» внутри. Высокий импакт, средний объём. |
| 41 | **↕ Drag-and-drop этапов/активностей** | Сейчас перестановка через ↑/↓ (работает, неудобно на мобиле). `@dnd-kit/sortable` (~20 КБ JS) → `onDragEnd` вызывает уже готовые `moveStage`/`moveActivity` из `lib/scenario/edit-ops.ts`. Логика есть — нужна только обёртка. Средний объём. ⚠️ Взвесить +20 КБ бандла против выгоды (CLAUDE.md §2 «Simplicity»). |
| 42 | **✅ Статус «проведено» + дата проведения** | Замыкает цикл план→сценарий→факт. `ALTER TABLE scenarios ADD conducted_at timestamp` + `conductScenarioAction(id, date)` + кнопка «Отметить как проведённое» в редакторе. Лёгкий. **Предпосылка для #51** (отчёт завучу). Изоляция по `user_id` обязательна. |
| 43 | **📱 Режим «чтения» для проведения занятия `/present`** | Учитель ведёт с телефона: stripped-down readonly-view (крупный шрифт, тёмная тема, степпер «Следующий этап / реплика», fullscreen API). Текущий редактор плохо читается в этой роли. Новый route `/app/scenarios/[id]/present`, readonly `ScenarioContent`. Высокий импакт, средний объём. |
| 44 | ✅ **ГОТОВО** — 👍 Оценка сгенерированного сценария | Миграция `0013`: `generations.rating smallint` (1=👍/−1=👎/null) + `feedback text`. `rateGenerationAction(scenarioId, rating, feedback?)` — находит последнюю `full`-генерацию сценария с изоляцией `user_id`, проставляет оценку (rating∈{1,−1}, feedback тримится/обрезается до 1000). Виджет `components/generation/RatingControls.tsx` рядом с лайком/шарингом в редакторе; при 👎 — необязательное поле отзыва; страница подтягивает текущую оценку. Без rate-limit (дешёвая запись, без LLM). Питает #57. Гейты: 307 тестов, tsc/lint/build. **Деплой: нужен `db:migrate` (миграция 0013).** |
| 45 | **✍ Кастомные инструкции / «стиль учителя»** | Поле «Дополнительные пожелания» (max 500 симв., с PII-проверкой перед отправкой в промпт) в форме `/app/new`. Без миграций: `customInstruction: z.string().max(500)` в `generationInputSchema` → в `buildSkeletonMessages`/`buildBlockMessages`. Учителя хотят свой тон/игровость/ОВЗ. Высокий импакт. |
| 46 | **🔄 Регенерация этапа целиком** | Сейчас `regenerateActivityAction` — только одна активность. Добавить `regenerateStageAction(scenarioId, stageIndex)` — когда «не то» весь этап. Переиспользовать per-block пайплайн (`block-gen.ts`) для всех блоков этапа + катящийся контекст. Изоляция `user_id` + rate-limit как у регенерации. Средний объём. |
| 47 | **⌨ Потоковый текст блока в реальном времени** | Сейчас при генерации видны прогресс-бар и названия этапов из каркаса, но сам текст блока не виден до конца блока → ощущение зависания при долгих блоках. Новый SSE-event `{type:'block_chunk', index, text}` (partial-json блока) → клиент «печатает» текст в карточке. Средний объём. Развивает существующий per-block стрим. |
| 48 | **⚡ Префил формы из плана (direction/grade) одним кликом** | Дополнение к #6: вкладка «Из плана» при `?planTopicId=` подставляет тему, но `direction`/`grade` НЕ подтягиваются из `workPlan`. Добавить: при `planTopicId` → `SELECT workPlans JOIN planTopics` (изоляция `user_id`) → prefill `direction`/`grade`. ⚠️ Проверить, есть ли эти поля у `work_plans`/`plan_topics` — если нет, источник prefill ограничен. Лёгкий. |
| 49 | **🔎 Семантический поиск в личной коллекции** | Сейчас поиск по своим сценариям (#28/#36) — ILIKE по `title`. У `scenarios` есть `embedding` (2560d) — добавить семантический («найди про дружбу»): `embed(q)` → `ORDER BY embedding <=> vec` с `WHERE user_id=...`. Инфраструктура embeddings есть. ⚠️ Многие старые сценарии могут иметь пустой `embedding` (best-effort populate) — учесть fallback на ILIKE. Средний объём. |
| 50 | **📊 Личная аналитика учителя `/app/stats`** | Сейчас статистика admin-only. Страница `/app/stats`: создано сценариев, по направлениям, экспорты, динамика за учебный год. Те же запросы что в `/app/admin`, но `WHERE user_id = session.user.id`. Данные уже в `generations`/`events`. Лёгкий. Мотивирует к активности. |
| 51 | **📥 Экспорт статистики для завуча (CSV/XLSX)** | «Сколько классных часов провёл учитель N за квартал». Зависит от #42 (`conducted_at`). `GET /api/admin/export?from=&to=&format=csv` → drizzle-запрос → CSV. Admin-функция (агрегаты по всем — как `lib/admin/stats.ts`, гейтить страницей-админкой). Средний объём. |
| 39 | ✅ **ГОТОВО** — PDF в фирменном стиле Planwise | `lib/export/to-pdf.tsx` переработан под фирстиль: фиксированная шапка с лого-маркой (SVG-примитивы `Svg`/`Path` из лого `logo-mark.svg`, без растеризации/новых ассетов) + водяной знак «Planwise» (brand-700) и подзаголовок; заголовок brand-800; мета-блок — карточка brand-50 с brand-100 рамкой и brand-700 лейблами; подзаголовки — с акцентной полоской brand-500; буллеты — фирменные точки; **дисклеймер об ИИ вынесен в фиксированный футер** (на каждой странице) + номер страницы. Палитра вписана вручную из `design_example` (Tailwind в react-pdf не применяется). DOCX оставлен нейтральным (опция). Без новых либ, без миграций. Визуально подтверждено растеризацией (кириллица PT Sans ок, 2 стр.). **Корень бага найден через systematic-debugging:** `lineHeight` на `Page` в `@react-pdf/renderer` v4 ломает отрисовку `fixed`-absolute футера (исчезает) — перенёс межстрочный интервал на текстовые блоки тела (см. коммент в `to-pdf.tsx`). **+ Вирусный промо-блок** («Создано в Planwise за пару минут» + текст + **QR на plan-wise.ru**) — чтобы коллега, увидевший распечатку/файл, перешёл сам. **Пришпилен к низу ПОСЛЕДНЕЙ страницы**; если не влезает — НЕ добавляется (без лишней страницы): `renderScenarioPdf` рендерит дважды для замера (без промо vs промо в потоке), сравнивает число страниц — влезло → финальный рендер с картинкой `position:absolute` внизу; не влезло → версия без промо. **Промо — запечённый PNG (текст+QR), а НЕ живой react-pdf-текст:** субсеттинг шрифта в @react-pdf/renderer v4 нестабильно роняет глифы в составных документах («Planwise»→«anwise», «plan-wise.ru»→«lan-wise.ru» — видно в реальных вьюерах; диагностировано бинарным поиском по составу документа). Картинка иммунна. Генерится `scripts/gen-promo.ts` (`pnpm gen:promo`, dev-dep `@resvg/resvg-js`, SVG со встроенным PT Sans → `assets/promo-card.png`) поверх `assets/qr-planwise.png` (`pnpm gen:qr`, dev-dep `qrcode`). Обе либы — **dev** (рантайм/бандл чисты). Ассеты в `outputFileTracingIncludes` для обоих экспорт-роутов (заодно починен пропуск ассетов у `/api/share/[token]/export`). |

---

## 👥 Сообщество / библиотека

| # | Доработка | Детали |
|---|---|---|
| 52 | **💬 Комментарии к сценариям в библиотеке** | Лайки есть, текстовой обратной связи нет. «Использовал с 5Б, дети активно участвовали» — ценный контент. `CREATE TABLE scenario_comments(id, shared_scenario_id FK, user_id FK, text varchar(280), created_at)` + **строгий PII-gate перед INSERT** (как `lib/community/pii-gate.ts`). Средний объём. |
| 53 | **🏷 Теги / метки (whitelist)** | Сейчас фильтр библиотеки — только direction/grade/format. Предзаданный whitelist тегов («интерактивный», «короткий», «для трудного класса», «для ОВЗ») — без свободного ввода (избегаем мусора и обхода PII). `TAGS as const` + `jsonb tags[]` в `shared_scenarios` + `TagFilter` в `/app/library`. Средний объём. |
| 54 | **🔥 «Топ недели» в библиотеке** | `likes` уже с `created_at`. `SELECT shared_scenario_id, COUNT(*) FROM likes WHERE created_at > now()-7d GROUP BY 1 ORDER BY 2 DESC LIMIT 5`. Блок «Популярное на неделе» на главной библиотеки повышает обнаруживаемость свежего. Лёгкий. |
| 55 | **🚩 «Сообщить о проблеме» (модерация)** | Нет модерации публичной библиотеки. Кнопка «Пожаловаться» с причиной (`содержит ПДн`/`не по теме`/`низкое качество`) → `CREATE TABLE reports(shared_scenario_id FK, reason enum, user_id, created_at)` → секция Reports в `/app/admin`. Лёгкий. |
| 56 | **👤 Публичный профиль учителя `/u/[slug]`** | Сейчас авторство в библиотеке анонимно. Опц. (**opt-in**) публичный профиль (никнейм/город) + опубликованные сценарии — репутация мотивирует делиться. `ALTER TABLE users ADD display_name text, profile_public boolean` + route `/u/[slug]`. ⚠️ Конфликт с приватностью: сейчас `shared_scenarios` намеренно БЕЗ `user_id`-связи наружу — раскрытие авторства требует пересмотра модели. Сложный. |

---

## ⚙️ Масштабируемость / инфраструктура

| # | Доработка | Детали |
|---|---|---|
| 33 | **🧵 Параллельные потоки к GigaChat (ограничитель конкуренции)** | Тариф GigaChat задаёт лимит ОДНОВРЕМЕННЫХ запросов: сейчас **1**, на новом тарифе будет **5**. Если уходит второй запрос, пока первый ещё в полёте → GigaChat возвращает **429**. Нужен семафор на N одновременных вызовов перед клиентом GigaChat (`lib/gigachat/client.ts`), N из env (напр. `GIGACHAT_MAX_CONCURRENCY`, деф. 1 → поднять до 5). Гейтить ВСЕ вызовы: генерация (skeleton + per-block — внимание: один сценарий = много последовательных вызовов), регенерация, embeddings, prematch. Прод — один Node-процесс → in-memory семафора достаточно (при масштабировании на несколько процессов понадобится распределённый — отметить). Связано с #34. |
| 34 | **📋 Очередь задач (перед потоками GigaChat)** | Когда все слоты GigaChat заняты (1, позже 5), новый запрос НЕ падает с 429, а **встаёт в очередь** и уходит в поток, как только слот освобождается. По сути — wait-queue поверх семафора из #33: запросы ждут свободный слот (с разумным таймаутом ожидания и обратной связью в UI «ваш запрос в очереди»). Реализуется в приложении (in-memory очередь + семафор), без тяжёлых зависимостей (не Redis/BullMQ). Объединяется с #33 в один модуль-ограничитель. |
| 38 | ✅ **ГОТОВО** — домен + HTTPS | Домен `plan-wise.ru` (Jino), A-записи `@`+`www` → `176.108.252.98`. Let's Encrypt через `certbot --nginx` (выпущен на `plan-wise.ru`+`www`, до 2026-08-22, автопродление через systemd-timer), редирект 80→443, `443/tcp` в ufw. `AUTH_URL=https://plan-wise.ru` в `.env`, `app` пересоздан. Сайт живой: `https://plan-wise.ru` 200, HTTP→301. **Грабли (на будущее):** certbot ронял интерактивный EFF-промпт при вставке многострочного блока с кириллицей в stdin → запускать ОДНОЙ строкой с `--non-interactive --no-eff-email`; в окружении VPS задан `HTTPS_PROXY=127.0.0.1:10809` (исходящие к ACME шли через него — сработало). |
| 57 | **📉 Дашборд качества генерации для admin** | В `GenerationMeta` (JSONB) уже копятся `thinBlocks`/`qualityWarnings`/`repaired`/`normalized`, но в `/app/admin` их нет (только latency + error rate). Добавить в `lib/admin/stats.ts`: `AVG(thin_blocks)`, топ предупреждений, % репарированных. Помогает калибровать промпты. Лёгкий. Питается от #44. |
| 58 | **🔒 Атомарный rate-limit (check-then-act гонка)** | **Подтверждено в коде:** `checkRateLimit` (`lib/ratelimit/index.ts`) делает `current()` SELECT → проверка `used>=limit` → `increment()`. `increment` атомарен (`onConflictDoUpdate`), но гейт — нет: два параллельных запроса прочитают `used=9`, оба пройдут лимит 10, оба инкрементнут → `count=11`. Фикс: один запрос `INSERT ... ON CONFLICT DO UPDATE SET count=count+1 RETURNING count` и сравнение результата с лимитом (decrement/ignore при превышении). Высокий импакт, лёгкий. |
| 59 | **🧵 Worker thread / разгрузка PDF-рендеринга** | `@react-pdf/renderer` рендерит синхронно в main thread → тяжёлый сценарий (>9000 симв.) блокирует event loop на 200–500 мс; при параллельных экспортах — деградация (особенно на 2 vCPU). `worker_threads`-пул ИЛИ очередь с `concurrency=1` для `renderScenarioPdf`. Средний объём. Связано с #35 (нагрузочное). |
| 60 | **📨 Очередь генераций (pg-boss) вместо SSE-в-одном-запросе** | Текущий длинный SSE-запрос (30–120 c): при обрыве соединения результат теряется. Альтернатива: генерация → очередь (`pg-boss` поверх существующего Postgres, без Redis/BullMQ), клиент поллит статус. **⚠️ Пересекается с #34** (wait-queue поверх семафора) — решить как ОДИН модуль. Сложный. Взвесить против простоты текущего стрима (CLAUDE.md §2). |
| 61 | **🔢 Миграция embedding-размерности без re-ingest** | Сейчас 2560d захардкожены в схеме. Смена модели/размерности → миграция всей `rag_chunks` (сотни строк). `ALTER TABLE rag_chunks ADD embed_model text` + lazy/incremental re-embed при несовпадении модели. Сложный. Низкий приоритет (пока модель фиксирована). |
| 62 | **♻ Кеш embed-запросов** | Популярные темы («Дружба», «Семья», «День знаний») гоняют GigaChat embeddings снова и снова → лишние токены + latency. `CREATE TABLE embed_cache(hash text PK, vector text, created_at)` по `SHA256(text)` в `lib/gigachat/embeddings.ts` (check → miss → embed → cache). Средний объём. Экономит квоту GigaChat. |
| 63 | **🧪 E2E-тесты критических флоу (Playwright)** | 281 unit-тест есть, e2e нет → регрессия в `/api/generate/stream` или форме не ловится. Минимум: регистрация → генерация → редактирование → экспорт → шаринг, с MSW-моком GigaChat. **По политике (CLAUDE.md): Playwright E2E — НЕ в CI, только manual перед демо.** Средний объём. |
| 35 | **📈 Нагрузочное тестирование** | Проверить, сколько одновременных пользователей/генераций держит прод (VPS 2 vCPU / 4 ГБ + swap). Найти узкие места: RAM при стриминге, лимиты GigaChat (rate-limit/конкурентность API), пул соединений Postgres, CPU на парсинге/эмбеддингах. Сценарии: N параллельных генераций, всплеск логинов, массовый экспорт. Инструмент: k6 / autocannon (без тяжёлой инфры). Дать выводы → калибровка rate-limit и решение по #33/#34. |

---

## 🐞 Баги UI

| # | Баг | Детали |
|---|---|---|
| 17 | Mobile-адаптив лендинга: визуальный reflow не подтверждён на глаз | Навбар-бургер для мобилы **реализован и задеплоен** (`LandingNavbar.tsx`, client-компонент). Desktop-раскладка `/` и все CTA-ссылки ОК (UAT 2026-05-21). Остаётся только глазами проверить reflow сеток на ≤640px (headless окно не сжалось). Низкий риск (Tailwind responsive-классы). Открыть `/` в DevTools device-toolbar / на телефоне. |

---

## 🚫 Из пакета предложений (2026-05-25) — НЕ записаны отдельными пунктами

| Предложение | Причина |
|---|---|
| История версий — «откатить к предыдущей» | Уже зафиксировано как **#30(б)** (`scenario_versions` пишутся, UI/откат — открыты). Дубль. |
| Автосохранение с дебаунсом | **Конфликтует с принятым решением** Plan 4: сохранение строго explicit (НЕ авто-сейв) — осознанный выбор. Полезен только **`beforeunload`-предупреждение при `dirty=true`** (защита от потери правок) — это можно сделать без нарушения решения; учесть при работе над редактором. |
| Параллельная генерация блоков (`Promise.all`) | **Ограничено #33**: лимит одновременных запросов GigaChat = 1 (на новом тарифе 5). Параллелить блоки одного сценария упрётся в семафор и поломает катящийся контекст (`buildRunningContext` последователен). Возможно только частично (по этапам) и только после поднятия конкурентности — переоценить после #33/#34. |

---

## ⚪ Обязательные артефакты кейса (баллы, не код)

| # | Артефакт | Кто |
|---|---|---|
| 11 | **Карточка прототипа** (по ссылке из кейса) | черновик можно подготовить; заполнить/отправить — вручную |
| 12 | **Краткий отчёт по продуктовому исследованию** | отдельный документ, черновик можно подготовить |
| 13 | **Скринкаст 5–7 мин** | запись экрана — вручную |
